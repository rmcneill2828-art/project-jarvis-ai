# EBR-0001 - Engineering Backlog Register

> *"Deferred work remains governed work."*

**Version:** 1.122

---

# 1. Document Control

| Field | Value |
|------|-------|
| Artefact ID | EBR-0001 |
| Title | Engineering Backlog Register |
| Version | 1.122 |
| Status | Draft |
| Owner | Programme Sponsor & Chief Engineering Advisor |
| Classification | Internal |
| Parent | CHR-0001 |
| Effective Date | 27 June 2026 |
| Review Frequency | At engineering session transition or backlog review |

---

# 2. Purpose

The Engineering Backlog Register is the authoritative register for approved, deferred, planned and candidate engineering backlog items within Project JARVIS AI.

It preserves engineering work that has been identified but not yet implemented, ensuring that deferred items remain visible, traceable and subject to Programme Sponsor review.

This register supports AIEMS by separating approved implementation scope from future engineering recommendations, technical debt, observations and candidate backlog intake.

---

# 3. Scope

This register records engineering backlog items that may require future Engineering Implementation Packages, engineering review, recovery, supersession, lifecycle assessment or controlled implementation.

It includes:

- Approved backlog items.
- Deferred engineering work.
- Planned engineering activities.
- Candidate backlog items awaiting review.
- Backlog status and priority information.

This register does not itself authorise implementation.

Backlog items shall not be implemented unless included within an approved Engineering Implementation Package or otherwise explicitly authorised by the Programme Sponsor.

---

# 4. Backlog Management Principles

Engineering backlog management shall follow these principles:

- Every backlog item shall have a unique backlog identifier.
- Approved backlog items shall remain traceable to their source where known.
- Candidate items shall remain separate from approved backlog items until reviewed.
- Backlog status shall reflect current engineering authority and disposition.
- Recommendations, observations, defects and technical debt shall not become implementation scope until approved.
- Backlog maintenance shall preserve repository-first engineering and AIEMS scope control.

---

# 5. Backlog Register

| Backlog ID | Title | Source | Status | Priority | Owner | Notes |
|------------|-------|--------|--------|----------|-------|-------|
| EBG-0001 | ADR-0004 recovery or formal supersession | ESR-0001 deferred work | Completed | Medium | Programme Sponsor | ADR-0004 recovered during ESR-0003 EIP-R2 from approved repository evidence. |
| EBG-0002 | ADR-0005 recovery or formal supersession | ESR-0001 deferred work | Completed | Medium | Programme Sponsor | ADR-0005 recovered during ESR-0003 EIP-R2 from approved repository evidence. |
| EBG-0003 | Lifecycle review of COC-0001 | ESR-0001 deferred work | Completed | Medium | Programme Sponsor | Review lifecycle status of COC-0001 and determine whether promotion or continued draft status is appropriate. **Resolved at ESR-0020**: COC-0001 promoted Draft to Approved (v1.13) alongside PBK-0001 (EBG-0004), per the same Programme Sponsor decision. Status corrected here at ESR-0021 WP5 - this row's own Status column had never been updated despite the resolution being recorded elsewhere (PST-0001, EIP-ESR0020-001, ESR-0020 session report), found during the JRM-0001 roadmap backlog audit. |
| EBG-0004 | Lifecycle review of PBK-0001 | ESR-0001 deferred work | Completed | Medium | Programme Sponsor | Review lifecycle status of PBK-0001 and determine whether promotion or continued draft status is appropriate. **Resolved at ESR-0020**: PBK-0001 promoted Draft to Approved (v1.22). Status corrected here at ESR-0021 WP5 - this row's own Status column had never been updated despite the resolution being repeatedly recorded elsewhere, found during the JRM-0001 roadmap backlog audit. |
| EBG-0005 | REG-0001 metadata alignment following P2-004A | ESR-0001 deferred work | Resolved by Attrition | Medium | Programme Sponsor | Closed at ESR-0028 WP1 per [[EIP-ESR0028-001_AIEMS_PROCESS_HYGIENE_BATCH|EIP-ESR0028-001]] Section 4.1. P2-004A (ESR-0001's "Engineering Session Lifecycle Alignment" work package, 27 June 2026) was recommended as ESR-0003's first EIP but never actioned; the item sat as Approved Backlog for 26+ sessions. REG-0001 has been continuously, actively maintained across all of those sessions (v3.205 at time of review, hundreds of version-history entries); no trace of an unresolved P2-004A-era metadata gap is identifiable in the current register, and re-deriving the original 26-session-old intent from FCH-0001's raw transcript would cost more than the item is worth for a Medium-priority, low-effort item. Disclosed openly as a judgement call (attrition, not a verified reconstruction of the original gap) rather than silently dropped. |
| EBG-0006 | REV-0002 Repository Baseline Verification | ESR-0001 deferred work | Approved Backlog | Medium | Programme Sponsor | Create REV-0002 as a controlled Repository Baseline Verification artefact. |
| EBG-0007 | STD-0004 Validation and Quality Assurance Standard | ESR-0001 deferred work; GSF-011 | Completed | Medium | Programme Sponsor | STD-0004 created during ESR-0004 EIP-ESR0004-02. |
| EBG-0008 | Create Engineering Implementation Package Standard | ESR-0002 Candidate Backlog Review; GSF-028 | Approved Backlog | Medium | Programme Sponsor | Create a dedicated controlled standard defining Engineering Implementation Package format, lifecycle, mandatory sections, numbering and approval rules to reduce repeated instruction overhead. |
| EBG-0009 | Create Engineering Session Standard | ESR-0002 Candidate Backlog Review; GSF-028 | Approved Backlog | Medium | Programme Sponsor | Create a dedicated controlled standard for Engineering Session initiation, execution, closure, handover, continuity, single active session governance and Engineering Session Archive standardisation to reduce repeated session overhead. Include an Engineering Session Archive template / standard with consistent archive sections for engineering session overview, engineering timeline, approved EIPs, baseline acceptance history, repository verification log, engineering decisions and rationale, backlog changes, AIEMS improvements introduced, session metrics, lessons learned, final repository health review outcome and handover to the next Engineering Session. |
| EBG-0010 | Define repository metadata and cross-reference validation rules | ESR-0002 Candidate Backlog Review | Approved Backlog | Medium | Programme Sponsor | Define validation rules for repository metadata, identifier uniqueness, artefact cross-references, parent artefact consistency and controlled document traceability as the rule baseline for future automation. |
| EBG-0011 | Create AI Roles and Capability Matrix | ESR-0002 Candidate Backlog Review; GSF-012 | Approved Backlog | Medium | Programme Sponsor | Create a matrix defining AI collaborator roles, capabilities, permissions, limitations, decision boundaries and tool usage across ChatGPT, Codex, GitHub connector and future AI agents, including GitHub connector capability validation boundaries. |
| EBG-0012 | Establish AIEMS roadmap and release planning artefact | ESR-0002 Candidate Backlog Review | Completed | Medium | Programme Sponsor | Create a controlled roadmap or release planning artefact showing AIEMS phases, milestones, dependencies, sequencing and target outcomes. **Resolved at ESR-0021 WP5**: [[JRM-0001_PROJECT_ROADMAP|JRM-0001]] created and Programme Sponsor-approved (v1.0), consolidating this item with EBG-0027 and EBG-0028 into one unified roadmap with AIEMS Process, JARVIS Product and UXP Evolution tracks. |
| EBG-0013 | Create Engineering Decision Index | ESR-0002 Candidate Backlog Review | Approved Backlog | Medium | Programme Sponsor | Create a searchable controlled index of ADRs, Engineering Decisions and significant governance changes to improve decision traceability. |
| EBG-0014 | Assess repository validation automation | ESR-0003 EIP-R4; GSF-020 | Approved Backlog | Medium | Programme Sponsor | Assess future automation for repository metadata and cross-reference validation after validation rules are defined; do not implement automation until separately approved. |
| EBG-0015 | Investigate JARVIS Human-AI Interaction Memory and Behavioural Intelligence Layer | ESR-0003 EIP-R4 | Approved Backlog | Medium | Programme Sponsor | Investigate future JARVIS memory and behavioural intelligence capability, including governance boundaries, risks and controls; no implementation is authorised by this backlog entry. |
| EBG-0016 | Consider renaming WP0 from "Engineering Synchronisation" to "Engineering & Repository Synchronisation" | ESR-0004 WP3 Governance Refinement | Deferred | Low | Programme Sponsor | Candidate terminology refinement for future consideration. Potentially clearer, but not required for the current implementation. |
| EBG-0017 | JARVIS Product Requirements and Capability Backlog | ESR-0004 EIP-EKR-0001 | Complete | High | Programme Sponsor | Closed at ESR-0028 WP2 per [[EIP-ESR0028-002_JARVIS_PRODUCT_REQUIREMENTS_BACKLOG_IDENTIFICATION|EIP-ESR0028-002]]. The cited source, EIP-EKR-0001 (an ESR-0004 transcript-only Codex package), was never itself a requirements backlog - it created this item as future work, not satisfied it. Investigated all candidate existing documents: [[JARVIS_PRODUCT_ARCHITECTURE|JARVIS Product Architecture]] (vision, MLP, roadmap, capability hierarchy) and [[JARVIS_CAPABILITY_READINESS_MATRIX|JARVIS Capability Readiness Matrix]] (per-capability maturity tracking) together already provide exactly what this item asks for; PCB-0001 explicitly disclaims the role in its own text, and JRM-0001 already judged this item "overdue, not urgent." Closed by **identify, not create** - creating a new document would have violated this item's own caution against duplicate product documentation. Both documents formally registered in REG-0001 for the first time (closing the Readiness Matrix's own previously-disclosed registration gap in the same package), and JARVIS_PRODUCT_ARCHITECTURE.md's Document Control identity field corrected to match its REG-0001 ID. The Readiness Matrix refreshed to v2.1: two rows found stale against direct PST-0001 evidence (Memory - ESR-0027 WP1 Personal Memory foundation; Provider Architecture - EBG-0070's production wiring at ESR-0022) corrected, along with the dependent narrative and RBL-0014 to RBL-0015 reference an Engineering Reviewer pre-implementation review confirmed were needed for internal consistency. |
| EBG-0018 | JARVIS AI Provider Abstraction Architecture | ESR-0004 EIP-EKR-0001 | Completed | High | Programme Sponsor | Define provider independence before any external AI provider integration. Preserve technology independence and avoid coupling JARVIS to one model or vendor. **Closed at ESR-0023 WP1** (Engineering Reviewer confirmed): judged substantially satisfied by the existing Sentinel provider abstraction - `aiems/architecture/CURRENT_ARCHITECTURE.md` documents execution provider abstraction, a provider registry, credential reference abstraction and three provider categories (foundation/aggregation/gateway) with explicit no-single-point-of-failure failover, backed by the live `sentinel/` implementation (OpenAI/Gemini/local adapters) and [[ADR-0018_SENTINEL_AI_EXECUTION_SECURITY_PLATFORM|ADR-0018]]. Same closure pattern as EBG-0030. |
| EBG-0019 | JARVIS Memory and Data Storage Architecture | ESR-0004 EIP-EKR-0001 | Complete | High | Programme Sponsor | Define memory, storage, privacy and consent boundaries before long-term memory or family knowledge is implemented. **Resolved at ESR-0026 WP2** per [[MDS-0001_MEMORY_AND_DATA_STORAGE_ARCHITECTURE|MDS-0001]] v1.0 (Approved) - a Session/Personal/Shared-Family memory taxonomy grounded in AAM-0001's Memory faculty and the existing product architecture; storage architecture principles (local-first persistence per ADR-0012, data-layer partitioning enforcing GAM-0001 Section 9.4's personal/shared-family boundary, an initial SQLite recommendation, technical retention/deletion tied to consent traceability); device portability bound by ADR-0012's existing decision, not a redefinition of it; and the extension point EBG-0023 (Backup, Recovery and Data Protection, gated on this item) needs once actioned. Engineering Reviewer (Codex) confirmed via the AIEMS Exchange Bridge: the GAM-0001 boundary is coherent (storage-architecture layer only, consent gate not reopened), the taxonomy matches AAM-0001/product architecture, the SQLite recommendation is appropriately scoped as a starting point - no blocking findings. Programme Sponsor approved via `sponsor-decision`. This is a specification, not a code implementation - `jarvis/memory/` remains an empty stub; a future Engineering Implementation Package would build against MDS-0001's architecture. **Final confirmation (Codex, post-commit review via the bridge): no blocking findings - committed content matches the reviewed draft, EBR-0001/REG-0001 entries are accurate.** WP2 fully closed. |
| EBG-0020 | JARVIS Guardian, Family Safety and Emergency Controls | ESR-0004 EIP-EKR-0001 | Completed | High | Programme Sponsor | Define parental oversight, child safety, human approval and pre-approved emergency action boundaries before Guardian capability is implemented. **Resolved at ESR-0023 WP3**: [[GAM-0001_GUARDIAN_AUTHORITY_AND_BOUNDARY_MODEL|GAM-0001]] Section 8 added (v1.1, Programme Sponsor-approved, Engineering Reviewer confirmed) - household role model (Administrator/Adult/Child/Guest, sourced from the original ESR-0004 EKR-0001 vision recovery), child-safe assistance boundary, emergency assistance scope, and a narrow pre-approved-emergency-action mechanism that does not soften Sentinel's EMERGENCY_CONTROL deny-by-default. |
| EBG-0021 | JARVIS Local Agent Permission Boundary | ESR-0004 EIP-EKR-0001 | Candidate Backlog | High | Programme Sponsor | Define local device control limits before local agent implementation; local agents must not receive unlimited control. |
| EBG-0022 | JARVIS AIEMS Knowledge Capability | ESR-0004 EIP-EKR-0001 | Candidate Backlog | Medium | Programme Sponsor | Investigate enabling JARVIS to understand and explain AIEMS using repository artefacts as governed knowledge. |
| EBG-0023 | JARVIS Backup, Recovery and Data Protection Guidance | ESR-0004 EIP-EKR-0001 | Candidate Backlog | Medium | Programme Sponsor | Define backup and recovery expectations before persistent memory, family data or local operational state becomes significant. |
| EBG-0024 | JARVIS Cost Strategy | ESR-0004 EIP-EKR-0001 | Complete | Medium | Programme Sponsor | Closed at ESR-0028 WP3 per [[EIP-ESR0028-003_COST_AND_STRATEGIC_VALUE_FRAMEWORK|EIP-ESR0028-003]], bundled with EBG-0045/EBG-0049 (JRM-0001 flagged this item as "likely mergeable" with that cluster). This item's own trigger condition - "before paid providers... materially affect the programme" - has now occurred via EBG-0070's live billed production usage, making timely closure appropriate rather than overdue. Cost principles are now defined in [[PEM-001_AI_PROVIDER_EVALUATION_MATRIX|PEM-001]]'s new Cost and Strategic Value Principles section (1.0 to 1.1): a generalised no-discretionary-recurring-spend default (extending the precedent already established for EBG-0057), confirmation that strategic value/product benefit are already covered by existing criteria rather than needing new ones, and a note on the institutional education credit lever. **Defined, not implemented as code or action**: no dynamic cost-aware routing logic was added to any Sentinel/provider code (that remains separate, not-yet-authorised future work, per EBG-0049's own closure); the institutional education credit lever remains unconfirmed and not actioned - no eligibility check performed, no credit claimed, sourced only from raw unprocessed chat content, not to be treated as an available resource until the Programme Sponsor confirms and actions it separately. No new document created - PEM-001 identified as the correct existing home, avoiding duplicate cost documentation. |
| EBG-0025 | JARVIS Home Assistant and Smart Home Integration Assessment | ESR-0004 EIP-EKR-0001 | Candidate Backlog | Medium | Programme Sponsor | Assess Home Assistant and smart-home integration options before any smart-home implementation package is approved. |
| EBG-0026 | Transcript Export Workflow Enhancement | ESR-0005 RBL-0006 observation; completed [[ESR-0020_ENGINEERING_SESSION_REPORT|ESR-0020]] WP6 | Complete | Medium | Programme Sponsor | **Implemented at ESR-0020 WP6**: `jarvis/gui/app.py` `_export_transcript` now writes directly to `~/.jarvis/transcripts/` with an auto-generated timestamped filename (`jarvis_transcript_YYYYMMDD_HHMMSS.md`/`.txt`), no popup save dialogue, existing status-label text as GUI acknowledgement. Reviewed by the Engineering Reviewer: no blocking issues. **Non-blocking finding, not yet actioned**: the timestamp is second-resolution only, so two exports within the same second can silently overwrite each other - future cleanup candidate is sub-second precision or an explicit collision guard (e.g. suffix increment on existing-file detection). |
| EBG-0027 | JRM-0001 JARVIS Product Roadmap | ESR-0005 closure recommendation | Completed | High | Programme Sponsor | Create a controlled product roadmap for JARVIS capability sequencing without expanding implementation scope. **Resolved at ESR-0021 WP5**: [[JRM-0001_PROJECT_ROADMAP|JRM-0001]] created and Programme Sponsor-approved (v1.0) under this reserved identifier, broadened from a JARVIS-only scope to a unified roadmap covering AIEMS Process, JARVIS Product and UXP Evolution tracks (consolidating this item with EBG-0012 and EBG-0028). |
| EBG-0028 | UXP Evolution Roadmap | ESR-0005 closure recommendation; ESR-0008 terminology alignment; [[ESR-0010_ENGINEERING_SESSION_REPORT|ESR-0010]] Section 15 phased achievability discussion | Completed | Medium | Programme Sponsor | Define staged User Experience Platform maturity from First Light through operational workspace and future product surfaces. GUI is preserved as the historical alias for earlier presentation-layer work. Per the ESR-0010 Guardian Orb design conversation (`aiems/History/Full Chat/FCH-0010_ESR-0010_FULL_CHAT_HISTORY.md`, retroactively incorporated into [[UAM-0001_GUARDIAN_EXPERIENCE_ARCHITECTURE_V1|UAM-0001]] Sections 8.1/8.2 at ESR-0017), a candidate phased sequencing for the knowledge-graph Orb specifically was discussed and should inform this roadmap's staging when actioned: Phase 1 - static live graph rendered from Obsidian/repository data; Phase 2 - cluster colours and chat UI around it; Phase 3 - agent activity/traversal animation; Phase 4 - connection to Guardian reasoning and future telemetry (e.g. GIA). **Resolved at ESR-0021 WP5**: [[JRM-0001_PROJECT_ROADMAP|JRM-0001]] created and Programme Sponsor-approved (v1.0), with this item's phase content preserved and directly referenced (not restated) as the source for JRM-0001 Track C's Guardian Orb sequencing, per JRM-0001's own no-duplication principle. This entry's phase detail remains the authoritative source for that content going forward. |
| EBG-0029 | Product Growth Philosophy | ESR-0005 closure recommendation | Candidate Backlog | Medium | Programme Sponsor | Record the principle that JARVIS grows by acquiring capabilities rather than accumulating features. |
| EBG-0030 | Sentinel Architecture Specification | ESR-0005 closure recommendation | Completed | High | Programme Sponsor | Define Sentinel responsibilities, boundaries and relationship to Guardian before Sentinel implementation. Satisfied by [[ADR-0018_SENTINEL_AI_EXECUTION_SECURITY_PLATFORM|ADR-0018]] and the Guardian/Sentinel boundary defined in ESR-0014 section 13. |
| EBG-0031 | Guardian Architecture Specification | ESR-0005 closure recommendation | Completed | High | Programme Sponsor | Define Guardian safety, permission, approval and protection boundaries before Guardian implementation. **Resolved at ESR-0023 WP2**: [[GAM-0001_GUARDIAN_AUTHORITY_AND_BOUNDARY_MODEL|GAM-0001]] (Guardian Authority and Boundary Model) created and Programme Sponsor-approved (v1.0), Engineering Reviewer confirmed. Maps Guardian authority onto Sentinel's existing trust-tier categories, defines three authority levels (Autonomous / Approval-Required / Out of Scope), and states general protection principles - with family-safety content (EBG-0020) and HITL/consent mechanics (EBG-0048) explicitly deferred rather than restated. |
| EBG-0032 | Historical Engineering Register | ESR-0005 closure recommendation | Candidate Backlog | Medium | Programme Sponsor | Consider a controlled register for historically significant engineering baselines, tags and milestones. |
| EBG-0033 | AIEMS Improvement Register / AIEMS Improvement Review | ESR-0005 closure recommendation | Candidate Backlog | Medium | Programme Sponsor | Define a controlled mechanism for recording AIEMS process improvements without disrupting active engineering flow. |
| EBG-0034 | Engineering Authority by Work Package | ESR-0005 closure recommendation | Candidate Backlog | High | Programme Sponsor | Define authority boundaries by work package type so implementation, assessment, correction and verification packages remain distinct. |
| EBG-0035 | Context Activation Guidance | ESR-0005 closure recommendation | Candidate Backlog | High | Programme Sponsor | Define how package context, repository baseline, role authority and relevant artefacts are activated before execution or verification. |
| EBG-0036 | WP6 Repository Content Verification Standard | ESR-0005 closure recommendation | Candidate Backlog | High | Programme Sponsor | Define repeatable WP6 repository content verification expectations, evidence requirements and reporting structure. |
| EBG-0037 | Engineering Package Classifications: EIP / EAP / ECP | ESR-0005 closure recommendation | Candidate Backlog | Medium | Programme Sponsor | Define controlled package classifications for Engineering Implementation Packages, Engineering Assessment Packages and Engineering Corrective Packages. |
| EBG-0038 | Formal AIEMS Standards Review | [[RBL-0007_REPOSITORY_BASELINE|RBL-0007]]; ESR-0006 continuous improvement | Candidate Backlog | High | Programme Sponsor | Validate CI-0001 through CI-0007 and determine which ESR-0006 working practices require formal standardisation. Rationale: preserves AIEMS improvement discipline while avoiding premature standard creation. |
| EBG-0039 | JARVIS Runtime Chat Archive | [[RBL-0007_REPOSITORY_BASELINE|RBL-0007]]; ESR-0006 repository readiness classification | Completed | High | Programme Sponsor | Runtime chat exports moved from repository root to `logs/chats/`, including `Jarvis one.md` and `Jarvis two.md`. Rationale: separates product runtime artefacts from AIEMS controlled artefacts while preserving export evidence. |
| EBG-0040 | AIEMS Repository Integrity Troubleshooting Playbook | [[RBL-0007_REPOSITORY_BASELINE|RBL-0007]]; ESR-0006 repository readiness review | Candidate Backlog | Medium | Programme Sponsor | Create practical troubleshooting guidance for environmental Git status, line-ending normalization, ignored workspace state and baseline-readiness checks. Rationale: captures repeatable repository integrity learning from ESR-0006. |
| EBG-0041 | Guardian Identity Architecture Validation | [[ESR-0008_ENGINEERING_SESSION_REPORT|ESR-0008]] | Completed | High | Programme Sponsor | Validate [[AAM-0001_GUARDIAN_IDENTITY_AND_COGNITIVE_ARCHITECTURE|AAM-0001]] and determine implementation sequencing for Guardian identity and faculties. **Resolved at ESR-0023 WP5**: content re-checked against current implementation (Programme Sponsor-approved, Engineering Reviewer confirmed), no contradictions found; AAM-0001 promoted Draft to Approved (v0.3) with a new GAM-0001 cross-reference. Implementation-sequencing judgement: Trust and Judgement architecturally defined but not operationally wired (see new EBG-0074, the significant finding this validation surfaced); Reasoning basic/live (EBG-0070); Memory/Voice/Vision Not Started (confirmed against JARVIS_CAPABILITY_READINESS_MATRIX); Action gated behind EBG-0021. Recommended order: EBG-0074 (wiring) first, then Memory, then Voice/Vision, then Action. |
| EBG-0042 | Agent Framework Architecture | [[ESR-0008_ENGINEERING_SESSION_REPORT|ESR-0008]]; [[ADR-0011_AGENT_FRAMEWORK|ADR-0011]] | Candidate Backlog | High | Programme Sponsor | Define specialist agent contracts, including Engineering Agent, while preserving Guardian as the singular user-facing identity. |
| EBG-0043 | Engineering Ecosystem Synchronisation Workflow | [[ADR-0013_ENGINEERING_ECOSYSTEM_SYNCHRONISATION|ADR-0013]] | Candidate Backlog | High | Programme Sponsor | Define repeatable WP0 workflow covering GitHub, AIEMS, OSE, Obsidian, registers, controlled artefacts, previous ESRs and summaries. |
| EBG-0044 | Obsidian / OSE Validation Workflow | [[ADR-0013_ENGINEERING_ECOSYSTEM_SYNCHRONISATION|ADR-0013]] | Candidate Backlog | Medium | Programme Sponsor | Define how Obsidian as the human-facing Engineering Knowledge Workspace validates and navigates repository Markdown without replacing GitHub as source of truth. |
| EBG-0045 | Cost and Strategic Value Framework | [[ADR-0008_HYBRID_AI_RUNTIME_STRATEGY|ADR-0008]] | Complete | High | Programme Sponsor | Closed at ESR-0028 WP3 per [[EIP-ESR0028-003_COST_AND_STRATEGIC_VALUE_FRAMEWORK|EIP-ESR0028-003]], together with EBG-0049 and EBG-0024 as flagged. Evaluation criteria for cost, privacy, strategic value and product benefit already existed in [[PEM-001_AI_PROVIDER_EVALUATION_MATRIX|PEM-001]] (Privacy/Data Handling, Vendor Independence, Capability Coverage, Resilience Fit); Cost specifically was named but tied for the lowest weight (10%) with a predictability-only description and no cost-minimisation posture - this closure reweights it to 15% (drawn from Latency and SDK/API Maturity, 10% to 7.5% each) with an explicit no-discretionary-recurring-spend description, and adds a new Cost and Strategic Value Principles section confirming strategic value/product benefit are already covered by existing criteria (no new criteria invented) plus a note on the institutional education-credit lever. **Criteria and framework defined, not implemented as code or action**: this closure changes evaluation criteria and principles only - no Sentinel/provider routing code was touched (dynamic cost-aware routing remains separate, not-yet-authorised future work, per EBG-0049's own closure), and the institutional education-credit lever remains unconfirmed and not actioned - disclosed only, not claimed or budgeted against, sourced from raw unprocessed chat content rather than a verified resource. PEM-001 1.0 to 1.1. No new document created, avoiding the duplicate product documentation this item's own scope note (via EBG-0049) cautioned against. |
| EBG-0046 | Device Independence and Restore Architecture | [[ADR-0012_DEVICE_INDEPENDENCE_AND_PORTABLE_RESTORE|ADR-0012]] | Candidate Backlog | High | Programme Sponsor | Define bootstrap, progressive restore, portable memory, configuration and encrypted sync requirements. |
| EBG-0047 | Sentinel Gate of Durin Architecture Specification | [[ADR-0009_SENTINEL_GATE_OF_DURIN_PATTERN|ADR-0009]] | Candidate Backlog | High | Programme Sponsor | Extend EBG-0030 with Sentinel trust gateway, trust tiers and platform-entry validation details. |
| EBG-0048 | Guardian HITL Governance Specification | [[ADR-0010_GUARDIAN_IDENTITY_AND_HITL_GOVERNANCE|ADR-0010]] | Completed | High | Programme Sponsor | Extend EBG-0031 with consent, policy, privacy, approval, memory retention and trusted mobile approve/deny governance. **Resolved at ESR-0023 WP4**: [[GAM-0001_GUARDIAN_AUTHORITY_AND_BOUNDARY_MODEL|GAM-0001]] Section 9 extended (v1.2, Programme Sponsor-approved, Engineering Reviewer confirmed) - consent mechanics scoped per action, a memory-retention consent boundary explicitly distinguished from EBG-0019's storage architecture, trusted mobile approve/deny architecture (confirmed by ADR-0010 as a future capability), and a privacy boundary reinforcement making personal/shared-family memory crossing an explicit approval gate. |
| EBG-0049 | Cost-Aware Provider Routing and PEM-001 Revisit | [[ESR-0016_ENGINEERING_SESSION_REPORT|ESR-0016]]; overlaps EBG-0045 | Complete | High | Programme Sponsor | Closed at ESR-0028 WP3 per [[EIP-ESR0028-003_COST_AND_STRATEGIC_VALUE_FRAMEWORK|EIP-ESR0028-003]]. **Policy defined, not implemented as code** - this item's own text asks to "define a policy," not build one. [[PEM-001_AI_PROVIDER_EVALUATION_MATRIX|PEM-001]] gained a new Cost-Aware Routing Policy design principle: prefer lower-cost/local/free providers by default, escalate to higher-capability/cost providers only when task requirements demand it. ESR-0016's trust-tier classification was investigated directly: `TrustTierPolicy` is genuinely production-wired (`build_default_runtime()`, per EBG-0074, Complete) but classifies purely on trust/safety dimensions with no cost dimension - it remains a "candidate mechanism" for cost-routing exactly as this item's text anticipated, not yet a confirmed one. Adding a cost dimension to `TrustTierPolicy` or wiring cost logic into `ProviderOrchestrator` is explicitly **not** delivered here and remains separate, not-yet-authorised future work, since no per-request cost or budget data exists anywhere in the repository yet. PEM-001's Cost criterion revisited and reweighted (10% to 15%) per EBG-0045's closure. The institutional cloud/education resource lever is disclosed in PEM-001's new Cost and Strategic Value Principles section as unconfirmed and not actioned (sourced only from raw, unprocessed chat content). |
| EBG-0050 | UXP-Backend Bridge Implementation | [[ADR-0019_UXP_BACKEND_INTEGRATION_ARCHITECTURE|ADR-0019]]; [[ESR-0017_ENGINEERING_SESSION_REPORT|ESR-0017]] WP1 | Completed (Foundation Scope) | High | Programme Sponsor | **Implemented at ESR-0017 WP9** (both phases originally planned for ESR-0018/ESR-0019 delivered in one session): the Tauri sidecar-managed Python process and duplex stdio JSON-RPC bridge decided in ADR-0019 - `python -m jarvis --ipc-stdio` headless entry point, JSON-RPC 2.0 message schema, `src-tauri/src/lib.rs` sidecar command and message forwarding, and `src/platformStatus.js`'s hardcoded state replaced with live data. Verified end-to-end including a real windowed Tauri app session. Remaining scope: streaming notifications - **closed at ESR-0031 WP2 (EBG-0099)**, no longer open under this item; production sidecar bundling/packaging (`tauri-plugin-shell`, still dev-mode `std::process::Command` only) - **promoted to its own first-class item, EBG-0102 (Guardian Desktop Distribution Foundation), following two independent external read-only reviews (21 July 2026) both ranking it the top deployment blocker**, tracked there rather than as residual bridge scope; full crash-restart policy beyond minimal lifecycle handling remains open under this item; per ChatGPT Engineering Reviewer's ESR-0017 WP9 review Minor Finding 2, handler exception messages should not be surfaced verbatim once external/provider-backed paths are wired through this bridge. No further implementation beyond what ESR-0017 WP9 already delivered is authorised by this backlog entry. |
| EBG-0051 | Gemini Provider Production Readiness | [[ESR-0017_ENGINEERING_SESSION_REPORT|ESR-0017]] WP3; completed [[ESR-0020_ENGINEERING_SESSION_REPORT|ESR-0020]] WP3 | Complete | Medium | Programme Sponsor | Per ChatGPT Engineering Reviewer's ESR-0017 WP3 observations, before `GeminiProvider` (`sentinel/gemini_provider.py`) is wired into any production `ProviderOrchestrator` route: (1) richer response parsing - handles safety-blocked responses, empty `parts`, tool responses and structured output, implemented at ESR-0018; (2) additional response metadata beyond `{"model": ...}` - finish reason, token usage, safety ratings, implemented at ESR-0018; (3) **required before Gemini is enabled as a production provider**: a single live smoke test against the real Gemini API, equivalent to the live OpenAI validation performed during ESR-0015 WP5 - **performed at ESR-0020 WP3**, 13 July 2026, via `scripts/gemini_provider_smoke_test.py` (Programme Sponsor-run, real billed call): Policy decision Allow, provider `gemini`, model `gemini-2.5-flash`, real generated response text received, 1 Sentinel decision, 2 audit events, audit log at `~/.jarvis/logs/gemini_smoke_test.jsonl`. All three items are now complete. Note: this closes the readiness prerequisite only - actually wiring `GeminiProvider` into a production `ProviderOrchestrator` route (regular JARVIS/Guardian runtime use, not just this standalone validation script) remains a separate, not-yet-authorised implementation decision. |
| EBG-0052 | PBK-0001/EE-0001 "Execute After Approval" Principle | [[ESR-0017_ENGINEERING_SESSION_REPORT|ESR-0017]]; [[EE-0001_INDEPENDENT_AI_PEER_REVIEW_TRIAL|EE-0001]] Section 9 ESR-0017 entry | Candidate Backlog | Medium | Programme Sponsor | Improvement finding from the EE-0001 trial, originally reported by the Programme Sponsor and independently confirmed and refined by the Engineering Reviewer (ChatGPT) in its own ESR-0017 review: following explicit Programme Sponsor approval, the Reviewer continued conversational confirmation rather than transitioning immediately into execution, and engineering outputs were produced only after further Sponsor prompting - a process-efficiency issue, not an engineering-quality issue, per the Reviewer's own characterisation. Consider formalising a standing "execute after approval, don't just acknowledge" rule in PBK-0001 (parallel to the ESR-0016A WP4 "Operational Verification Before Reporting" precedent) and/or clarifying it within EE-0001 Section 3.2's Independent Reviewer role definition. No implementation is authorised by this backlog entry. |
| EBG-0053 | EE-0001 "Review Gate Compliance" Criterion | [[ESR-0017_ENGINEERING_SESSION_REPORT|ESR-0017]]; [[EE-0001_INDEPENDENT_AI_PEER_REVIEW_TRIAL|EE-0001]] Section 9 ESR-0017 entry | **Adopted (10 July 2026)** | Medium | Programme Sponsor | Jointly recommended by the Engineering Lead (Claude) and Engineering Reviewer (ChatGPT) during ESR-0017's scorecard reconciliation: EE-0001 currently has no criterion measuring whether agreed Lead/Reviewer checkpoints occurred before subsequent implementation proceeded (ESR-0017 was the first session to actually violate this, mid-session, and self-correct once caught by the Programme Sponsor). The Reviewer specifically recommended this stay distinct from Section 5.5 Lead Scope Discipline, since scope and execution-cadence discipline measure different things. **Adopted as [[EE-0001_INDEPENDENT_AI_PEER_REVIEW_TRIAL|EE-0001]] Section 5.12 on 10 July 2026 (dated Section 8 entry, ESR-0018 WP2)**, after ESR-0018's own Lead draft self-assessment folded WP0 process/state incidents into Lead Scope Discipline anyway, demonstrating the exact ambiguity this criterion was meant to resolve. Wording: "Review Gate Compliance - measures whether agreed Lead/Reviewer checkpoints occurred before subsequent implementation proceeded." |
| EBG-0054 | Dev-Environment Setup Automation Expansion | [[ESR-0017_ENGINEERING_SESSION_REPORT|ESR-0017]] Section 15.2 | Candidate Backlog | Low | Programme Sponsor | `setup.bat` / `scripts/setup-dev-environment.ps1` were added in ESR-0017 (prompted by the Programme Sponsor moving between machines, including a work laptop) to automate a fresh clone's environment: `npm install`, `cargo build`, Python venv + editable install, pre-commit hook activation, validator and test suite as a smoke test. Verified working end-to-end and idempotent. Candidate future expansion, not yet authorised: a cross-platform (bash/zsh) equivalent for non-Windows machines; reusing the same script in CI for environment parity with local dev; an "environment doctor" mode that only diagnoses/reports without changing anything; and prerequisite-version checks (e.g. minimum Node/Rust/Python versions) rather than only presence-on-PATH checks. No implementation is authorised by this backlog entry. |
| EBG-0055 | Knowledge Graph Phase 1 - Static Live Graph | [[UAM-0001_GUARDIAN_EXPERIENCE_ARCHITECTURE_V1|UAM-0001]] Section 8.1; [[ESR-0010_ENGINEERING_SESSION_REPORT|ESR-0010]] Section 15; [[ESR-0017_ENGINEERING_SESSION_REPORT|ESR-0017]] WP4 roadmap revision | Completed (Phase 1, ESR-0019; Phase 1.5, ESR-0028) | High | Programme Sponsor | Phase 1 of the four-phase Guardian Orb achievability roadmap recorded in EBG-0028's note: a new backend capability parsing the repository's markdown artefacts and WikiLinks into a knowledge-graph structure (nodes = artefacts, edges = relationships), exposed via a new JSON-RPC method over the existing ADR-0019 bridge (for example `knowledge.graph`), and a first-pass frontend rendering. **Corrected scale estimate**: the mock-up's stated metrics (`aiems/models/UAM-0001_GUARDIAN_ORB_MOCKUP.jpg` - 6,842 nodes, 18,392 connections) are illustrative/aspirational, not the actual current repository state - confirmed by direct count at ESR-0017, ~135 markdown artefacts repository-wide (125 within `aiems/`), consistent with the Programme Sponsor's own real Obsidian graph screenshot at this scale. At ~135 nodes, rendering performance is not a significant constraint for any standard graph library (2D force-directed or a modest 3D sphere are both realistically achievable for Phase 1) - implementation should size for the repository's actual current scale, not the mock-up's illustrated future scale, and should re-assess rendering approach if/when the real node count grows by an order of magnitude. Distinct from Phase 2 (cluster colours, already-existing chat UI), Phase 3 (agent-traversal animation, blocked on GIA telemetry - not started) and Phase 4 (Guardian reasoning connection) - this item is Phase 1 only. Candidate slot: ESR-0018 or ESR-0019, per the WP4 roadmap revision recorded in `ESR-0017_WP4_ENGINEERING_REVIEW_PACKAGE.md` Sections 5/6, since EBG-0050 (originally the headline item for both those sessions) completed early at ESR-0017 WP9.

**Delivered at ESR-0019 WP2** (`jarvis/interfaces/knowledge_graph.py`, `src/GuardianOrbGraph.jsx`): the backend graph builder and JSON-RPC method were delivered as planned. The frontend delivery went further than "a separate first-pass panel" - following Programme Sponsor direction during the session (referencing the real `aiems/models/UAM-0001_GUARDIAN_ORB_MOCKUP.jpg` reference image), the graph became the Guardian Orb's actual visual presence itself, replacing the prior placeholder glow/ring/text-label animation, matching UAM-0001 8.1's original design intent rather than sitting as a disconnected panel. Implementation: a 2D force-directed layout confined to a circle (custom hard-boundary containment force, tuned and verified against the real 148-node/1220-edge graph for both radial fill and angular symmetry - centroid within 5px of true centre, 0 overlapping node pairs), with a deterministic hemisphere depth projection (z = sqrt(1 - r^2)) giving a parallax-sphere illusion from purely 2D-computed, data-driven positions - not a 3D physics engine or WebGL scene, and not decorative animation. Text labels (both a standalone hub-label attempt and the orb's own "Guardian"/"AI Companion" text) were tried and explicitly removed on Programme Sponsor decision in favour of cluster colour coding alone, with Obsidian available separately for detailed inspection.

**Explicitly not delivered, next candidates for this item's continuation**: true 3D rendering (real depth/rotation, e.g. via WebGL or a 3D scene library, rather than the current geometric 2D-to-hemisphere projection) and live animation/rotation of the sphere. The Programme Sponsor explicitly framed both as acceptable to defer ("doesn't have to be perfect first time"), to be layered on incrementally in a future session rather than attempted in one pass. These overlap conceptually with Phase 3's agent-traversal animation but are broader (an idle-rotating or genuinely 3D orb is a rendering upgrade independent of agent traversal specifically) - a future EIP should clarify whether this becomes explicit Phase 1.5 scope or folds into Phase 3. No further implementation beyond what ESR-0019 WP2 delivered is authorised by this backlog entry.

**Delivered at ESR-0021 WP4** (`src/KnowledgeGraphPanels.jsx`, new; `src/GuardianOrbGraph.jsx` refactored to export shared cluster-order/colour/degree helpers): a Knowledge Metrics panel (Nodes, Connections, Clusters, Density, Orphaned Nodes) and an Active Clusters panel (cluster name and node count, sorted descending, colour-swatched to match the Orb), both derived entirely from the existing `knowledge.graph` data - no new backend call. Surfaces Phase 2's cluster-colour data as its own panel for the first time, per the Programme Sponsor's minor-UXP-increment selection this session; status label above left unchanged pending a future session's judgement on whether this constitutes Phase 2 completion. Honest connecting/unavailable states confirmed with no graph data; populated rendering confirmed against a synthetic 42-node/5-cluster graph via Playwright, using `@tauri-apps/api/mocks.js`'s own `window.__TAURI_INTERNALS__.invoke` mechanism rather than a live backend. No further implementation beyond this WP is authorised by this backlog entry.

**Delivered at ESR-0028 WP4** (`src/GuardianOrbGraph.jsx`) per [[EIP-ESR0028-004_GUARDIAN_ORB_3D_ROTATION|EIP-ESR0028-004]], closing Phase 1.5, the continuation this item's own note above invited a future EIP to scope. `layoutSphere()` extended with a deterministic (id-hash, not random) front/back hemisphere sign per node, giving every node a real `(x, y, z)` sphere-surface position rather than only a front-hemisphere depth magnitude; a slow (~90-second full rotation) idle rotation around the Y axis - genuine nodes rotate to front and back, not a decorative spin. `prefers-reduced-motion` is respected (rotation disabled entirely, not merely slowed).

**This paragraph originally described the first implementation attempt (component-local React state on a throttled interval, re-sorting both nodes and edges by depth each tick); that description is now stale and corrected here rather than left inaccurate. The final, current state after six rounds of fixes (detailed below) is: rotation is driven by an imperative `requestAnimationFrame` loop mutating existing DOM nodes directly via refs (not React state/re-render); the update rate is capped (~12/second for nodes, ~4/second for edges, decoupled since edges are ~8.6x the element count); only nodes are re-sorted in DOM order by depth for correct front/back occlusion (edges' positions and opacity are still derived from depth each update, but their DOM order is not resorted - a deliberate performance tradeoff, since edges are thin/semi-transparent and always render behind all nodes regardless of their order relative to each other, making inter-edge depth ordering a negligible visual concern not worth its added cost); the loop also pauses when the window is hidden or the user has been idle 45+ seconds.** No new rendering dependency introduced - stays within the existing SVG/`d3-force` architecture, deliberately not WebGL/Three.js. Force parameters retuned (charge -60 to -100, link strength 0.2 to 0.05, x/y centering 0.5 to 0.53) using the Programme Sponsor's own tuned Obsidian graph-view measurements as directional signal, not literal values (this system's hard circular containment doesn't exist in Obsidian's unbounded canvas, so literal porting would misbehave) - re-verified against the real repository graph (195 nodes, 1687 edges, grown from the 148/1220 at original tuning): centroid 4.88px off-centre (original: 4.62px), 0 overlapping node pairs, 0 nodes beyond the containment boundary, matching the original tuning's own precedent. Live-verified via Playwright against the real backend graph data: node positions confirmed to change over time under normal motion, and confirmed to stay fixed under `prefers-reduced-motion: reduce`. `src/KnowledgeGraphPanels.jsx`'s shared helper exports (`computeClusterOrder`, `colorForCluster`, `computeDegree`) unchanged. **Explicitly still not delivered**: a true depth-buffered/WebGL 3D scene, real perspective or lighting - this remains an orthographic geometric rotation, disclosed honestly, not a full 3D rendering engine. Phase 3 (agent-traversal animation, blocked on GIA telemetry) and Phase 4 (Guardian reasoning connection) remain separate, not-started, later phases. No further implementation beyond this WP is authorised by this backlog entry.

**Post-implementation fix (ESR-0028 WP4)**: the Programme Sponsor observed sustained "Very high" power usage and elevated GPU/CPU load (Windows Task Manager, Microsoft Edge WebView2 process) on real hardware after this delivery - the disclosed-but-unmeasured performance risk in EIP-ESR0028-004 Section 11 Risk 1 materialised. Root cause: the rotation interval re-rendered the full SVG (~1,880 elements - 195 nodes + 1,687 edges on the real graph) 20 times per second. First attempt: reduced the rotation interval from 50ms to 200ms (20 to 5 ticks/second), cutting render load 4x. **This first attempt was insufficient** - the Programme Sponsor confirmed power usage was still "Very high" and the reduced tick rate made the rotation visibly choppy rather than smooth, disproving the assumption that per-tick movement was below the threshold of visible motion.

**Second fix (same WP)**: implemented the architectural change Section 11 Risk 1 had named as a fallback but not initially used - rotation is now driven by direct DOM attribute mutation inside a `requestAnimationFrame` loop, entirely bypassing React's state/re-render/reconciliation cycle for the animation hot path. React renders the SVG structure once (with element refs captured per node/edge); each animation frame directly sets `cx`/`cy`/`r`/`opacity` on the existing DOM nodes via those refs, using elapsed-wall-clock-time-based angle calculation (frame-rate-independent, so motion is smooth regardless of achieved FPS). Front/back occlusion re-sorting (painter's algorithm) is decoupled from per-frame position updates and only reorders the DOM every 300ms via `appendChild`, since depth-order crossings are infrequent at this rotation speed - avoiding a full DOM reorder on every frame. Live-verified via Playwright: sampled node position every 100ms over 3 seconds and found 30 distinct values across 30 samples (continuous motion, not discrete steps), and confirmed `prefers-reduced-motion` still correctly freezes rotation entirely (no rAF loop starts). `computeClusterOrder`/`colorForCluster`/`computeDegree` exports (`KnowledgeGraphPanels.jsx`'s dependency) unchanged.

**Third fix, unrelated to rotation itself**: the Programme Sponsor still observed "Very high" power usage after the rAF rewrite. Investigation found the real dominant cause was not GuardianOrbGraph.jsx at all - a pre-existing `guardian-breathe` CSS animation on `.guardian-orb` (`src/styles.css`), continuously animating a multi-layer `box-shadow` (up to 112px blur) on an infinite 4.8-second loop regardless of the rotation work. Animating `box-shadow` forces continuous repaint (not cheap GPU compositing the way `transform`/`opacity` animations are), a well-known expensive CSS pattern. This animation predates ESR-0028 entirely and was missed during the direct Programme Sponsor-instructed decoration removal earlier this session (`a5a19be`, `070eb2a`) - those commits removed `.guardian-orb`'s *static* border/box-shadow declaration but not this separate `@keyframes` rule still targeting the same element. Removed outright, consistent with the Sponsor's repeated "remove everything apart from the orb" direction. Not itself part of EBG-0055/Phase 1.5's scope - a genuine leftover from the earlier direct-instruction cleanup, only found because this WP's CPU/power investigation looked past `GuardianOrbGraph.jsx` at the wider `.guardian-orb` styling.

**Fourth fix - isolated diagnostic and final cap**: after the third fix, the Programme Sponsor also found and killed an unrelated leaked `find.exe` process (orphaned from an Engineering Implementer `find` command earlier in the session, unrelated to this component) that had been confounding every prior Task Manager reading. With that genuinely eliminated, a clean isolated comparison (rotation off vs rotation on, otherwise identical system state) confirmed rotation itself - even via the second fix's ref-based rAF rewrite - was still the real, attributable driver of "Very high" power usage (WebView2 alone: ~21% CPU, ~15.5% GPU with rotation on, negligible with it off). Root cause: `requestAnimationFrame` fires at the display's native refresh rate (60Hz or higher), and the rewrite was mutating all ~1,880 SVG element attributes on every single callback - far more frequently than this slow, 90-second rotation needs. Fixed by capping the actual DOM-mutation work to ~12 updates/second regardless of how often `requestAnimationFrame` itself fires (the scheduling loop still runs every frame for smooth, low-overhead timing, but the expensive per-element attribute writes only execute when at least ~83ms have elapsed since the last update) - `requestAnimationFrame` itself is retained rather than switching to `setInterval`/`setTimeout`, preserving its automatic pause when the window is hidden/backgrounded. Live-verified via Playwright: 30/30 distinct position samples over 3 seconds at the capped rate (motion remains effectively continuous to the eye at 12 updates/second for this rotation speed), `prefers-reduced-motion` still correctly prevents any DOM mutation.

**Fifth fix**: with a genuinely fresh app restart (ruling out stale hot-reload state) and the leaked `find.exe` gone, the Programme Sponsor confirmed via Windows Task Manager's per-process tree that `WebView2 GPU Process` specifically (14.8% CPU, 10.1% GPU) remained "Very high" with rotation on, and dropped out of the visible process list entirely (negligible) with rotation live-toggled off via the same `prefers-reduced-motion` setting, no restart needed - the cleanest isolation yet, conclusively confirming rotation as the sole remaining driver. A stylesheet audit for other always-rendering expensive effects (`box-shadow`, `filter`, gradients) found nothing else animated. Two further optimisations applied: (1) edges (1,687 on the real graph, ~8.6x the 195 nodes - the dominant share of the ~1,880 mutated elements) now update at a third of the node rate (~4/second vs ~12/second), since their thin, low-opacity lines lagging slightly behind node motion is imperceptible, while node motion itself - the visually salient part of "rotation" - stays at the full capped rate; (2) added a Page Visibility API check so the rAF loop's DOM-mutation work is skipped entirely whenever the window is hidden/backgrounded/minimised, not just when reduced-motion is set. Live-verified via Playwright: node position 30/30 distinct samples (unchanged smoothness), edge position 13/30 distinct samples (confirms the ~3x rate reduction took effect), `prefers-reduced-motion` still correctly freezes everything.

**Sixth fix - idle-timeout pause**: the Programme Sponsor raised a forward-looking concern after the fifth fix (see EBG-0081): sustained continuous animation prevents the GPU from reaching its deepest idle/power-saving states regardless of how cheap each individual update is, a real cost for as long as the window is open and visible even if the user isn't actively looking at it. Added a 45-second no-interaction idle timeout (mouse/keyboard/wheel/touch activity tracked via a ref, not React state, so idle/active transitions don't restart the animation effect) - rotation pauses after 45 seconds of no interaction and resumes on the next interaction. Implemented with delta-based angle accumulation (rather than absolute-elapsed-since-mount) specifically so pausing and resuming - whether via idle timeout or the existing page-visibility check - never causes the rotation to visibly jump forward to "catch up" on resume; `lastFrame` is kept pinned to the current timestamp throughout any paused period, so the first post-resume frame's delta reflects only real time since resuming. Live-verified via Playwright with the idle timeout temporarily lowered for the test (reverted before commit): position confirmed frozen throughout an idle window, and confirmed to resume with only a small, proportionate movement (2.7px over 300ms) rather than a jump reflecting the whole idle gap.

**Seventh fix, per Engineering Reviewer (Codex) post-commit findings on the sixth fix**: Finding 1 (Medium) - the sixth fix's "keep `lastFrame` pinned every frame while paused" strategy only works if the `tick` callback keeps executing during the pause. True for the idle-but-visible case (the browser has no reason to suspend `requestAnimationFrame` just because the user isn't interacting), but not for a hidden/minimised/backgrounded window - browsers commonly suspend or heavily throttle `requestAnimationFrame` entirely while hidden, so the pinning logic inside `tick` might never run during that period, and the first callback after visibility returns could add the *entire* hidden duration to the rotation angle in one jump. Fixed with an explicit `visibilitychange` listener that resets `lastFrame` the moment visibility returns, independent of whether any `requestAnimationFrame` callback ran during the hidden period. Live-verified via Playwright with a corrected test methodology - genuinely suspending `requestAnimationFrame` (buffering the callback rather than dropping it, matching real browser behaviour of delaying rather than cancelling) for a 2-second simulated hidden period: position confirmed frozen throughout, and confirmed to resume with only a small movement (2.6px over 300ms), not a jump reflecting the 2-second gap. Finding 2 (Low) - this entry's original delivery paragraph (above) still described the first implementation attempt (React state, throttled interval, both nodes and edges re-sorted by depth), now stale after six rounds of architecture changes; corrected in place rather than left inaccurate, with the current final state summarised and the fix-by-fix history below retained for the full trail. |
| EBG-0056 | PCB-0001 Product Capability Baseline Refresh | [[RBL-0013_REPOSITORY_BASELINE|RBL-0013]] Section 6; completed [[ESR-0020_ENGINEERING_SESSION_REPORT|ESR-0020]] WP4 | Complete | Medium | Programme Sponsor | [[PCB-0001_PRODUCT_CAPABILITY_BASELINE|PCB-0001]] was materially stale (last updated 1 July 2026): Section 6 falsely stated "Guardian capability is not implemented" and Section 4 described only the Tkinter GUI, not the live Tauri UXP. **Refreshed to v2.0 at ESR-0020 WP4**, reflecting Sentinel/Guardian foundation, the live UXP-backend bridge, the Guardian Orb knowledge graph and both live-validated provider adapters. Reviewed by the Engineering Reviewer: no material overstatement found, refresh judged if anything conservative. **PCB-0001 v2.1 accepted by the Programme Sponsor, 13 July 2026** - this backlog item is now closed. **Non-blocking finding, not yet actioned**: some Section 4 wording still reads as an accepted-baseline snapshot dated to the refresh rather than the later acceptance - candidate for wording polish later if stricter consistency is wanted, not required to close this item. |
| EBG-0058 | PBK-0001 Clause Consolidation | ESR-0020 closure discussion | Complete | Medium | Programme Sponsor | Closed at ESR-0028 WP1 per [[EIP-ESR0028-001_AIEMS_PROCESS_HYGIENE_BATCH|EIP-ESR0028-001]] Section 4.4, addressing all three of this item's own named starting candidates plus one independently-found cluster. **Named Candidate 1 (Approval Before Change / Working Report Lifecycle / Engineering Scope Control)**: genuine overlap only at the edges, not throughout - Working Report Lifecycle's closing sentence trimmed to a cross-reference to Principle 3; Engineering Scope Control's checklist bullet retained unchanged (removing one bullet from a five-item operational checklist would hurt scannability more than the duplication costs). **Named Candidate 2 (Operational Verification Before Reporting vs Validation Before Completion)**: not accidental duplication - Operational Verification's own text already states it deliberately operationalises Principles 2 and 4 for repository/tool operations; retained both sections unchanged. **Named Candidate 3 (Feature-First Delivery Discipline's four sub-clauses)**: each targets a distinct, non-overlapping failure mode; no genuine redundancy found, all four retained unchanged. **Independently-found Candidate 4 (repository-operations-authorisation)**: the clearest, lowest-risk win - a near-verbatim sentence appeared three times (Session Initialisation item 14; Repository Lifecycle opening sentence; Git Operations opening sentence) with zero added content at each restatement. Consolidated: item 14 retained as the sole canonical statement, the other two now cross-reference it (PBK-0001 1.27 to 1.28). Closed as `Complete` because each of the four candidates was actually reviewed and disposed, not because every candidate resulted in a merge - satisfying this item's own "identify... and merge... without losing the substance of any" framing (some substance is worth keeping exactly where it is). Bundled in the same package: the already-flagged PBK-0001 RBL-0014 to RBL-0015 stale baseline reference fix (Related Artefacts, OSE Relationships), unrelated to this item but the same file and Work Package. Original scope note retained below for full traceability. Original context: PBK-0001 has grown by accretion over 20 sessions: a new clause added after most individual incidents (Operational Verification Before Reporting at ESR-0016A; Feature-First Delivery Discipline at ESR-0017, extended at ESR-0019; the still-uncationed EBG-0052 "Execute After Approval" candidate from ESR-0017). Raised directly by the Programme Sponsor after ESR-0020 recorded two self-disclosed process deviations (implementation preceding Engineering Reviewer review, recurring despite an initial correction - Sections 9A/9B): PBK-0001 already states the relevant principle ("Approval Before Change," one of the Five Foundational Principles) and describes it again from at least two more angles (the Working Report Lifecycle; Engineering Scope Control) without those restatements being clearly identified as the same rule. The Programme Sponsor's concern, by direct analogy: continually adding directives to an already-large rule set risks the RoboCop 2 failure mode - accumulated instructions that overlap, partially restate each other, or drift toward quiet contradiction, rather than making the document easier to actually follow. **No implementation is authorised by this backlog entry.** Scope for the eventual work: identify PBK-0001 clauses that restate the same underlying principle from different angles (starting candidates: Approval Before Change / Working Report Lifecycle / Engineering Scope Control's overlap; also worth checking Operational Verification Before Reporting against Validation Before Completion, and Feature-First Delivery Discipline's sub-clauses against each other) and merge them into fewer, clearer statements without losing the substance of any. Given PBK-0001 is the document that itself defines the Engineering Reviewer's role, editing it should go through Engineering Reviewer review before implementation at least as rigorously as any other change - arguably more so. |
| EBG-0057 | Claude&harr;Codex Engineering Bridge | ESR-0018 post-appointment discussion; [[EE-0001_INDEPENDENT_AI_PEER_REVIEW_TRIAL|EE-0001]] Section 9 ESR-0018 entry | Complete | Medium | Programme Sponsor | Following the Section 7 appointment (Claude Lead Engineer, ChatGPT Lead Reviewer, permanent), a file-based, command-driven bridge to let the two exchange Work Packages directly rather than the Programme Sponsor manually relaying between two windows. Architecture (proposed by ChatGPT as Lead Reviewer, endorsed by Claude as Lead Engineer): `.aiems-exchange/` repository-local directory (inbox/outbox per role, append-only transcript, locks); structured handover files (plain header block, not YAML, to avoid a new parser dependency) carrying message/session/work-package/sender/recipient/type/repository-ref/files-in-scope metadata; three explicit commands (`submit-to-review`, `return-findings`, `submit-response`) with no autonomous multi-turn loop and no default-continue human prompt; role-locked permissions (Claude write-capable only when a handover is Sponsor-stamped `programme_sponsor_authorisation: true` - a field neither AI can set itself; Codex read-only, findings only, never direct edits); continuity via repository state and transcript files rather than CLI session-resume flags (Claude Code `-r` and Codex `exec resume --last` both deliberately dropped - `--last` risks silently resuming an unrelated Codex session, and resume is unnecessary once every handover is self-contained); automatic evidence capture (`pytest` / `validate_repository.py` output attached to each handover, not interpreted, matching how the Independent Reviewer verified WP1 by hand this session). Cost: Claude Pro (existing, unchanged) plus a new personal ChatGPT Plus subscription ($20/mo) dedicated to Codex, kept separate from the Programme Sponsor's ChatGPT Business workspace, which was confirmed to have no Codex seat available and closed to new provisioning since 24 June 2026. An independent Lead Reviewer code review of the original prototype script (`claude_codex_bridge.sh`, an earlier autonomous-loop version, superseded by this architecture) was completed by ChatGPT and reconciled by Claude - full detail in [[EE-0001_INDEPENDENT_AI_PEER_REVIEW_TRIAL|EE-0001]] Section 9 ESR-0018 entry. Findings folded into this architecture: `sponsor-decision` is its own explicit Sponsor-only command producing a timestamped, structured record - not a flag on `submit-to-review` and not a trusted text label anywhere in relayed content (the original prototype's plain `HUMAN_TAG` string was found to be spoofable, since nothing prevented an AI response or repository content from containing the same text and being mistaken for genuine Sponsor authorship); `.aiems-exchange/` must be `.gitignore`d with restrictive file permissions, not left writable in the repo root (the prototype wrote its own transcript into the repo root while incorrectly reporting "both read-only," a real bug, not a nitpick); command flow adds `bridge init <session> <wp>` ahead of the three handover commands; every invocation performs CLI/auth preflight checks (`claude`/`codex` present, versions, `codex login-status`) before acting; the repository HEAD is captured at both submission and review time so drift between the two can be detected and flagged, not silently ignored; session IDs (Claude `-r`, Codex `exec resume`) are retained only as an optional convenience, never as the authoritative continuity source - repository state and the handover/transcript files are. **Venue reconsidered post-closure, not superseding the above design, refining where it runs.** Discussion after ESR-0018 closed questioned whether VS Code should be the venue at all. Ruled out: browser automation of either AI's consumer web UI (via tools such as n8n/Puppeteer or "AutoGen web-driver extensions", raised via a Gemini research session) - OpenAI's Terms of Use explicitly prohibit automated/programmatic access to ChatGPT outside the API, with enforcement extending to account suspension; this would risk the Programme Sponsor's actual paid subscriptions and was rejected outright, not merely deprioritised. Retained as portable regardless of venue: reading authoritative files live (e.g. from a raw GitHub URL) rather than pasting potentially-stale copies - a principle both ChatGPT (reviewing the original bridge script) and, independently, Gemini (in the same research session that also produced the rejected browser-automation idea) converged on separately. **EBG-0060 overlap resolved post-ESR-0024 (Programme Sponsor-approved):** EBG-0060's DCE component (single-AI direct execution) is Superseded by this architecture's two-AI handover model, not complementary to it - see EBG-0060's entry for the full disposition, including the ESR-0023 write-boundary incidents as direct empirical evidence against DCE. EBG-0060's REA component (a future agent automating the mechanical commit/diff/reporting execution step, with human review before acceptance) is folded into this item's own future-phase scope as a plausible extension, not a competing item - a candidate for a later phase once the core handover architecture is implemented and proven, not part of this item's own MVP scope. **Also decided post-ESR-0024 closure: this workstream shall run as a separate AIEMS/engineering-tooling stream, outside JRM-0001's JARVIS/Guardian product tracks** - it governs how the two AI engineering collaborators exchange work during sessions on this repository, not any Guardian cognition, Sentinel, or UXP capability. **Resolved (MVP implemented) at ESR-0025 WP1** per [[EIP-ESR0025-001_AIEMS_EXCHANGE_BRIDGE_MVP|EIP-ESR0025-001]] v1.0 (Engineering Reviewer approved v0.2 after one High and one Medium finding addressed - a canonical transcript-only location for `sponsor-decision` records, since it has no Claude/Codex inbox to route to; and repository-drift since sponsor approval now hard-blocks `submit-response` rather than only warning - Programme Sponsor approved). `scripts/aiems_bridge.py` implements all five commands (`init`, `submit-to-review`, `return-findings`, `sponsor-decision`, `submit-response`). Role-locking is a real code gate, not only a documented convention: `return-findings` has no code path capable of writing outside `.aiems-exchange/` (no file-path argument exists to exploit), and `submit-response` hard-refuses - before any evidence capture or file write - unless the transcript's most recent `sponsor-decision` approves and the current repository HEAD matches the exact state that decision approved. 15 new tests (227 total) prove both properties directly, plus the drift-refusal-then-fresh-approval-clears-it sequence. A live dry run confirmed the authorisation-refusal, approval-success and preflight-refusal paths for real (not simulated) from the Engineering Implementer's own environment; the full successful cycle with real `claude`/`codex` CLI presence could not be verified from that same sandboxed environment (neither binary is on its `PATH`) and remains for the Programme Sponsor's own terminal to exercise when convenient - disclosed as an honest gap, not asserted as tested. REA (execution-automation agent) remains this item's own future-phase scope, not part of this MVP. **v1.0 to v1.1, post-implementation Engineering Reviewer findings addressed:** High - `session`/`work_package` fed unsanitised into path construction could escape `.aiems-exchange/` via `..`/path-separator injection, undermining the return-findings containment claim; fixed by validating both identifiers inside `_wp_key()`, verified live via the real CLI plus 6 new tests. Medium - `capture_evidence()` never checked validation return codes, so a failing run could produce a handover that looked successful; `submit-response` now hard-refuses on validation failure (3 new tests), `submit-to-review` stays non-blocking but its evidence now opens with an unmissable `VALIDATION: PASSED`/`FAILED` marker. **v1.1 to v1.2, a further post-implementation Medium finding (TOCTOU) addressed:** `submit-response` read the transcript, checked drift, ran preflight and captured evidence all before acquiring the Work Package lock, only locking for the final write - a concurrent `sponsor-decision` could land in that window and the response would still proceed on the stale approval already read. Fixed by moving the entire read-check-evidence-write sequence inside the lock, matching every other command's lock-before-write discipline; proven by a test injecting a concurrent `sponsor-decision` during evidence capture and asserting it is genuinely blocked. **Post-closure (ESR-0025A, Programme Sponsor-authorised deviation from the standard review-before-implementation lifecycle):** `claude`/`codex` CLIs were installed and authenticated for the first time on the Engineering Implementer's machine, surfacing a genuine Windows-specific bug in `run_preflight()` - `subprocess.run` crashed on npm's `.CMD` shim files without `shell=True`. Fixed and live-verified end-to-end (all five commands run cleanly against the real, authenticated CLIs). 238 tests total.

ChatGPT, as Lead Reviewer, researched and proposed a four-phase venue roadmap, verified accurate by Claude on the two technical protocol claims (Agent2Agent/A2A: real, Linux-Foundation-governed, reached v1.0 in 2026; MCP: stdio and Streamable HTTP are indeed its standard transports, and the 2026-07-28 MCP spec release candidate is moving the protocol stateless-at-the-protocol-layer - independently converging with this design's own repository-state-not-session-memory principle):

- **Phase 1 (recommended prototype target): GitHub-native handover.** Claude creates a structured handover artefact/issue; Programme Sponsor triggers review (`repository_dispatch` allows this externally, without either AI running inside VS Code); ChatGPT/Codex reads the referenced commit and posts findings; Claude responds in a new immutable handover; Sponsor approval remains a separate recorded action. Preserves everything already decided: repository as source of truth, full attributability, exact commit refs, no hidden session continuity, Sponsor gates every transition.
- **Phase 2: small JARVIS Review Gateway** - `.aiems-exchange/`'s design moved outside VS Code into a small local/hosted service (`submit_proposal` / `request_review` / `return_findings` / `submit_response` / `record_sponsor_decision`), storing structured messages and state transitions; transports and records evidence, does not itself judge PBK-0001 compliance.
- **Phase 3: MCP as the shared tool-access layer** - both AIs read/submit handovers through the same MCP server tools (`get_current_session`, `read_handover`, `submit_review`, `record_sponsor_decision`, `get_repository_ref`); explicitly not treated as the governance model itself, only the access layer.
- **Phase 4: A2A interoperability, only if justified** - Agent Cards for each role, handovers mapped onto A2A Tasks/Messages/Artifacts; recommended only if JARVIS later coordinates multiple agents/services/machines, not before. A durable message broker (e.g. NATS JetStream) was considered and explicitly deferred as more infrastructure than a two-agent, one-Sponsor setup currently needs.

**Budget constraint, stated explicitly by the Programme Sponsor: no budget for new recurring spend beyond what is already decided** (Claude Pro; the new personal ChatGPT Plus subscription for Codex). Every phase above is achievable self-hosted at no additional cost - Phase 1 within GitHub Actions' free tier, Phases 2-3 run locally on the Programme Sponsor's own machine. Phase 4 (A2A) and any durable message broker are the only places genuine hosting cost could arise, since both presuppose remote/multi-machine agents this project does not yet have - reinforcing, not just permitting, why both remain explicitly deferred. Self-hosted, no new recurring spend, is the hard default for any future proposal against this backlog item unless the Programme Sponsor states otherwise.

**Plus-tier rate-limit expectation, for calibrating real-world bridge usage.** Per a third-party pricing tracker (not an official OpenAI page - treat as approximate, not exact), ChatGPT Plus's Codex allowance is roughly a 50-message soft cap and ~200-message hard cap per rolling 5-hour window, with the same source noting "heavy users hit Plus hard caps within 1-2 hours of focused work." This matches the Programme Sponsor's own prior experience on Plus (previously hit the time window, not credits) and confirms the earlier finding that the wall to expect is a rate limit, not a bill. Worth designing the eventual bridge's `sponsor-decision`/review cadence with this in mind - space Work Package handovers out rather than pushing many through in one sitting, consistent with the guidance already recorded above.

**Empirical update, ESR-0019 (post-ESR-0018 closure): the Business workspace can apparently run Codex without spending workspace credits, refining the earlier "no Codex capacity" finding.** The Programme Sponsor used Codex through the ChatGPT Business account (not the planned new personal Plus subscription) for ESR-0019 review work, and confirmed zero workspace credits were consumed - known directly, since none remained available. The only change made was selecting the `gpt5.4 mini` model at medium speed. This does not contradict the earlier Members-page check (no usage-based Codex *seat* type is offered on this workspace, confirmed directly and still accurate) - it indicates the bundled standard-seat allowance apparently supports running Codex on a lighter model without drawing on the credit-based seat mechanism at all, which was only speculated as a possibility, not confirmed, when the original cost decision was made. **Consequence: the new personal ChatGPT Plus subscription should be treated as not yet needed, pending confirmation this bundled-allowance path holds up over more than one session's use** (does not start drawing credits unexpectedly, does not hit some other undocumented limit) - do not action the new Plus subscription purchase on the strength of this one data point alone. If it continues to hold, this would mean the bridge's Codex side costs nothing beyond the existing Business plan, better serving the no-new-recurring-spend constraint than the originally decided path.

**Corroborated with direct usage-meter evidence, same day.** ChatGPT's own Settings -> Usage panel confirms the mechanic: a **monthly** usage limit ("resets 11 Aug 2026"), shared across Codex, Work, Workspace Agents and ChatGPT for Excel, explicitly excluding Chat conversations - a materially different mechanic from the 5-hour rolling window documented above for the Plus tier specifically; the two should not be conflated. After an afternoon of real ESR-0019 review work (usage was at 100% that afternoon), the meter showed **99% remaining**. This is stronger evidence than the zero-workspace-credits observation alone - a near-untouched monthly allowance after a genuine review session. Still one observation period, not a full monthly cycle - worth checking the meter again as 11 Aug 2026 approaches before treating the bundled-allowance path as fully proven, but this now clearly outweighs the case for the new Plus subscription rather than merely offsetting it.

No implementation is authorised by this backlog entry; a proper Engineering Implementation Package for a future ESR is required before any of this is built. |
| EBG-0059 | Engineering Assurance Capability (EAC) Implementation | ESR-0012 WP1/WP2; discovered during ESR-0021 WP6 historical audit | Candidate Backlog | High | Programme Sponsor | ESR-0012 developed a complete architecture spec for an independent, process-centric assurance layer, distinguishing three concepts: **Engineering Assurance Capability (EAC)** - "provides independent, deterministic verification of AIEMS engineering process execution... does not participate in engineering reasoning or engineering decision making"; **Engineering Assurance Agent (EAA)** - one possible implementation of the EAC; **Engineering Assurance Review (EAR)** - a governance mechanism for reviewing challenged assurance findings. The spec covered six operational principles, six interfaces (Programme Sponsor, Engineering Implementer, AIEMS, Repository, HABEI, Guardian), an information model (Assurance Record, Behaviour Record, Drift Record, Evidence Record, State Record - explicitly ephemeral/stateless), 8 operational scenarios derived from ESR-0001-0012 evidence, and a full adversarial specification review (ASR-001 through ASR-010, all Pass). A concrete Work Package plan followed - WP4.1 (EAC Bootstrap), WP4.2 (Behaviour Observation Engine), WP4.3 (Assurance State Engine), WP4.4 (Drift Detection Engine), WP4.5 (Assurance Reporting), WP4.6 (Integration Testing), WP4.7 (Engineering Review) - all recorded as "Planned," none implemented. Mid-session the AI proposed folding EAC into EBG-0042 (Agent Framework Architecture) rather than creating a new artefact, and pivoted effort toward GIA instead; **that fold was never actually executed** - EBG-0042's current text has no mention of EAC, EAA or EAR. ESR-0012's own closure report and PST-0001 (at ESR-0013 closure) both list "Future EAC implementation" as deferred, alongside Conversation Engine expansion, Guardian Developer Console and GDP-0001 - the same cluster of perpetually-deferred, never-backlogged items this audit was directed to find. Directly relevant to reducing reliance on a human noticing process deviation - the exact failure mode behind ESR-0020's two self-disclosed process deviations (Section 9A/9B of that session's report). Cross-reference EBG-0042 (Agent Framework Architecture) and EBG-0055 (blocked pending further GIA implementation). No implementation is authorised by this backlog entry. |
| EBG-0060 | Direct ChatGPT Execution (DCE) / Repository Execution Agent (REA) Governance Model | ESR-0010/ESR-0011; discovered during ESR-0021 WP6 historical audit | Superseded (DCE) / Approved Backlog (REA, folded into EBG-0057) | Medium | Programme Sponsor | After live experimentation at ESR-0010 (ChatGPT successfully created/deleted a test file via the GitHub connector; one attempt blocked by OpenAI safety checks), the session formally proposed **Direct ChatGPT Execution (DCE)** as a controlled exception execution path with mandatory controls, and a **Repository Execution Agent (REA)** as a future dedicated governed agent bridging the reasoning-chat/GitHub-chat/Codex execution islands (scope validation, no-secrets handling, create/fetch/delete capability testing, full commit/diff reporting, GitHub Desktop human review before acceptance). ESR-0010 explicitly deferred this as "Repository Execution Agent / DCE governance assessment"; ESR-0011 carried it forward without resolving it; ESR-0012 pivoted entirely to GIA and the AIEMS Engineering Agent, leaving DCE/REA an orphaned thread with no EBG number since. JRM-0001 flagged this against EBG-0057 (Claude-Codex Bridge) as an overlap pair needing a combined judgement before either is next actioned. **Resolved (post-ESR-0024, Programme Sponsor-approved):** the two components are not equivalent and were disposed separately. **DCE (single-AI direct execution by ChatGPT/Codex, without Claude in the loop) is Superseded, not complementary** - it is the exact opposite design choice from EBG-0057's two-AI handover model, where Codex is deliberately hard-capped to read-only/findings-only and never writes directly. ESR-0023's write-boundary incidents (Codex twice attempting direct repository writes, both caught as violations, root-caused to a permissive local tool-config setting since removed - EE-0001 Section 7.4) are direct empirical evidence against building DCE, not merely evidence of redundancy: it would deliberately re-open the exact door EBG-0057 and that incident both close. **REA (a future agent automating the mechanical commit/diff/reporting execution step, with human review before acceptance) is a genuinely separate idea, not superseded** - it concerns automating the Engineering Implementer's own execution step (currently manual, per PBK-0001's Git Operations section), not which AI is permitted to write. Folded into EBG-0057's future-phase scope rather than retained as its own competing backlog item, since it is a plausible extension of that architecture, not an alternative to it. No implementation is authorised by this backlog entry beyond the disposition recorded here. |
| EBG-0061 | Engineering Workspace Standard (EWS-0001) | ESR-0010/ESR-0011; discovered during ESR-0021 WP6 historical audit | Candidate Backlog | Medium | Programme Sponsor | ESR-0010 produced a full EWS-0001 Draft v0.1 covering engineering principles, a tool-responsibility matrix (ChatGPT Desktop/Obsidian/VS Code/GitHub Desktop/GitHub Connector roles, later echoed almost verbatim in ESR-0010's own chat summary Section 5), workflow and governance model. It was twice explicitly deferred as a controlled artefact "if required" - once at ESR-0010 closure, once at ESR-0011 WP6 gap validation ("no immediate creation of new controlled artefacts recommended") - and never resolved either way. Real content already exists to draw from (the tool-responsibility model is still operative practice today); this item is about formally converting it into a controlled standard rather than leaving it as an unrecorded working assumption. No implementation is authorised by this backlog entry. |
| EBG-0062 | CPS-0001 Codex Prompt Standard | ESR-0007; discovered during ESR-0021 WP6 historical audit | Candidate Backlog | Low | Programme Sponsor | Proposed and approved at ESR-0007 as "a Codex Prompt Standard (CPS-0001) that defines the canonical structure for Engineering Execution Packages," explicitly deferred to ESR-0008, never mentioned again and never created. Lower priority than other Tier 1 items since the underlying need (consistent EIP-style package structure) has since been substantially met by 20+ sessions of established EIP convention, even without a named standard codifying it. No implementation is authorised by this backlog entry. |
| EBG-0063 | TMP-0002 Engineering Session Chat Summary Template | ESR-0007/ESR-0008; discovered during ESR-0021 WP6 historical audit | Candidate Backlog | Medium | Programme Sponsor | Recommended at ESR-0007 ("an AIEMS-controlled template... rather than recreating it each session") and again at ESR-0008 under a related discussion (ISTL-0001, see EBG-0064) - chat summaries have continued to be produced as ad hoc `.docx`/markdown exports (the HST-#### series) through the present session without a standard template. Complements EBG-0009 (Engineering Session Standard, which covers the Engineering Session Archive but not the chat-summary format specifically) - consider actioning together. No implementation is authorised by this backlog entry. |
| EBG-0064 | ISTL-0001 Engineering Inter-Session Task Register | ESR-0008; discovered during ESR-0021 WP6 historical audit | Candidate Backlog | Medium | Programme Sponsor | Recommended twice at ESR-0008 closure as a standing AIEMS artefact for durable inter-session task tracking, replacing one-off task lists, to be "maintained in the repository and linked through the OSE." The actual deliverable produced was only a `.docx` file, never a controlled/backlog artefact. Overlaps conceptually with EBG-0033 (AIEMS Improvement Register) but is distinct - ISTL-0001 is about carrying forward outstanding tasks between sessions, not recording process improvements. Consider whether EBR-0001's own "Active and Next Planned Work" mechanism (per PST-0001 Section 8) now substantially satisfies this need before actioning. No implementation is authorised by this backlog entry. |
| EBG-0065 | STD-0006 Configuration and Secrets Standard | ESR-0001 initial bootstrapping session; discovered during ESR-0021 WP6 historical audit | Candidate Backlog | High | Programme Sponsor | Explicitly named and scoped during the initial project bootstrapping conversation ("prevent configuration drift, and protect sensitive information"), sequenced alongside STD-0003-STD-0008 in a proposed standards roadmap. Only STD-0001 through STD-0004 exist today; no STD-0006 was ever created, and the topic recurs across later session chat histories (ESR-0004, ESR-0005, ESR-0006, ESR-0013 per direct grep) without ever being backlogged. Higher priority than most other Tier 1 items: JARVIS now holds real provider API keys (OpenAI, Gemini) and Sentinel credential references exist in the live codebase - configuration/secrets governance is no longer a purely theoretical concern. No implementation is authorised by this backlog entry. |
| EBG-0066 | AIEMS Maturity Model / Index | Proposed independently at ESR-0002 ("AIEMS Engineering Maturity Index") and ESR-0003 ("Recommendation 4 - Introduce an AIEMS Maturity Model"); discovered during ESR-0021 WP6 historical audit | Candidate Backlog | Medium | Programme Sponsor | A scoring mechanism (governance maturity, repository integrity, backlog health, architecture readiness, automation coverage, product readiness) to measure AIEMS/programme maturity objectively rather than relying on subjective Programme Sponsor judgement about phase transitions. Proposed twice in two different early sessions, both times explicitly deferred as "a future backlog item... an optimisation rather than a prerequisite," never captured. Distinct from EBG-0012 (AIEMS roadmap, now closed by JRM-0001 - a sequencing artefact, not a maturity-scoring one) and EBG-0033 (AIEMS Improvement Register - a log, not a scoring model). Note PBK-0001's own "Repository Engineering Health Review Guidance" already includes a "JARVIS Development Readiness Assessment" mandatory question - this item would be the AIEMS-maturity analogue of that, and the two should be considered together if actioned. No implementation is authorised by this backlog entry. |
| EBG-0067 | Dropped ADR-0007 Topics: JARVIS UI Architecture Strategy and AIEMS Knowledge Architecture | ESR-0004 and ESR-0006; discovered during ESR-0021 WP6 historical audit | Approved Backlog | Medium | Programme Sponsor | Two distinct architecture decisions were each explicitly proposed and Programme Sponsor-approved under the placeholder name "ADR-0007," in two different sessions, before the number was eventually reused for an unrelated decision (User Experience Platform Selection - the real ADR-0007 in the repository today). At ESR-0004, "ADR-0007 - JARVIS User Interface Architecture Strategy" was proposed (Python GUI vs. web-frontend/Electron-Tauri architecture) and explicitly deferred ("not today"). At ESR-0006, "ADR-0007 - AIEMS Knowledge Architecture" was proposed and approved ("Agreed and Approved," framed as "Milestone M-0002"), defining AIEMS as also being an engineering knowledge architecture independent of any specific tool, including a controlled relationship vocabulary (`implements/supports/depends_on/verifies/records/supersedes/references/relates_to/derived_from/governed_by`). Neither topic was ever captured under a different number once ADR-0007 was reassigned. **Reviewed at ESR-0023 WP1** (Engineering Reviewer confirmed): split disposition applied. The JARVIS UI Architecture Strategy topic is judged Superseded, resolved by the real [[ADR-0007_USER_EXPERIENCE_PLATFORM_SELECTION|ADR-0007]] and [[ADR-0019_UXP_BACKEND_INTEGRATION_ARCHITECTURE|ADR-0019]], both implemented and live. The AIEMS Knowledge Architecture / relationship vocabulary topic remains a genuine live gap - confirmed directly against [[GDE-0001_PROJECT_KNOWLEDGE_MAP|GDE-0001]], which does not define the relationship vocabulary approved at ESR-0006. Promoted to Approved Backlog, scoped to this remaining sub-topic only. No implementation is authorised by this backlog entry. |
| EBG-0068 | Engineering Implementation Brief (EIB) Artefact Type - Adopt or Formally Drop | ESR-0006/ESR-0007; discovered during ESR-0021 WP6 historical audit | Superseded | Low | Programme Sponsor | Closed at ESR-0028 WP1 per [[EIP-ESR0028-001_AIEMS_PROCESS_HYGIENE_BATCH|EIP-ESR0028-001]] Section 4.2. A new controlled artefact type "between the approved Work Package and the implementation itself" was introduced at ESR-0006 (EIB-0001 onward), explicitly distinct in intent from the still-unbuilt EIP Standard (EBG-0008). EIB was used at ESR-0006 and ESR-0007, then never appears again in any later session. Confirmed directly at ESR-0028 WP1: no `EIB-*` file exists anywhere in the repository (`git ls-files` returns nothing) and no `EIB-*` row exists in REG-0001; the only remaining mentions are this backlog note and one historical chat reference (`FCH-0006`). The EIP convention has been used exclusively and consistently for 20+ sessions since. Closed as Superseded by the EIP convention; no artefact cleanup needed since there was never a registered EIB artefact to retire. |
| EBG-0069 | JARVIS_CAPABILITY_READINESS_MATRIX.md Refresh | Flagged as stale at ESR-0017; discovered during ESR-0021 WP6 historical audit | Complete | Medium | Programme Sponsor | During ESR-0017 WP9 follow-up, this document was directly flagged as stale ("still dated to ESR-0009, before Guardian/Sentinel/UXP were actually implemented") - PST-0001 Section 5 confirmed this remained true through ESR-0020. Unlike its sibling document PCB-0001 (Product Capability Baseline), which received the same staleness flag and was refreshed and closed via EBG-0056, this matrix's staleness was never given its own backlog item and remained unrefreshed for four sessions. **Refreshed to v2.0 at ESR-0021 WP7**, following the same pattern as EBG-0056: Guardian, Sentinel, Platform Services, User Experience Platform, Knowledge and Provider Architecture updated from Not Started/Planned to Implemented (Foundation), cross-checked against PST-0001 Section 5 and delivered session evidence. Memory, Voice, Vision, Home Automation, Productivity, Multi-device and a JARVIS-internal Engineering Agent confirmed still genuinely Not Started, not newly discovered gaps. A Document Control block and Refresh History section were added to the document itself, since neither existed before. **Non-blocking finding, not actioned by this item**: the document has never been registered in REG-0001 as a controlled artefact (no artefact ID, unlike PCB-0001) - left for a separate Programme Sponsor decision. |
| EBG-0070 | Wire a Live Provider into a Production ProviderOrchestrator Route | Recurring unnumbered item, PST-0001 ESR-0018 through ESR-0020; recommended for registration by [[JRM-0001_PROJECT_ROADMAP|JRM-0001]] Section 9; actioned ESR-0022 WP1 | Complete | High | Programme Sponsor | Implemented per [[EIP-ESR0022-001_PROVIDER_WIRING_AND_SYSTEM_HEALTH_PANEL|EIP-ESR0022-001]] v1.0 (Engineering Reviewer approved, Programme Sponsor approved) - a genuinely prospective package: an earlier retroactive revision (0.1) was correctly declined by the Engineering Reviewer and fully reverted before any implementation was authorised. `build_default_runtime()` (`jarvis/interfaces/stdio_rpc.py`) now optionally registers a real provider (`OpenAIProvider` or `GeminiProvider`) as the primary `text-generation` route provider, selected via `JARVIS_PRIMARY_PROVIDER` (default `openai`, per PEM-001's Primary designation), gated on that provider's credential environment variable being present and non-blank - `LocalEchoProvider` always remains registered as the final failover. `platform.status` extended with a new `providers` field. Incorporates the Engineering Reviewer's Medium finding directly: model env var resolution uses `environ.get(key) or default_model`, so a present-but-blank `OPENAI_MODEL`/`GEMINI_MODEL` falls through to the default instead of causing a startup failure. Six tests added, all using an explicit synthetic `environ` so the suite never depends on or exercises real host credentials. Full suite (209 tests) and `validate_repository.py` (full, not governance-only) both pass clean. No change to `PEM-001` itself. |
| EBG-0071 | REG-0001 HST/FCH Registration Gap | Number reserved at ESR-0022 WP1 ([[JRM-0001_PROJECT_ROADMAP|JRM-0001]] Section 9, "Unnumbered Items Requiring an EBG Identifier") when EBG-0070/EBG-0072 were registered; formally created and closed at ESR-0028 WP1 | Complete | Medium | Programme Sponsor | Diffed every git-tracked `aiems/History/**/*.md` file against REG-0001's registered rows directly at ESR-0028 WP1 and found 10 tracked artefacts with no REG-0001 row: HST-0015 (Claude and GPT), HST-0016 (Claude, GPT and the incremental HST-0016a_GPT), HST-0017 (Claude and the incremental HST-0017a_GPT), HST-0020 (Claude and GPT), and FCH-0020_GPT. All 10 registered in REG-0001 as `Archived` `Historical Session Record`/`Full Chat Historical Evidence` artefacts, matching the existing row format exactly. Engineering Reviewer (Codex) confirmed the inventory complete after an initial v0.1 draft under-counted by one (missed FCH-0020_GPT). |
| EBG-0072 | System Health Panel (Guardian/Sentinel/Providers, Real platform.status Fields) | [[JRM-0001_PROJECT_ROADMAP|JRM-0001]] Track C Near-term; actioned ESR-0022 WP1 | Complete | Medium | Programme Sponsor | Implemented per [[EIP-ESR0022-001_PROVIDER_WIRING_AND_SYSTEM_HEALTH_PANEL|EIP-ESR0022-001]] v1.0 (Engineering Reviewer approved, Programme Sponsor approved). Identified during ESR-0021 WP4 scoping as buildable without violating the no-mock-fallback rule, deliberately deferred to a future WP. A new dedicated panel (`src/App.jsx`, `src/styles.css`) showing exactly three rows - Guardian, Sentinel, Providers - derived only from the real `platform.status` RPC response (including the new `providers` field EBG-0070 added), distinct from the existing `DiagnosticsPanel`, which mixes these same real-derived rows with several permanently-static placeholder rows. Honest connecting/unavailable states confirmed via Playwright against the real Vite dev server (mocking `window.__TAURI_INTERNALS__.invoke`, no live backend), covering: not-yet-resolved, backend-unavailable, populated with a real provider plus local-echo fallback, and populated with local-echo only. No fabricated or illustrative figures at any state. |
| EBG-0073 | UXP Duplicate Monitoring Elements Tidy-up (SystemHealthPanel / DiagnosticsPanel Overlap) | Programme Sponsor visual check, ESR-0022 WP1, live Tauri app | Completed | Medium | Programme Sponsor | Once EBG-0072's System Health panel was seen live (real backend, not mocked), the Programme Sponsor observed it visually duplicates part of the existing `DiagnosticsPanel`: both now show Guardian/Sentinel/Providers rows derived from the same `platform.status` data, just styled differently. `DiagnosticsPanel` additionally mixes those real-derived rows with several permanently-static placeholder rows (boundary, shell, agents, first-light per `src/platformStatus.js`'s `diagnostics` export), compounding the clutter. **Resolved at ESR-0023 WP6** via option (a): the `guardian`/`sentinel`/`providers` entries removed from `platformStatus.js`'s static `diagnostics` export, `App.jsx`'s `deriveDiagnostics()` (which existed only to re-inject live data into those rows) removed entirely, `diagnosticIcons` trimmed to match. `DiagnosticsPanel` now shows only its four permanently-static placeholder rows (boundary, shell, agents, first-light); `SystemHealthPanel` is the sole owner of live Guardian/Sentinel/Providers status. Verified live via a Playwright-driven headless Chromium screenshot against the real Vite dev server (`DiagnosticsPanel` confirmed to render exactly 4 rows, `SystemHealthPanel` unaffected, zero console errors) and independently confirmed by the Programme Sponsor viewing the running dev server directly. Engineering Reviewer confirmed the diff against this scope, no blocking findings. Option (b)/(c) and the separate question of whether the remaining static-only rows should be removed or made real remain open, not actioned by this item. |
| EBG-0075 | Implement Ollama Local Fallback Provider | PEM-001 Decision Outcome (ESR-0015 WP3a) named Ollama as the local fallback path; never separately backlogged for implementation until now, discussed conversationally at ESR-0025 | Complete | Medium | Programme Sponsor | PEM-001's already-decided Resilience Principle 5 ("local fallback should exist for degraded operation even if quality is reduced") has never been implemented - `sentinel/` has only `openai_provider.py` and `gemini_provider.py`; no Ollama adapter exists. Confirmed directly at ESR-0025: Ollama is installed and running on the Programme Sponsor's machine (models migrated to `I:\Ollama Models`, verified via a real `/api/generate` call - cold start ~64s, warm ~6s, response field is `response` with a separate `thinking` field for reasoning-capable models that must not leak into visible content). Scope: a new `sentinel/ollama_provider.py` mirroring the `OpenAIProvider`/`GeminiProvider` adapter pattern (no credential required, local HTTP endpoint, configurable model/endpoint via environment variables), registered in `build_default_runtime()`'s route between the primary cloud provider and `LocalEchoProvider`, relying on `ProviderOrchestrator`'s existing failover-on-exception mechanism rather than a new health-check pattern. Programme Sponsor confirmed the strategic direction (Ollama as fallback, not complexity-based routing - the latter deferred to EBG-0045/EBG-0049 Cost-Aware Provider Routing, pending real usage data). **Resolved at ESR-0026 WP1** per [[EIP-ESR0025-002_OLLAMA_LOCAL_FALLBACK_PROVIDER|EIP-ESR0025-002]] v1.0 - Engineering Reviewer (Codex) gave a genuine, real (non-manual-relay) review via the now-working AIEMS Exchange Bridge: no blocking findings, one non-blocking observation (keep the no-credential-rejection and `thinking`-field-discard tests explicit) already satisfied by the EIP's own Section 7/9 requirements. Programme Sponsor approved via `sponsor-decision`. `sentinel/ollama_provider.py` implements `OllamaProvider` exactly as scoped: rejects any configuration carrying a credential, requires a default model, calls `POST {endpoint}/api/generate` with `stream: false`, reads only `response["response"]`, and never surfaces `thinking`. Wired into `build_default_runtime()`'s `text-generation` route between the primary cloud provider (if configured) and `LocalEchoProvider`, unconditionally (no credential gate applies), via `JARVIS_OLLAMA_MODEL` (default `qwen3.5:2b`) and `JARVIS_OLLAMA_ENDPOINT` env vars, `timeout_seconds=90.0` per the EIP's own recommendation. 11 new tests (`jarvis/tests/test_ollama_provider.py`), `jarvis/tests/test_stdio_rpc.py`'s `configured_providers()` expectations updated. **A genuine test-isolation defect was found and fixed during this work**: because Ollama needs no credential, the shared `_server()` test helper's `build_default_runtime(environ={})` began making a real, non-deterministic network call to the Programme Sponsor's actual running Ollama server during automated tests (71s runtime, real AI-generated content instead of the expected `local-echo` stub) - fixed by pointing `JARVIS_OLLAMA_ENDPOINT` at a reserved, never-listening port in that helper, restoring deterministic failover-to-local-echo behaviour without depending on whether Ollama happens to be running on the test machine. 249 tests total (was 238). **Live smoke check finding, disclosed rather than hidden**: the required live `execute()` call against the Programme Sponsor's real Ollama instance timed out at the EIP's own recommended 90-second timeout on the first attempt, and only succeeded at 180 seconds - confirming the EIP's Section 11 risk ("cold-start latency is real and noticeable") was correctly anticipated, but with wider real-world variance than the single prior ~64s measurement suggested. Implemented exactly as approved (90.0s) rather than unilaterally extending scope; per the EIP's own framing this is a quality-of-experience risk, not a safety one, since a cold-start timeout still fails over cleanly to `LocalEchoProvider`. A candidate for future tuning (a longer default, or pre-warming the model at startup) if this proves frequent in practice - not actioned here. **Post-implementation review of the real committed diff (Codex, via the bridge) found one genuine finding, fixed at [[EIP-ESR0025-002_OLLAMA_LOCAL_FALLBACK_PROVIDER|EIP-ESR0025-002]] v1.1**: `execute()` called `data.get("response")` without checking `data` was a dict - valid JSON that isn't an object (`null`, an array, a bare string/number) has no `.get` method, raising `AttributeError` instead of the intended `RuntimeError`. Fixed with an explicit `isinstance` check; 4 new parametrised tests. Codex separately confirmed the test-isolation fix and route wiring/timeout match the approved package, no further findings. 254 tests total (was 250). **Final confirmation (Codex, via the bridge): no blocking findings - the AttributeError fix closes the previous finding, everything else remains sound.** WP1 fully closed - the first Work Package in this project to complete the entire real cycle (draft, review, approval, implementation, commit, post-commit review, fix, final confirmation) via genuine Claude<->Codex exchange with no manual relay. |
| EBG-0076 | Network Exposure Security Hardening (Authentication, Rate Limiting, Encryption) | Programme Sponsor question at ESR-0025 ("how robust is Sentinel - can it be exposed to the internet, would it be safe?"); Programme Sponsor directed "sooner rather than later" | Complete | High | Programme Sponsor | Confirmed directly at ESR-0025: no network-facing interface exists anywhere in the current architecture today - the UXP-backend bridge (`jarvis/interfaces/stdio_rpc.py`) is a local stdio JSON-RPC pipe between the Tauri shell and a spawned child process, not a socket, so "exposing Sentinel to the internet" is not currently possible without first building an entirely new network interface. Separately and more importantly: `TrustTierPolicy`/GAM-0001 is a *behavioural* authority boundary (what Guardian may do once a request has already arrived), not a *network security* boundary - there is no authentication, no rate limiting, no TLS, and no session/identity concept anywhere in Sentinel or the RPC layer. If a network-facing interface were ever built without first addressing this, anyone reaching it could call `guardian.converse` and consume the Programme Sponsor's billed API credit freely, or read `platform.status`/`knowledge.graph` and see internal repository structure. Scope for the eventual work: authentication, rate limiting and TLS/encryption for any future network-facing Guardian interface - a prerequisite gate for any remote-access capability, not an incremental hardening pass on something already exposed. **Resolved at ESR-0026 WP3** per [[ADR-0020_SENTINEL_NETWORK_EXPOSURE_SECURITY_REQUIREMENTS|ADR-0020]] v1.0 (Approved) - a binding three-part security gate (authentication, rate limiting, TLS) any future network-facing Guardian/Sentinel interface proposal must satisfy before approval, grounded in ADR-0019 (which already flagged this exact scenario as its own revisit trigger), `sentinel/policy.py` (confirming TrustTierPolicy carries no authentication/rate-limiting/transport-security concept), and PEM-001 (confirming `guardian.converse`'s billed-provider routing as a direct cost-exposure risk, not only a confidentiality one). Engineering Reviewer (Codex) confirmed via the AIEMS Exchange Bridge: scope is coherent with ADR-0019/GAM-0001/PEM-001, correctly framed as a future proposal's approval gate, no scope overreach - no blocking findings. Programme Sponsor approved via `sponsor-decision`. This is a decision record, not an implementation - no network interface exists, none is built by this item; a future proposal must satisfy the gate before its own approval. **Final confirmation (Codex, post-commit review via the bridge): no blocking findings - committed content matches the reviewed draft, EBR-0001/REG-0001/REG-0002 entries are accurate.** WP3 fully closed. |
| EBG-0077 | UXP Static Placeholder Row Reconciliation Against Reference Mock-up | Programme Sponsor question at ESR-0025 (should mock-up UXP elements be removed given the planned final design?); tied explicitly to preparing the UXP for the planned Guardian self-awareness/introspection capability (discussed same session) | Complete | Medium | Programme Sponsor | Partially overlaps EBG-0073's own explicitly-left-open question ("whether the remaining static-only rows should be removed or made real remains open, not actioned by this item") - `DiagnosticsPanel`'s four permanently-static rows (boundary, shell, agents, first-light) had never been reconciled against `UAM-0001`'s reference mock-up or the current roadmap. **Implemented at ESR-0027 WP2** per [[EIP-ESR0027-002_UXP_DIAGNOSTICS_PANEL_RECONCILIATION|EIP-ESR0027-002]] v0.2 (Engineering Reviewer/Codex reviewed via the bridge - concept approved with one Low/editorial finding, no blocking findings; Programme Sponsor approved via `sponsor-decision`): row-by-row reconciliation judged `boundary`/`shell`/`agents` as honest, UAM-0001-grounded diagnostics worth keeping unchanged (`boundary`/`shell` are true structural statements using UAM-0001 Section 14's "diagnostic" colour semantic; `agents` honestly maps to the real, roadmapped-but-genuinely-not-started Agent Framework/GIA capability, confirmed by `JARVIS_CAPABILITY_READINESS_MATRIX.md`), and `first-light` as out of place - true but reporting on an unrelated legacy product (the Tkinter First Light app) rather than this shell's own implementation boundary. `first-light` removed from `src/platformStatus.js`'s `diagnostics` export and `src/App.jsx`'s `diagnosticIcons` map; the now-unused `Zap` icon import removed; a stale comment enumerating the four rows corrected to three. `CapabilitySidebar`'s separate `capabilityStatuses` list explicitly excluded (a distinct component, not this reconciliation's target - Engineering Reviewer confirmed this scope boundary is coherent). Verified live via an ad hoc Playwright check against the real Vite dev server (mocking `window.__TAURI_INTERNALS__.invoke`, no live backend, per the EBG-0072/EBG-0073 precedent - no committed frontend test infrastructure exists yet, a separately-tracked gap): `DiagnosticsPanel` renders exactly three rows, zero console errors, no `first-light` row present. `npm run build` clean. **Post-implementation review (Codex, via the bridge, against committed diff f29cf66): Pass, no blocking findings** - independently verified every claim (row removal, orphaned `Zap` import removed, stale comment corrected, `boundary`/`shell`/`agents` genuinely unchanged, no backend files touched). WP2 fully closed, no fix cycle needed. |
| EBG-0078 | JARVIS/Guardian Desktop Launcher (`scripts/start-jarvis.bat`) | Programme Sponsor request at ESR-0025 ("build an executable/bat file to start JARVIS/Guardian without command line ... so it can be running on my desktop") | Completed | Low | Programme Sponsor | `scripts/start-jarvis.bat` was added at ESR-0025 - a double-click launcher running the same `npm run tauri dev` path this session verified live (Vite dev server, Rust `cargo` build, spawned Python backend), with a header comment pointing to setting `OPENAI_API_KEY`/`GEMINI_API_KEY` as a persistent Windows user environment variable rather than pasting a credential into the file. Verified end-to-end live: window opened, chat round-trip returned `local-echo: <message>` (no provider key set in that test). A copy was also placed at `C:\Users\rober\OneDrive\Desktop\Start JARVIS.bat` for direct desktop use outside the repository. Deliberately dev-mode only, mirroring EBG-0054's precedent of recording already-delivered tooling rather than authorising new implementation - it depends on a full local dev toolchain (`npm`, `cargo`, Python) being present, not a standalone packaged executable. The Programme Sponsor separately confirmed a real installer-style packaged `.exe` is out of scope until closer to a live/update-only phase of the project; that remaining gap is already tracked under EBG-0050's "production sidecar bundling/packaging" scope, not duplicated here. No implementation is authorised by this backlog entry - the launcher itself is already delivered and working. |
| EBG-0079 | `validate_repository.py` `.aiems-exchange/` Scan Exclusion Fix | Discovered live at ESR-0026 WP1 - Engineering Reviewer (Codex) appeared to hang for 12+ minutes on a post-implementation review request | Completed | Medium | Programme Sponsor | Programme Sponsor-authorised deviation from the standard Working Report Lifecycle (no EIP drafted, fixed directly), same authorised-deviation pattern as ESR-0025A. Root cause: `validate_repository.py`'s `iter_markdown_files()` scans every `*.md` file under the repository root with an `IGNORED_DIRS` exclusion set that never included `.aiems-exchange/` - the AIEMS Exchange Bridge's own gitignored working directory. Since `scripts/aiems_bridge.py`'s `capture_evidence()` embeds `validate_repository.py`'s own output as evidence inside transcript/inbox/outbox `.md` files, and those files were themselves being scanned and re-embedded on every subsequent evidence capture, the warning count grew without bound across ESR-0026 WP1's three real evidence captures: 104 (clean baseline) to 425 to 1279. The Engineering Reviewer's apparent 12-minute "stuck thinking" state was very likely caused by being handed a 177KB review request message, mostly this compounding noise, not a genuine defect in the code under review or a lock/deadlock in the bridge itself (confirmed directly: `.aiems-exchange/.locks/` was empty at the time). Fixed by adding `.aiems-exchange` to `IGNORED_DIRS` - confirmed to restore the clean 104-warning baseline even with the (still-present, unrelated) bloated exchange content on disk. One regression test added (`test_iter_markdown_files_excludes_aiems_exchange`). 250 tests total (was 249). |
| EBG-0080 | Personal Memory Implementation (`jarvis/memory/`) | MDS-0001 Section 11 (Future Evolution, anticipated first follow-on); selected by the Programme Sponsor at ESR-0027 WP0B | Complete | High | Programme Sponsor | Implements MDS-0001's Personal Memory tier (Section 6.2) gated by GAM-0001's memory-retention consent boundary (Section 9.2) - the first real instance of a Sentinel `REVIEW` decision anywhere in this codebase actually being resolved by a human, not just classified. A WP0-style scope check during drafting found GAM-0001's own evidence check accurate - no approval workflow existed in code for any `REVIEW`-classified action - so the Programme Sponsor selected the expanded scope (build the minimal mechanism, not just storage) over deferring or descoping. **Implemented at ESR-0027 WP1** per [[EIP-ESR0027-001_PERSONAL_MEMORY_IMPLEMENTATION|EIP-ESR0027-001]] v0.3 (Engineering Reviewer/Codex reviewed via the bridge across two rounds - two Medium and one Low finding on v0.1 addressed at v0.2, confirmed resolved, one further Low/editorial finding corrected at v0.3 - Programme Sponsor approved via `sponsor-decision`): `jarvis/memory/store.py` (`PersonalMemoryStore`, SQLite-backed, two tables - `personal_memory` and a durable `consent_decisions` table added specifically to close the Engineering Reviewer's consent-traceability finding, so every stored item's `consent_decision_id` references a real persisted decision, not a transient in-memory value); `jarvis/memory/service.py` (`PersonalMemoryService.propose()/approve()/deny()`, reusing Sentinel's existing `requires_approval=True` classification path unmodified - no `sentinel/policy.py` or `sentinel/core.py` change was needed - with `propose()` explicitly asserting the Sentinel outcome is `REVIEW` and refusing on `DENY` or unexpected `ALLOW`, per the Engineering Reviewer's third finding); wired into `GuardianRuntime`'s previously-`UNAVAILABLE` `"Guardian Memory Boundary"` service (now goes `ONLINE` when a memory service is connected, mirroring the existing Provider Boundary pattern) and four new `jarvis/interfaces/stdio_rpc.py` JSON-RPC methods (`memory.propose`/`approve`/`deny`/`list`). `JARVIS_MEMORY_DB_PATH` env var overrides the default `~/.jarvis/memory/personal.db` location for test isolation - applied retroactively to every existing `build_default_runtime()` test call in `jarvis/tests/test_stdio_rpc.py`, which had all been touching the real store path before this package, the same test-isolation defect class ESR-0026 WP1 found for Ollama's network endpoint. **A genuine defect was found and fixed during this package's own live smoke check**: `PersonalMemoryStore`'s SQLite connections were never explicitly closed (`sqlite3.Connection`'s context manager only commits/rolls back, never closes), leaking a file handle on every operation - harmless on most platforms but caused a real `PermissionError` on Windows when the smoke check's temp directory tried to clean up. Fixed with an explicit `_transaction()` context manager that always closes the connection; re-run smoke check (propose->approve->list, propose->deny->list confirming a denied item never appears, and a fresh process confirming durable persistence across restart) passed cleanly. Excludes Session/Shared-Family memory tiers, GAM-0001 Section 9.3's future trusted-mobile approval channel, household-role authentication (out of GAM-0001's own scope - the single running-application user is the sole approver), encryption/sync, and any UXP/React changes (backend-only, per PBK-0001's Feature-First Delivery Discipline allowance for backend capability delivery) - all disclosed in EIP-ESR0027-001 Section 8, not silently dropped. **Post-commit review** (Engineering Reviewer, Codex, via the bridge, against committed diff 505023c) found two Medium blocking findings, both fixed: (1) `GuardianRuntime`'s memory methods checked only service connectivity, not runtime state, letting a connected-but-not-started runtime propose/approve/list memory - fixed by also requiring `RUNNING` state, mirroring `converse()`'s two-check pattern, with two new regression tests; (2) `PersonalMemoryStore.add()` accepted any `consent_decision_id` without verifying a matching approved decision existed, so the storage layer itself could create orphan records - fixed by checking, inside the same transaction as the insert, that the referenced decision exists and is `"approved"`. 286 tests total (was 254), `validate_repository.py` 0 errors (120 warnings, up from 111 pre-existing - the increase is this package's own disclosed, accepted cross-document Section-reference false positives, per the corrected Section 10 criterion). **Final confirmation (Codex, via the bridge, against fix commit dbd8424): Pass - both Medium findings resolved, no new blocking findings.** Advisory only, not actioned: `PersonalMemoryStore.add()`'s traceability guard permits multiple records to reference the same approved decision if a future caller bypasses `PersonalMemoryService` and calls the store directly - the service path always creates exactly one record per approval today, so this is not a current gap, flagged for a future caller to consider if `PersonalMemoryStore` is ever used outside the service. WP1 fully closed. |
| EBG-0074 | Wire TrustTierPolicy as SentinelCore's Production Default | Discovered at ESR-0023 WP5 (EBG-0041 validation); Engineering Reviewer (Codex) confirmed and recommended High/Near-term | Complete | High | Programme Sponsor | **Resolved at ESR-0024 WP1** per [[EIP-ESR0024-001_TRUSTTIERPOLICY_PRODUCTION_WIRING|EIP-ESR0024-001]] v1.0 (Engineering Reviewer approved v0.2 with two Medium findings addressed, Programme Sponsor approved). `jarvis/interfaces/stdio_rpc.py`'s `build_default_runtime()` now constructs `SentinelTrustGateway(policy_engine=TrustTierPolicy())`, replacing the class default `SimpleApprovalPolicy()` for the one runtime object the live UXP actually runs - `SentinelTrustGateway`'s own class-level default is deliberately left unchanged, confining the change's blast radius away from the many existing tests/scripts that construct a bare gateway elsewhere. A new `policy_engine` property on `SentinelTrustGateway`, `gateway` property on `SentinelGatedConversationProvider` and `sentinel_gateway()` accessor on `GuardianRuntime` (mirroring the existing `configured_providers()` pattern) let a new test assert `isinstance(runtime.sentinel_gateway().policy_engine, TrustTierPolicy)` directly against the production construction path - genuine wiring confirmation, not inference from behaviour (the real conversation request shape classifies `ROUTINE_INTERACTION` -> `ALLOW` under either policy, confirmed unchanged). A second new test evaluates that exact conversation request shape through the wired gateway and confirms `ALLOW`/`ROUTINE`/`ROUTINE_INTERACTION`, without duplicating `TrustTierPolicy`'s own already-covered category-classification tests. `TrustTierPolicy` (Sections 5-9's classification model, implemented ESR-0016 WP1) is therefore now genuinely in the production runtime's decision path; no new production call site yet varies request shape per Guardian action (Action faculty remains Not Started, gated behind EBG-0021), so this closes the "operationally inert" gap without yet producing an observable behaviour change in the live conversation path - a deliberate, disclosed scope boundary, not an oversight. 211 tests pass (was 209), `validate_repository.py` 0 errors. |
| EBG-0081 | UXP Animation Performance Policy | Discovered during ESR-0028 WP4's five-round power-usage investigation (`GuardianOrbGraph.jsx` rotation) | Candidate Backlog | Medium | Programme Sponsor | The Programme Sponsor raised a forward-looking concern directly during WP4's fix cycle: if a single, deliberately-optimised ambient rotation still measurably costs sustained GPU/CPU (confirmed via isolated Windows Task Manager comparison - real, attributable cost, not a measurement artefact), what happens as more animated UXP elements are added over the rest of the project, each potentially running its own independent update loop? Two distinct, related questions: **(1) Shared animation scheduling** - establish a single, shared animation clock/driver for the whole UXP that any future continuously-animated element registers with, rather than each spinning up its own independent `requestAnimationFrame` loop. **Remains open, Candidate Backlog** - no implementation authorised. **(2) Rendering engine re-evaluation for `GuardianOrbGraph.jsx` specifically - split disposition applied at ESR-0028 WP5.** The decision half is **Complete**: [[ADR-0021_GUARDIAN_ORB_RENDERING_ENGINE|ADR-0021]] (Approved, Programme Sponsor, 18 July 2026) recommends migrating `GuardianOrbGraph.jsx` from SVG DOM rendering to Canvas 2D, grounded directly in WP4's seven-round optimisation evidence, and explicitly rejects WebGL/Three.js/PixiJS as disproportionate to the current/anticipated ~195-node scale. The implementation half remains **not authorised** - ADR-0021 is a decision-only artefact per the Programme Sponsor's explicit instruction to "review... but not implement"; a future EIP must separately scope the actual Canvas migration, including replacing SVG's free hit-testing/tooltips/DOM-accessibility with manual equivalents and live before/after performance verification against the real graph. **Cross-reference EBG-0082 (ESR-0029 WP1, this register)**: external research confirms this item's Question (1) - a single shared animation clock/driver - is not a novel proposal but standard, well-documented practice ("centralized scheduler" / animation-manager pattern), giving Question (1) confirmed prior art rather than only first-principles reasoning. Question (1) remains open, Candidate Backlog - no implementation authorised. **Question (2)'s implementation delivered at ESR-0029 WP2** per [[EIP-ESR0029-001_GUARDIAN_ORB_CANVAS_MIGRATION|EIP-ESR0029-001]]: `GuardianOrbGraph.jsx` migrated from per-element SVG DOM rendering to a single Canvas 2D surface, plus a static glow/gradient node treatment toward `UAM-0001_GUARDIAN_ORB_MOCKUP.jpg`'s look and layout (explicitly not its data-driven activity semantics, per EIP-ESR0029-001 Section 8). Live-verified via Chrome DevTools Protocol TaskDuration against the real 200-node/1,745-edge graph, isolating two steps as the EIP required: plain Canvas measured ~4.46% of one core versus the SVG baseline's ~9.05% (both measured the same way, same real graph, same wall-clock window) - a genuine improvement, not just a differently-shaped cost; adding the glow treatment brought this to ~6.26%, still below the SVG baseline. **A genuine defect was found and fixed during this package's own verification, disclosed rather than hidden**: an initial implementation individually stroked each of the 1,745 edges every redrawn frame, which measured *more* expensive than the SVG baseline (~27-29% of one core) - the opposite of this migration's purpose. Root-caused via a Chrome DevTools Protocol ablation study to the combination of Canvas's circular clip with per-edge stroke calls, not the edge count or the clip alone in isolation. Fixed in two steps: (1) batching edges into 8 depth-banded `Path2D` objects built during the already-throttled recompute step, reducing per-frame edge draw calls from ~1,745 to a handful of batched strokes; (2) recognising edges are mathematically guaranteed to stay within the circular boundary (the force-containment radius bounds every node, rotation preserves each node's distance from centre, and a disk is convex), so the clip is now scoped to only the ~200 node/glow draws that can actually overflow it, not the edges - this second fix was the larger factor, confirmed via the same ablation methodology. Functional verification (Playwright, mocking the real backend bridge): rotation confirmed in motion, `prefers-reduced-motion` confirmed static, hover tooltip confirmed against real node labels, correct DPR/responsive canvas sizing confirmed at both the desktop (312px) and mobile (`min(280px, 78vw)`) breakpoints via `ResizeObserver`, zero console errors. `npm run build` clean, `python -m pytest` 286/286 passing (no Python touched). **Real hardware confirmation (Programme Sponsor)**: ran the live Tauri app and observed Windows Task Manager directly - JARVIS Guardian Shell at 1.4% CPU, 1.0% GPU, Power Usage "Low" (trend "Very low"), a categorical change from the sustained "Very high" classification the SVG-era implementation never cleared across all seven ESR-0028 WP4 fix rounds. This confirms the actual symptom this migration exists to resolve, complementing (not duplicating) the CDP TaskDuration comparison above, which measured a different thing (headless JS main-thread script time) and could not by itself confirm the Windows-reported GPU/power-state improvement. **Post-implementation Engineering Reviewer (Codex) findings on the v1.1 diff, both addressed at EIP-ESR0029-001 v1.2**: Medium - hit-testing built its lookup array from `baseNodes`' arbitrary iteration order rather than the actual depth-sorted paint order, so for overlapping nodes the tooltip could report a node other than the frontmost one visually under the pointer; fixed by deriving hit entries from the same `sortOrderRef.current` order used for painting. Medium - REG-0001's EBR-0001/EIP-ESR0029-001 rows were briefly stale relative to a v1.1 update still in flight when Codex's review ran (a submission-timing artefact, not a final-state error); reconfirmed consistent before resubmission. |
| EBG-0082 | Cross-Module Resource Interaction Research (Modular Architecture CPU/Memory Baseline) | Programme Sponsor concern raised at ESR-0029 WP0B, after reflecting overnight on ESR-0028 WP4's animation power-usage investigation | Complete (research delivered; no implementation authorised) | Medium | Programme Sponsor | The Programme Sponsor raised a broader, forward-looking version of EBG-0081's concern: JARVIS's modular architecture (a single Python backend process hosting Guardian, Sentinel, Personal Memory and Provider routing in-process, plus a React UXP frontend that will host an increasing number of independently-built interactive/animated components) has no established baseline for how these modules will interact under shared CPU/memory pressure as more are added - is JARVIS missing a known architectural solution other comparable projects already use? Explicitly scoped as research to inform judgement, not to copy a solution wholesale. Real external research conducted (not confined to JARVIS's own codebase), mapped against JARVIS's actual current architecture rather than the aspirational Core-Engine-plus-independent-services diagram in [[JARVIS_PRODUCT_ARCHITECTURE|JARVIS Product Architecture]] Section 6: **(1) Frontend/UXP main-thread scheduling** - browsers' own cooperative-scheduling primitive (`requestIdleCallback`, W3C) and the widely-documented "centralized animation manager" pattern (one shared `requestAnimationFrame` loop that individual components register with, rather than each running its own) are standard, mature practice for exactly the risk EBG-0081 Question (1) named. This is confirmed prior art, not a novel proposal - directly informs EBG-0081's still-open Question (1), cross-referenced there. **(2) Process-isolation reference architectures** (VS Code's Extension Host, Electron's per-window renderer processes, Tauri's own Rust-backend/webview split) isolate *crash and memory blast-radius*, not *CPU fairness*, between modules - confirmed directly: VS Code received exactly such a request - an upstream feature request (microsoft/vscode#233842) asking for Extension Host CPU usage limiting - and closed it as not planned rather than building it, meaning even that far-larger, well-funded reference architecture has declined to solve fine-grained per-module CPU governance via process isolation alone, not merely left it pending. Important negative finding: adopting a similar multi-process split for JARVIS's backend would not, by itself, buy the CPU-fairness guarantee the Programme Sponsor is concerned about - it solves a different problem (stability containment) at real cost (IPC overhead, packaging complexity). **(3) Comparable modular AI-assistant / smart-home platforms** (Mycroft's skills-as-plugins model, Home Assistant's integrations) - no published per-module CPU/memory governance pattern was found for either. Home Assistant's own community has an open discussion (home-assistant/home-assistant#2239) explicitly requesting per-integration memory visibility in its own Task Manager, i.e. even that mature, widely-deployed modular platform has not solved this either. **(4) Resource governor pattern** - a recognised general throttling pattern (route all access to a scarce shared resource through one governing component, as used for e.g. database connection pooling) is conceptually transferable to a future backend "background task budget" component that Guardian/Sentinel/Memory/Provider background work would register through, but no ready-made reference implementation exists at JARVIS's scale; any such component would need JARVIS-specific design, not adoption of an existing library/pattern. **Synthesis**: JARVIS is not missing some already-solved, off-the-shelf answer that comparable projects already use and JARVIS alone lacks - cross-module resource governance in modular assistant/plugin architectures is a genuinely under-addressed area industry-wide, confirmed by the same open gap existing in far more mature reference projects (VS Code, Home Assistant). **Recommendation (research only, no implementation authorised by this item)**: (a) the frontend shared-animation-scheduler pattern is confirmed, low-risk, standard practice - the concrete near-term candidate, already tracked as EBG-0081 Question (1), a future EIP still required; (b) the backend remains appropriately single-process at current module count/scale - no urgent architectural change is evidenced or recommended; (c) explicitly do **not** recommend premature multi-process backend decomposition modelled on VS Code's Extension Host - the evidence shows it would not address the Programme Sponsor's actual CPU-fairness concern, only a different one (crash isolation), and would add real complexity; (d) if backend background/scheduled work (as opposed to request-response calls) grows materially beyond its current scope, the resource-governor pattern in (4) is the reference shape to design toward - flagged for a future backlog item if and when that materialises, not actioned now. No implementation is authorised by this backlog entry. |
| EBG-0083 | Guardian Instrumentation Agent (GIA) - Phase 1 Local Resource Observability | Named and approved at ESR-0010 (architecture defined, implementation deferred); refined at ESR-0011 into a formal readiness design and phased roadmap; selected as ESR-0029 WP3/WP4/WP6/WP7 following the Programme Sponsor's direct request after ESR-0029 WP1/WP2's resource-interaction and performance work | Complete (Phase 1 in full) | High | Programme Sponsor | GIA has been discussed since ESR-0010/ESR-0011 but has never had its own backlog item - it exists only as a referenced blocker inside EBG-0055 ("Phase 3, blocked on GIA telemetry") and EBG-0059's text, and `JARVIS_CAPABILITY_READINESS_MATRIX.md` still lists the Engineering Agent row as "Proof of Concept only (GIA-BOOT)". This item registers it properly and scopes its first real implementation slice. **Authoritative definition (ESR-0011 Section 10, the session's own closure report, not chat exploration)**: "GIA should be treated as the platform observability layer, beginning with local engineering and workstation state. Initial GIA implementation should prioritise: CPU state, Memory state, Storage state, Process and service health, Local engineering environment state, Event/state publication for future Guardian Orb and platform consumption. GIA shall observe and publish state. It shall not become a policy engine, decision-maker or owner of platform state." ESR-0011's own working discussion (`FCH-0011`) further staged this into four phases: Phase 1 - local resource/engineering-environment monitoring (no internet dependency); Phase 2 - platform service instrumentation (Guardian/Sentinel/Agent Framework/Memory/Provider state); Phase 3 - engineering instrumentation (repository health, session/baseline state, git state); Phase 4 - external instrumentation (internet, external providers), gated behind Sentinel as the mandatory trust gateway at that point. **Explicitly distinct from `jarvis/gia/bootstrap.py`'s existing `GiaBootstrap`/`EngineeringRequest`/`EngineeringReadinessContext` (ESR-0012 WP3, "GIA-BOOT")**: that Proof of Concept evaluates whether an *engineering request* (a WP/task) has minimum readiness (identifier, objective, scope) before proceeding - a narrow exploration of one facet of "GIA" that turned out to be about engineering-workflow readiness gating, not platform observability. It is accepted as a Proof of Concept only per ESR-0012's own closure ("Further GIA implementation remains deferred until separately approved") and is not extended, modified or superseded by this item - the real observability capability this item scopes is new, separate work. **Scope of this item, originally (WP3)**: only Phase 1's first slice - CPU state, Memory state and event/state publication (a new read-only RPC method), explicitly deferring Storage state, Process/service health and Local engineering environment state to a later phase, matching this project's established small/evidence-led/staged delivery discipline (the ESR-0011 discussion itself: "The next package should be small, evidence-led and staged. It should not jump directly to large product implementation."). No policy, decision-making or platform-state ownership is authorised - GIA observes and publishes only, per its own defining constraint. **Storage state was subsequently delivered at WP4, process health (self-observation) at WP6, and local engineering-environment state at WP7 (see their own delivery notes below) - Phase 1 is now complete in full; only Phases 2-4 remain not delivered**, not a future EIP's full remaining scope as originally written here. **Implemented at ESR-0029 WP3** per [[EIP-ESR0029-002_GIA_PHASE1A_LOCAL_RESOURCE_OBSERVABILITY|EIP-ESR0029-002]] v1.0: `jarvis/gia/observability.py` (new) - `GiaSnapshot` (frozen dataclass: `cpu_percent`, `memory_percent`, `memory_used_mb`, `memory_total_mb`, `captured_at`) and `LocalResourceObserver`, reading real host state via a new `psutil` dependency (disclosed, no comparable stdlib-only cross-platform option exists) through an injectable `ResourceReader` protocol so unit tests never depend on the actual host machine's live values. A new `gia.status` JSON-RPC method added to `jarvis/interfaces/stdio_rpc.py`'s dispatch table, calling `LocalResourceObserver` directly - not routed through `GuardianRuntime`, `SentinelTrustGateway` or any memory/conversation boundary, confirmed by a test constructing a bare, unstarted `StdioRpcServer(GuardianRuntime())` and still resolving `gia.status` successfully. **Post-implementation Engineering Reviewer (Codex) finding, addressed at EIP-ESR0029-002 v1.1**: the first RPC tests called the real psutil-backed observer instead of a deterministic fake, leaving the serialization/shape path unproven per the EIP's own Section 4.6/5.5 requirement; fixed via a new injectable `gia_observer` parameter on `StdioRpcServer` (backwards-compatible, defaults to the real observer) and rewritten tests asserting exact camelCase output from a fixed `GiaSnapshot`, with a separate, clearly-labelled test confirming the default wiring still genuinely uses the real host. **A disclosed, real limitation, not fixed by this package**: this is method-level code decoupling only - `run()`'s existing startup sequence still constructs and starts `GuardianRuntime` before `StdioRpcServer` exists at all, so `gia.status` remains unreachable if that construction fails; true process-level resilience (GIA answering even when Guardian fails to start) would need a separate `run()` startup redesign, not authorised here (an Engineering Reviewer finding on the design draft, corrected in EIP-ESR0029-002 before implementation began - see its own version history). `psutil.cpu_percent` is sampled with a real, disclosed 0.2s blocking interval (a bare call returns a meaningless `0.0` on first invocation with no prior baseline). Failure propagates as a real exception through the existing JSON-RPC error path - never a fabricated snapshot, per the no-mock-fallback rule. 7 new tests (4 for the observer using a fake injected reader covering success/failure/interval-correctness, 3 for the RPC wiring - exact serialization from a fake snapshot, Guardian-independence, and default-wiring sanity), 293 tests total (was 286). Live-verified via the real production code path (`build_default_runtime()` + `StdioRpcServer.serve_forever()`, no mocking of `GuardianRuntime`, `Sentinel` or the observability reader) using in-memory streams rather than an OS pipe - a raw `python -m jarvis.app --ipc-stdio` invocation through a Git Bash pipe on this Windows machine produced no output for `gia.status`, but the same was independently confirmed true of the pre-existing `platform.status` method too, isolating this to a general Windows/Git-Bash stdin-piping quirk affecting every RPC method equally, not a defect introduced here. The in-memory-stream call returned genuine, plausible real host figures (CPU 6.7%, memory 47.8%/15.6GB used of 32.7GB total, at time of verification). No frontend/UXP change - backend-only capability delivery, per PBK-0001's Feature-First Delivery Discipline allowance, matching EBG-0080's precedent. `jarvis/gia/bootstrap.py` (GIA-BOOT) untouched. **Phase 1b (Storage state) implemented at ESR-0029 WP4** per [[EIP-ESR0029-003_GIA_PHASE1B_STORAGE_STATE|EIP-ESR0029-003]] v1.0: additive extension only, no new architecture - `GiaSnapshot` gained `disk_percent`/`disk_used_gb`/`disk_total_gb`; `ResourceReader`/`PsutilResourceReader` gained a `disk_usage(path)` method; `LocalResourceObserver.snapshot()` reads it via `STORAGE_PATH = os.path.abspath(os.sep)` (cross-platform-safe - the current drive root on Windows, `/` on POSIX, never a hardcoded drive letter); `gia.status`'s response gained `diskPercent`/`diskUsedGb`/`diskTotalGb`. No new dependency (`psutil` already present). WP3's own Engineering Reviewer finding was carried forward without regression: all RPC tests continue to assert exact camelCase serialization from a deterministic fake `GiaSnapshot`, never real host state. 2 new/extended observer tests (disk figures in the success-path assertion, a dedicated test confirming `disk_usage` is called with `STORAGE_PATH`), fake reader/snapshot constructions across both test files extended with disk fields, 294 tests total (was 293). Live-verified via the same real production code path as WP3 (`build_default_runtime()` + `StdioRpcServer.serve_forever()` via in-memory streams) - genuine real figures returned (28.7% used, ~401GB/1397GB at verification time). Local engineering-environment state (Phase 1's remainder) and Phases 2-4 remain not delivered at this point (see their own later delivery notes). **Phase 1c (Process health, self-observation only) implemented at ESR-0029 WP6** per [[EIP-ESR0029-004_GIA_PHASE1C_PROCESS_HEALTH|EIP-ESR0029-004]] v1.0: additive extension only, no new architecture - `GiaSnapshot` gained `process_status`/`process_uptime_seconds`/`process_cpu_percent`/`process_memory_mb`; `ResourceReader`/`PsutilResourceReader` gained a `process_health(interval)` method backed by `psutil.Process(os.getpid())`; `LocalResourceObserver.snapshot()` reads it and derives uptime from `create_time`; `gia.status`'s response gained `processStatus`/`processUptimeSeconds`/`processCpuPercent`/`processMemoryMb`, named to avoid ambiguity with the existing whole-machine `cpuPercent`/`memoryPercent`/`memoryUsedMb` fields. Deliberately scoped to the JARVIS backend's own process only - no external process/service lookup capability, avoiding the ambiguous "which services count" design question. No new dependency. WP3's own Engineering Reviewer finding was carried forward without regression: RPC tests continue to assert exact camelCase serialization from a deterministic fake `GiaSnapshot`. Fake reader/snapshot constructions across both test files extended with process-health fields, 294 tests total (unchanged count - existing tests extended, not new ones added). Live-verified via the same real production code path as WP3/WP4 (`build_default_runtime()` + `StdioRpcServer.serve_forever()` via in-memory streams) - genuine real figures returned (status "running", uptime 0.6s, correctly near-zero for a freshly-started process). Local engineering-environment state (VS Code/Obsidian/GitHub Desktop or similar detection) remained the last undelivered Phase 1 item at that point, deferred to its own future package given it needs a separate design conversation about which tools matter. **Phase 1d (Local engineering-environment state) implemented at ESR-0029 WP7** per [[EIP-ESR0029-005_GIA_PHASE1D_ENGINEERING_ENVIRONMENT_STATE|EIP-ESR0029-005]] v1.0, closing EBG-0083 Phase 1 entirely: additive extension only, no new architecture - `GiaSnapshot` gained `engineering_tools_running` (a `dict[str, bool]`); `ResourceReader`/`PsutilResourceReader` gained `running_process_names()` backed by a single `psutil.process_iter(["name"])` pass; `LocalResourceObserver.snapshot()` matches a small, fixed, disclosed constant (`ENGINEERING_TOOLS`: `vscode`, `obsidian`, `githubDesktop`, `chatgpt`) against the running-names set; `gia.status`'s response gained `engineeringToolsRunning`. Real process names were confirmed directly via `tasklist` against the Programme Sponsor's actual running machine, not assumed - found ChatGPT Desktop's real process name is `ChatGPT Classic.exe`, differing from the obvious guess `ChatGPT.exe`; both are configured as candidates. Presence-only (boolean) - no PID, memory, CPU, window title or command-line metadata for these external tools, and no arbitrary process lookup capability. No new dependency. WP3's own Engineering Reviewer finding was carried forward without regression: RPC tests continue to assert exact camelCase serialization from a deterministic fake `GiaSnapshot`. 1 new test (confirming ChatGPT Desktop matches under either candidate name) plus existing tests extended with tool-presence fields, 295 tests total (was 294). Live-verified via the same real production code path as WP3/WP4/WP6 (`build_default_runtime()` + `StdioRpcServer.serve_forever()` via in-memory streams) - genuine figures returned matching the independently-confirmed `tasklist` evidence exactly: `vscode: true`, `chatgpt: true` (both running), `obsidian: false`, `githubDesktop: false` (both closed) - real evidence of both states, not just fake-reader coverage. EBG-0083 Phase 1 is now Complete in full; only Phases 2-4 (platform/engineering/external instrumentation) remain not delivered. |
| EBG-0084 | Sponsor Approval Service - Remote Gated Approval for the AIEMS Exchange Bridge | Programme Sponsor proposal at ESR-0029 WP5, following Claude's own review of `scripts/aiems_bridge.py` identifying that `cmd_sponsor_decision` has no technical barrier preventing self-approval | Complete | High | Programme Sponsor | The Programme Sponsor proposed replacing `scripts/aiems_bridge.py`'s file-based `sponsor-decision` command with a remote HTTP service: agents hold only a read-only agent token (can `GET /decisions/latest`, structurally cannot write regardless of payload), while the sponsor token (the only credential that can `POST` a decision) never exists in any environment the agents can read, entered only into a host terminal running a separate `sponsor_client.py`. Deployed behind Tailscale (private address, free/personal tier) rather than the open internet. Claude's independent review (direct code inspection, not assumption) confirmed the motivating problem is real and understated: `cmd_sponsor_decision` was a plain CLI subcommand with no code-level gate - any process with repository access, agent included, could invoke it directly, and separately, the transcript `.md` file it wrote to was unsigned plaintext, appendable by anything with filesystem write access. Also surfaced a gap the proposal alone would not have closed: `cmd_submit_response` (the code path meant to gate "proceed with the approved change") had not actually been used as the real gate in practice across ESR-0029 - every Work Package's actual commit went through reading the local transcript directly and running `git commit` via Bash, never through `submit-response`. **The decision half is Complete**: [[ADR-0022_SPONSOR_APPROVAL_SERVICE|ADR-0022]] (Approved, Programme Sponsor, 19 July 2026) records the architecture decision and binding requirements, including the operational-practice requirement that `submit-response` becomes the actual practiced commit gate once implemented. **The code half of the implementation is Complete, delivered at ESR-0030 WP1** per [[EIP-ESR0030-001_SPONSOR_APPROVAL_SERVICE_IMPLEMENTATION|EIP-ESR0030-001]] v1.0: `scripts/sponsor_approval_service.py` (new, stdlib-only HTTP service - `http.server.ThreadingHTTPServer`, SQLite persistence, `hmac.compare_digest` token comparison, structurally write-incapable GET route), `scripts/sponsor_client.py` (new, host-side CLI auto-capturing `repository_ref` via local `git rev-parse HEAD`), and the `scripts/aiems_bridge.py` diff (`cmd_sponsor_decision`/`find_latest_sponsor_decision` deleted entirely, `cmd_submit_response` now calls the new `fetch_latest_decision`, fail-closed on any unreachable/malformed-response case). Zero new third-party dependency - stdlib (`http.server`, `sqlite3`, `urllib.request`) covers every capability needed, an improvement on ADR-0022's own anticipated `requests` dependency. 21 new tests in two new files (13 service, 8 client); the existing bridge test file lost 2 (the now-deleted `cmd_sponsor_decision`/`find_latest_sponsor_decision` tests) and gained 1 (a new fail-closed-when-unreachable case), with several pre-existing tests rewritten to inject a fake decision instead of calling `cmd_sponsor_decision` directly. 315 tests total (was 295). Live end-to-end verified against a real running instance on `127.0.0.1` (not a real Tailscale address - see below): agent-side `GET` succeeding with a valid token and failing (401/403) with a missing/wrong one; `sponsor_client.py` successfully `POST`ing a real decision; `POST` failing 403 when presented the agent token instead of the sponsor token; `submit-response` correctly refusing when no decision exists, when the decision rejects, when the repository has drifted (tested against a deliberately stale fabricated `repository_ref`), and when the service is genuinely stopped (confirmed via a real killed process and closed port, not simulated) - and succeeding once a matching real approval exists. A real connection-draining bug was found and fixed during this live verification's own test-suite run (not hypothetical): `do_POST` returned an auth-failure response without first draining the request body already sent by the client, leaving unread bytes on the socket that intermittently caused a client-side connection reset under the full test suite's load - fixed by reading the body unconditionally before any early return. **Sponsor-side deployment acceptance confirmed at ESR-0030 WP2**, satisfying EIP-ESR0030-001 Section 9 item 4 in full: the Programme Sponsor installed Tailscale on both the service host and a genuinely separate second device (Android, via Termux), generated and holds `AIEMS_SPONSOR_TOKEN` exclusively (never shared with or visible to the Engineering Implementer), started `sponsor_approval_service.py` themselves from their own terminal session with real non-development tokens, and exposed it via `tailscale serve` at a real private tailnet address (`https://desktop-ogfkjds.tail266bfe.ts.net`). Live end-to-end confirmed with genuine separation, not simulated: the Engineering Implementer's `GET` succeeded from the real agent-side environment using only `AIEMS_AGENT_TOKEN`; a real approval decision was recorded by the Programme Sponsor from the separate Android device (`~/approve`, a personal convenience wrapper around `sponsor_client.py` kept on that device only, never part of this repository) and independently fetched, ref-matched and accepted by `submit-response` on the Engineering Implementer's side with zero involvement in the approval action itself - genuine separation between the party requesting and the party granting approval, not merely file-based convention, closing the self-approval gap this item exists to close. This exceeds the Section 9 item 4 bar, which only required the Engineering Implementer's own `GET` to succeed; a real separate-device Sponsor-originated `POST` is stronger evidence still. EBG-0084 is Complete in full - both the decision (ADR-0022) and the implementation (code plus deployment). |
| EBG-0085 | esbuild/vite Dev-Server Vulnerability (npm audit, moderate/high) | External read-only security review, 19 July 2026, following ESR-0029's closure - reviewer examined credential handling, injection surfaces, network exposure, the AIEMS bridge, Tauri/CSP config and both dependency trees, no files touched | Candidate Backlog | Medium | Programme Sponsor | `npm audit` flags `esbuild <=0.24.2` (pulled in via `vite`): while `npm run dev` is running, any website a browser tab has open can send requests to the local dev server and read the response (a cross-origin dev-server request-forgery class of issue). Partially mitigated already - `vite.config`/`package.json` explicitly bind the dev server to `--host 127.0.0.1` rather than Vite's default, narrowing exposure to processes/browsers on the same machine - but the vulnerable dependency version remains installed, so the gap is not fully closed. The reviewer's suggested fix, `npm audit fix --force`, pulls in Vite 8 - a breaking major-version change that needs its own build/dev-server smoke test before merging, not a drop-in patch. No implementation authorised by this entry; a future EIP should scope the Vite 8 upgrade and its verification. |
| EBG-0086 | `.aiems-exchange/` Filesystem Permission Enforcement (Currently Convention Only) | External read-only security review, 19 July 2026 | Candidate Backlog | Medium | Programme Sponsor | `.gitignore`'s comment states `.aiems-exchange/` (the AIEMS Exchange Bridge's transcript/lock working directory) is never shared or committed, per the bridge's own restrictive-permissions requirement - but `scripts/aiems_bridge.py` never actually calls `chmod` or otherwise restricts filesystem permissions on that directory or its contents; the only real protection is `.gitignore` keeping it out of version control. On a machine with other local user accounts, transcript and lock files would be readable (and writable, depending on default umask) by any other account with shell access to that machine. Low real-world risk today - the Programme Sponsor is the sole user of this machine - but the doc comment currently implies an enforced control that isn't actually implemented in code, which cuts against this project's own stated principle (see [[ADR-0022_SPONSOR_APPROVAL_SERVICE|ADR-0022]] and this session's own `submit-response`-gate finding) of enforcing properties in code rather than by convention alone. Two candidate resolutions, either acceptable: (a) have `aiems_bridge.py` restrict permissions on `.aiems-exchange/` at creation (e.g. owner-only), or (b) correct the `.gitignore` comment to accurately describe convention-only protection rather than an enforced guarantee. No implementation authorised by this entry - a future EIP should pick one and scope it. |
| EBG-0087 | GitHub Actions Pinned by Mutable Tag, Not Commit SHA | External read-only security review, 19 July 2026 | Candidate Backlog | Low | Programme Sponsor | CI workflow(s) reference actions like `actions/checkout@v4` by mutable tag rather than a pinned commit SHA - tags can be moved/repointed by the action's maintainer (or an attacker who compromises their account), where a SHA pin cannot silently change underneath the workflow. This is the OpenSSF-recommended hardening for third-party Actions. Low urgency here specifically: the workflow carries no secrets and triggers on `pull_request` (not the riskier `pull_request_target`, which would run with write-level secrets against untrusted PR code), so there is little for a compromised action to actually steal in this repository's current CI configuration. Cheap to fix if wanted as a standing habit. No implementation authorised by this entry. |
| EBG-0088 | No Explicit `.env` Entry in `.gitignore` | External read-only security review, 19 July 2026 | Candidate Backlog | Low | Programme Sponsor | Nothing currently tracked in the repository is a real credential - every credential is a `CredentialReference` to an environment variable name, never a value in source, confirmed directly by the reviewer. But `.gitignore` has no explicit `.env`/`.env.*` entry, so there is no standing guard against a future accidental `git add .env` introducing a real local secret file into version control. A one-line insurance addition. No implementation authorised by this entry - trivial enough to action directly in a future session without a full EIP if preferred. |
| EBG-0089 | Tauri CSP Allows `style-src 'unsafe-inline'` | External read-only security review, 19 July 2026 | Candidate Backlog | Low | Programme Sponsor | The Tauri Content-Security-Policy is otherwise tight (`object-src 'none'`, `base-uri 'none'`, `script-src 'self'` only) but `style-src` permits `'unsafe-inline'`. This is a common, minor tradeoff made to support inline styles and is not by itself a script-injection path (CSS-only injection cannot execute arbitrary script), but it is a real, if narrow, weakening of the policy versus a fully locked-down CSP. No implementation authorised by this entry - candidate for a future pass if/when inline styles can be migrated to a nonce/hash-based or external-stylesheet approach. |
| EBG-0090 | Investigate CLI-Driven Coding Agents (Claude Code / Codex CLI) as a Cost-Avoidance Route for Guardian Research/Engineering Tasks | Programme Sponsor discussion following ESR-0030's closure, prompted by observing this session's own use of `claude`/`codex` CLI subprocess invocation in `scripts/aiems_bridge.py` | Candidate Backlog | Low | Programme Sponsor | The Programme Sponsor asked whether Guardian could use Claude Code/Codex CLI - the same tools this project's own engineering workflow already shells out to (`aiems_bridge.py`'s `run_preflight()`) - for reasoning or research tasks instead of Sentinel's existing direct provider APIs (`sentinel/openai_provider.py`, `sentinel/gemini_provider.py`, `sentinel/ollama_provider.py`), specifically where doing so could avoid metered per-token API cost by using subscription-based billing (Claude Pro/Max, ChatGPT Plus) the Programme Sponsor already holds. **Explicitly scoped as investigation only, not a replacement for Guardian's main conversational route**: Claude Code and Codex CLI are built for interactive, stateful coding-agent sessions with heavy tool-use scaffolding (file I/O, bash execution, large coding-oriented system prompts) - shelling out to them per ordinary conversational turn would add real subprocess/session overhead unsuited to lightweight text-generation calls, a genuine architecture cost distinct from any licensing question. **Where it would genuinely earn its keep**: a narrow, separate "research/engineering assistant" capability Guardian invokes only for tasks that benefit from these tools' existing tool ecosystems (web search, code execution) - which Sentinel's current bare provider adapters have no equivalent of at all - not a wholesale reasoning-backend swap. **Confirmed this is personal, non-commercial use** (JARVIS/Guardian is the Programme Sponsor's own personal assistant, not a resold product), which removes the more serious commercial-resale licensing concern; a softer point remains worth disclosure rather than silently assumed away - Claude Code specifically is scoped around software-engineering assistance even in its personal-use terms, so driving it as a general conversational backend sits somewhat outside its designed lane, practically low-risk for a single-user personal automation but not squarely "what it's for." **The Programme Sponsor's own explicit condition, recorded verbatim**: cost is important, but only where using an existing subscription "doesn't compromise performance" - any future implementation must demonstrate no material latency/quality regression versus the current direct-API route before being adopted, not merely be cheaper. No implementation authorised by this entry - a future investigation/spike should scope: which specific Guardian task types would route through this (starting with research, not general conversation), a working prototype invoking `claude -p`/equivalent non-interactive Codex CLI invocation as a subprocess, and a real side-by-side cost/latency/quality comparison against the existing direct-API route before any production wiring is proposed. |
| EBG-0091 | Streamline Daily Sponsor Approval Command (Drop Explicit Session Identifier) | Programme Sponsor brainstorming, 20 July 2026, following ESR-0030's own live use of `~/approve`/`~/reject` on a real device | Candidate Backlog | Low | Programme Sponsor | The daily approval command (`~/approve <session> <work_package> "note"`) requires retyping the full session identifier (e.g. `ESR-0030`) every time, even though PBK-0001 already enforces that only one Engineering Session is active at any time - making the session identifier inferable, not real information the Sponsor needs to supply. Assessed in discussion as safe and easy: unlike the batching idea considered and explicitly rejected in the same discussion (which would have turned Sponsor approval into a rubber-stamp of already-completed work, undermining the exact property ADR-0022 exists to protect), this is pure redundancy removal with no security cost. Proposed mechanism: the `~/approve`/`~/reject` wrapper scripts (kept on the Sponsor's own device only, never part of this repository) read the current session name from a small local file (e.g. `~/.current_session`), updated once per session-open as part of the WP0B ritual rather than on every approval. No implementation authorised by this entry - a future EIP or lightweight change should scope the wrapper-script update and the WP0B ritual addition together, since the two need to land in step. |
| EBG-0092 | Formalise WP-Independence / Scope-Creep-Flagging Discipline into PBK-0001 | Programme Sponsor self-diagnosed pattern, 20 July 2026: authorising a Work Package then mid-session folding in an unrelated backlog item, causing scope growth, multi-hour sessions, and the original objective being pushed to backlog instead | Candidate Backlog | Medium | Programme Sponsor | The Programme Sponsor observed that keeping Work Packages genuinely independent of one another (no WP depending on another still-pending WP) serves two purposes at once: it is a prerequisite for any future asynchronous approval workflow, and it is a direct countermeasure to their own recurring pattern of expanding a WP's scope mid-session with an unrelated idea, which has historically produced long sessions and left older backlog items untouched. This practice has already been adopted as a standing behavioural instruction for the Engineering Implementer (flag any proposed addition that would expand a WP's scope or create a dependency on another pending WP, name it plainly, and require the Programme Sponsor to explicitly override rather than silently accommodating it - Principle 5, Human Accountability, means this is a named checkpoint, not a hard veto) and is already in effect regardless of this entry. This backlog item is narrower: formalise that already-adopted practice into PBK-0001 itself (alongside existing behavioural sections such as Feature-First Delivery Discipline and Operational Verification Before Reporting), so it is durable governance record rather than only a private working understanding. No implementation authorised by this entry - a future EIP should draft the PBK-0001 wording and route it through the normal Engineering Reviewer/Programme Sponsor review cycle like any other playbook change. |
| EBG-0093 | PC-Side Installer Script for the Sponsor Approval Service | Programme Sponsor brainstorming, 20 July 2026, drawing a direct comparison to the existing `scripts/start-jarvis.bat` launcher (EBG-0078) | Candidate Backlog | Low | Programme Sponsor | Setting up `scripts/sponsor_approval_service.py` today requires the Programme Sponsor to manually check for/install Tailscale, set two environment variables in their own terminal session, and start the service and `tailscale serve` by hand - straightforward but manual, and only documented in the field-reference guide (a Claude Artifact, not a controlled repository artefact) rather than a runnable script. Proposed scope, mirroring EBG-0078's existing precedent of recording an already-useful double-click launcher rather than a packaged installer: a `.bat` or `.ps1` script that checks for and offers to install Tailscale (`winget install tailscale.tailscale`) if missing, prompts interactively for `AIEMS_SPONSOR_TOKEN` (must never hardcode, auto-generate, or silently persist this value without the Sponsor's explicit action - that would undermine the separation-of-duties property the service exists for) and `AIEMS_AGENT_TOKEN`, then starts the service and `tailscale serve`. Assessed in discussion as genuinely easy, comparable effort to the existing launcher script - a one-time setup convenience rather than a per-approval time saver, so lower priority than EBG-0091/EBG-0094/EBG-0095's recurring-friction reductions. No implementation authorised by this entry - a future EIP should scope the exact script, its token-prompt behaviour, and confirm it is documented consistently with (or supersedes) the equivalent manual steps in the field-reference guide. |
| EBG-0094 | Self-Service Sponsor Approval Service Status Check | Claude's own suggestion during a 20 July 2026 review of this session's own approval-workflow friction, prompted by two real failures earlier the same day (the service not running, a stale duplicate process holding the port) | Candidate Backlog | Low | Programme Sponsor | Twice in ESR-0030/its post-closure follow-on work, the Programme Sponsor attempted an approval and received a confusing failure (an HTTP 502 when the service process had stopped; persistent 403s when a stale duplicate process was still holding the port from an earlier run) that required a diagnostic back-and-forth with Claude to resolve, rather than being self-diagnosable in one step. Proposed scope: a small `~/bridge-status` (or similarly named) command, alongside the existing `~/approve`/`~/reject` wrappers, that performs an unauthenticated or lightweight check against the configured `AIEMS_SPONSOR_URL` and reports plainly whether the service is reachable - letting the Programme Sponsor confirm the service's state themselves before attempting an approval, rather than discovering a failure mid-attempt. No implementation authorised by this entry - a future EIP should scope the exact check performed (e.g. a bare connectivity probe versus a full authenticated round-trip) and confirm it reveals no information a passive network observer couldn't already infer from the existing `GET /decisions/latest` route's own behaviour. |
| EBG-0095 | Auto-Start the Sponsor Approval Service on Windows Login | Claude's own suggestion during the same 20 July 2026 review, directly motivated by the Programme Sponsor forgetting to start the service before attempting an approval earlier the same day | Candidate Backlog | Low | Programme Sponsor | The Sponsor Approval Service must currently be started manually, in the Programme Sponsor's own terminal, every time the host machine restarts or the process is otherwise stopped - a real, already-observed friction point (the Programme Sponsor's own "my bad, i forgot to run the service" this same day). Proposed scope: a Windows Scheduled Task (or a Startup-folder shortcut) that starts `scripts/sponsor_approval_service.py` automatically on the Programme Sponsor's own login, reading `AIEMS_AGENT_TOKEN`/`AIEMS_SPONSOR_TOKEN` from a persistent user environment variable or a local credential store rather than requiring interactive entry at every boot - a real design question distinct from EBG-0093's installer script, since a value read automatically at login is a different trust posture than one entered interactively, and should be scoped and reviewed as such rather than assumed equivalent. No implementation authorised by this entry - a future EIP should scope exactly how the sponsor token is persisted for unattended startup without weakening the property that it must never exist in any agent-reachable environment. |
| EBG-0096 | Automate the Claude<->Codex Review Handoff via Non-Interactive CLI Invocation | Programme Sponsor question, 20 July 2026: is there a way to automate submit-to-review/return-findings instead of manually typing "check inbox" into separate Claude Code and Codex tabs each time | Candidate Backlog | Medium | Programme Sponsor | Today, every `submit-to-review`/`return-findings` cycle in `scripts/aiems_bridge.py` requires the Programme Sponsor to act as a manual relay: flicking between two separate VS Code tabs (Claude Code, Codex) and typing "check inbox" into each in turn. A genuine feasibility test was run live in conversation, both directions, before this item was registered - not assumed: **(1) Claude invoking Codex**: `codex exec -C "i:/Project AI" -s read-only "Reply with exactly the word PONG and nothing else. Do not read or write any files."` ran cleanly from Claude's own Bash tool and returned `PONG` in a few seconds, with no session/login conflict despite the Programme Sponsor's own interactive Codex tab being open in the same project at the time. **(2) Codex invoking Claude** (tested independently by Codex itself, at the Programme Sponsor's direct request, before Claude saw any result): `claude -p --no-session-persistence --tools= "Reply with exactly the word PONG and nothing else. Do not read or write any files."` (note the `--tools=` empty-value syntax - an initial attempt using `--tools ""` as a separate argument failed with `Error: Input must be provided either through stdin or as a prompt argument when using --print`, since the CLI's argument parser consumed the prompt differently) returned `PONG` cleanly, again with no session conflict despite Claude's own active interactive session running in the same project at the same time. **Genuinely de-risks the "single-session-per-account" gotcha** noted during EBG-0057's own original setup (ESR-0025A) - that limitation does not appear to apply to these specific non-interactive invocation modes (`codex exec`, `claude -p --no-session-persistence`), only (apparently) to concurrent interactive sessions. **One real operational nuance surfaced during Codex's own test, disclosed rather than glossed over**: Codex reported its own execution sandbox "often fails before these commands run," requiring escalation outside its normal sandbox to get the invocation to actually succeed - any real implementation needs to account for sandbox/permission requirements on at least the Codex-invoking side, not assume a default sandboxed shell suffices. No implementation authorised by this entry - a future EIP should scope: which side initiates the automated cycle (a fully closed loop with no human trigger at all, versus the Programme Sponsor still manually kicking off each review cycle but the back-and-forth CLI calls happening automatically thereafter - a real design choice, not decided here); how the existing file-based `.aiems-exchange/*/inbox` audit trail is preserved when invocation is automated rather than manual; and the exact sandbox/permission mode each side's non-interactive invocation should run under, informed directly by the sandbox-escalation finding above. **A full real end-to-end round-trip was subsequently tested live, not just a text-only probe**: `codex exec -s workspace-write` was invoked directly from Claude's own Bash tool with a genuine review task (verify a real EBR-0001/REG-0001 registration against the actual repository state, independently run validation, then call `return-findings` itself). The first attempt failed entirely - every Python invocation (`pytest`, `validate_repository.py`, and the `aiems_bridge.py return-findings` call itself) errored before Python could even start, with `windows sandbox: helper_unknown_error: setup refresh had errors`; Codex correctly refused to fabricate a bridge response file when blocked, reporting the limitation honestly instead. After the Programme Sponsor adjusted a local sandbox/permission setting (reversibly, as a deliberate test), a second attempt succeeded in full: Codex read the real inbox request, verified the real register content, ran `python scripts/validate_repository.py` itself (0 errors/130 warnings, matching submitted evidence), and successfully called `return-findings` - a genuine, complete, unattended round trip with no manual "check inbox" typing at all. **One narrower limitation remained even after the fix**: `python -m pytest -q` specifically still failed with the identical sandbox error on every attempt (including `py -3 -m pytest -q` and `cmd /c python -m pytest -q`), while `validate_repository.py` and the bridge script itself ran successfully in the same environment - meaning whatever the Programme Sponsor changed fixed most Python execution but not pytest specifically, suggesting the failure is tied to something pytest itself does on invocation (likely subprocess/multiprocessing) rather than Python generally. **A cold-start cost was also observed and is worth expecting**: every single `codex exec` invocation (the initial probe and both review attempts) printed `ERROR codex_models_manager::cache: failed to load models cache: missing field 'supports_reasoning_summaries'` before proceeding - consistent with the Programme Sponsor's own separate observation that the first invocation in a session tends to run noticeably slower than subsequent ones, though the exact mechanism (cache rebuild specifically, versus general process/auth warm-up) is not confirmed, only the correlation. **A further refinement was tested and confirmed after the above**: the same sandbox helper error reappeared on a later review attempt despite the earlier fix, which prompted direct root-cause investigation rather than a retry. Direct inspection of `~/.codex/.sandbox/sandbox.2026-07-20.log` showed the write-ACE grant to the project folder failing with `SetNamedSecurityInfoW failed: 5` (Windows `ERROR_ACCESS_DENIED`) on every single attempt that day (33 of 33) - the earlier "worked once, broke again" appearance was in fact a same-session read-ACL fallback occasionally letting individual invocations through, not the underlying grant ever actually succeeding. The root cause traced to `[windows] sandbox = "elevated"` in Codex's own `config.toml` - a setting the Programme Sponsor had deliberately introduced for a specific reason: when the Programme Sponsor was manually copy-pasting whole EIP documents between Claude and Codex, Codex would sometimes misread the pasted directive language as authorisation to act, and would push/commit without a genuine Sponsor decision. Rather than relaxing `elevated` (which would reopen that exact risk), the automated review invocation was redesigned to run Codex in `-s read-only` sandbox mode instead of `workspace-write`: Codex has zero write/commit/push capability by construction in this mode, not as a fragile side-effect of a failing permission grant, and reports findings as plain text instead of calling `return-findings` itself; the Claude Engineering Implementer then relays Codex's verbatim findings into the bridge, under explicit per-instance Programme Sponsor approval for that relay act (the first attempt to do this was correctly blocked by Claude Code's own auto-mode permission classifier pending that approval). Tested live end-to-end for POST-ESR0030/WP1 reviewing commit `f8c42b5`: Codex independently verified the actual EBR-0001/REG-0001 state, ran `validate_repository.py` successfully (0 errors, 130 warnings) with no sandbox error at all, and reported a genuine Pass; `pytest` still failed in this mode, but for a different, benign reason (`FileNotFoundError: No usable temporary directory found`, since read-only mode cannot create pytest's own cache/capture temp files at all) rather than the sandbox helper error - a cleaner, more honest failure mode than before. This read-only-plus-relay pattern is now the recommended approach for any future implementation of this automation: it fully preserves the Programme Sponsor's `elevated`-sandbox safety boundary against unauthorised Codex writes while still allowing genuine unattended review cycles. |
| EBG-0097 | AIEMS Session-Opening Launcher | ESR-0031 WP0B, selected as the session's process-improvement objective (paired with WP2 per PBK-0001's Feature-First Delivery Discipline) | Complete | High | Programme Sponsor | Implemented per [[EIP-ESR0031-001_SESSION_OPENING_LAUNCHER|EIP-ESR0031-001]] v1.0: `scripts/session_launcher.py`, a read-only reporting script gathering PST-0001's Current Mode/Baseline, EBR-0001's open High-priority backlog, and JRM-0001's Near-term roadmap candidates into a single report. A genuine defect was found and fixed during the package's own required live smoke check (per PBK-0001 Section 10 validation), not just unit tests: a naive `line.split("|")` table-row parser silently corrupted every row after the first one containing a WikiLink with display text in a non-ID cell (a double-square-bracket link whose target and display text are themselves separated by a literal pipe character), shifting later columns and dropping several genuinely High-priority backlog items from the report without any error - fixed with a WikiLink-aware row splitter that treats each such link as one atomic cell, with a regression test. A second defect (the table header row `| EBG-ID | ... |` itself matching the "row found" detection, since "EBG-ID" starts with "EBG-", masking the intended "no data rows" error case) was also found via the test suite and fixed. 9 new tests (347 total, was 338), `validate_repository.py` clean. |
| EBG-0098 | Version Badge/Table Drift Detection in validate_repository.py | Discovered during ESR-0031 WP0: PST-0001's top-of-file `**Version:**` badge had silently diverged from its own Document Control table Version field (2.66 vs 2.58, eight versions apart, unnoticed for several sessions) | Candidate Backlog | Medium | Programme Sponsor | `validate_repository.py`'s existing `extract_document_version()` only ever reads one of the two locations (preferring the Document Control table), so a badge/table drift like the one found this session produces no validation error - the check that exists (`check_controlled_register`) only compares the table value against REG-0001's row, never the badge against the table within the same document. A future EIP should add a check comparing both locations directly and erroring on mismatch. `scripts/bump_version.py` already updates both atomically when used, so this drift most likely originated from a manual edit that bypassed it - also worth noting in that future EIP that `bump_version.py` should be used in preference to manual edits, not just that the drift-check should exist. No implementation authorised by this entry. |
| EBG-0099 | Streaming Notifications MVP (EBG-0050 remaining scope) | ESR-0031 WP2, selected as the session's product-moving objective after Shared Family Memory was ruled out as premature (no household identity/authentication system exists to enforce access differentiation against, per GAM-0001 Section 10's own explicit non-goal) | Complete | High | Programme Sponsor | Implemented per [[EIP-ESR0031-002_STREAMING_NOTIFICATIONS_MVP|EIP-ESR0031-002]] v0.2, closing EBG-0050's long-open "streaming notifications" remaining scope. `jarvis/interfaces/stdio_rpc.py`'s `StdioRpcServer` gained a background heartbeat thread emitting a `system.heartbeat` JSON-RPC notification (no `id` key) every `JARVIS_HEARTBEAT_INTERVAL_SECONDS` (default 30s), guarded by a shared write lock so it can never interleave with response writes - 4 new in-process unit tests (StringIO-based, exercising `_heartbeat_loop()`/`serve_forever()` directly), covering notification shape/no-`id`-key, stop-before-first-interval, regression safety for existing request/response calls, and interleaving-corruption safety. **Correction (Codex post-implementation finding)**: this row previously and incorrectly described a real-subprocess smoke test as part of "4 new tests" in the committed suite - it is not; the real-subprocess verification (a manual script invoking the actual `python -m jarvis --ipc-stdio` entry point, confirming genuine heartbeat emission at the configured interval) was run separately as ad hoc manual verification, never added to the committed test suite, and should not have been described as if it were. `src-tauri/src/lib.rs` was restructured from a single synchronous write-then-read per call into a background reader thread plus a pending-call dispatch map: a line with an `id` key routes to the matching in-flight call, a line with no `id` key is forwarded to the frontend via `app_handle.emit("jarvis://notification", ...)`, and an unparsable line fails every currently-pending call and resets connection state (the multi-call generalisation of the pre-existing single-call teardown behaviour) rather than being silently skipped, per a Codex pre-implementation finding fixed in EIP v0.2. Compiled cleanly with `cargo build`, no warnings. `src/App.jsx` gained a second, independent `useEffect` listening for `jarvis://notification` via `@tauri-apps/api/event`, displaying the live heartbeat timestamp next to the System Health panel - the existing one-time `platform_status`/`knowledge_graph` mount fetch is unchanged. `npm run build` clean. **Live smoke check performed per PBK-0001's Operational Verification Before Reporting**: a real `npm run tauri dev` session was launched, the actual native window screenshotted twice ~35 seconds apart - the heartbeat timestamp genuinely advanced (16:34:18 to 16:35:48) while `platform_status`/`knowledge_graph` (the pre-existing request/response path, using the same restructured `call_backend()`) continued to populate real System Health/Knowledge Metrics/Active Clusters data correctly in the same session, directly confirming Implementation Requirement 4 (existing call semantics preserved) without relying on unit tests alone. A stray UI-automation attempt to also exercise `send_message` via simulated clicks/keystrokes misfired onto the wrong foreground window (VS Code, not the JARVIS app) and was abandoned as unnecessary once this evidence was already in hand - disclosed here for completeness, not because it affected the result. The dev session left a Vite `node.exe` holding port 1420 after `TaskStop` (the same known-insufficient-cleanup pattern already recorded in standing practice) - found and killed explicitly, port confirmed free. 351 tests total (was 347), `validate_repository.py` clean. |
| EBG-0100 | UXP Capability Sidebar Memory Row is Hardcoded, Never Live-Data-Backed | Discovered during ESR-0031 WP2's live smoke check screenshot: the sidebar showed "Memory: Not Implemented" despite EBG-0080's real Personal Memory implementation existing since ESR-0027 | Candidate Backlog | Medium | Programme Sponsor | `src/platformStatus.js`'s `capabilityStatuses` array hardcodes the Memory row's state as `NOT_IMPLEMENTED` with detail "Persistent memory is outside this implementation". `App.jsx`'s `deriveCapabilityStatuses()` only overrides the `sentinel` and `providers` rows with live `platformState` data - `memory`, `platform-services` and `agent-framework` all pass through unchanged from the static array regardless of actual backend state, so the Memory row can never reflect reality no matter what is actually implemented. This is the inverse of the usual no-mock-fallback concern (PBK-0001): rather than overclaiming a capability that doesn't exist, it under-claims one that does. A future EIP should wire a real memory-capability field into `platform.status`'s RPC response and have `deriveCapabilityStatuses()` derive the Memory row from it, the same pattern already used for `sentinel`/`providers`. No implementation authorised by this entry. |
| EBG-0101 | `bump_version.py`'s `--date` Flag Silently Defaults to a Stale Hardcoded Placeholder | Discovered during ESR-0031 WP4's own post-implementation fix round: three real `bump_version.py` invocations (PBK-0001, PST-0001, RBL-0018) were run without an explicit `--date`, and every resulting Version History row/REG-0001 alignment row was silently dated "9 July 2026" - eleven days stale against the actual date (20 July 2026) | Candidate Backlog | Medium | Programme Sponsor | `scripts/bump_version.py`'s argument parser sets `parser.add_argument("--date", default="9 July 2026")` - a literal example value from its own `--help` text, reused as a real functional default rather than deriving today's date via `datetime.date.today()`. Silently produces incorrect Version History dates whenever `--date` is omitted, which is exactly the kind of unforced documentation staleness the session's own Documentation Debt Discipline amendment (this same session, PBK-0001) exists to catch - found ironically while applying that same discipline to this baseline's own post-review fix round. Caught only because the resulting dates were visibly out of chronological order against surrounding entries, not by any automated check. A future EIP should change the default to compute today's date automatically (respecting an explicit `--date` override for backdating historical entries where genuinely needed), and add a regression test asserting the no-argument case produces today's date rather than any fixed string. No implementation authorised by this entry - the affected rows from this session were caught and corrected manually before commit. |
| EBG-0102 | Guardian Desktop Distribution Foundation (Sidecar Packaging, Tauri Bundling, Clean-Machine Install) | Two independent external read-only repository reviews, 21 July 2026 ("Deployment Readiness and Gap Analysis", "Backlog and Additional Feature Development Gap Analysis"), both cross-checked directly against the live repository by Claude Engineering Implementer and independently by Codex Engineering Reviewer before registration - not taken on the reports' word alone | Complete | Critical | Programme Sponsor | Implemented per [[EIP-ESR0032-001_GUARDIAN_DESKTOP_DISTRIBUTION_FOUNDATION|EIP-ESR0032-001]] v1.0, promoting EBG-0050's long-open "production sidecar bundling/packaging" remaining scope to a first-class item and closing it. `scripts/jarvis_backend_entry.py` (new) - a headless entry point running only the stdio JSON-RPC bridge, deliberately separate from `jarvis.app.main()` to avoid bundling Tkinter (which `jarvis.app` imports unconditionally for the legacy First Light GUI) into a backend that never needs it. `scripts/build_backend_sidecar.py` (new) - packages that entry point via PyInstaller (`--onefile`) into `src-tauri/binaries/jarvis-backend-<target-triple>.exe`, matching Tauri's `externalBin` sidecar naming convention; target triple auto-detected via `rustc -vV` rather than hardcoded. `src-tauri/src/lib.rs`'s `spawn_backend()` now branches on `cfg!(debug_assertions)`: dev builds keep the original unchanged `std::process::Command::new("python")` path; release builds spawn the packaged sidecar via `tauri-plugin-shell`'s async `Command::sidecar()` API. A new `BackendHandle` enum (`Dev`/`Sidecar` variants) and a shared `dispatch_line()` function let both paths reuse the same JSON-RPC response/notification classification logic (EIP-ESR0031-002) rather than duplicating it - only the mechanism for obtaining each line differs (synchronous `BufReader` vs. an async `CommandEvent` channel consumed via `blocking_recv()`). `tauri-plugin-shell` (Cargo) and `@tauri-apps/plugin-shell` (npm) added; `src-tauri/capabilities/default.json` grants `shell:allow-execute` scoped to exactly this one named sidecar, not a general shell-execute permission. `bundle.active` enabled, `bundle.targets` set to `["nsis"]`, `bundle.externalBin` registered. **Live-verified end to end, not assumed**: a real `npm run tauri build` produced an actual installer (`JARVIS Guardian Shell_0.1.0_x64-setup.exe`) - Tauri auto-fetched NSIS live, resolving the pre-implementation-disclosed toolchain uncertainty. The release executable was run directly (screenshot evidence): the System Health panel populated with genuine live data via the bundled sidecar (`platform.status` round-trip, not stuck connecting); process inspection confirmed the sidecar's PyInstaller onefile bootloader+child pair both genuinely running; graceful app close triggered `BackendHandle::kill()`, and process inspection afterward confirmed **zero orphaned processes** - both the bootloader and its child terminated. Dev mode was independently re-verified live afterward with zero regression: `npm run tauri dev` still spawns a raw `python -m jarvis --ipc-stdio` process exactly as before (confirmed via process command-line inspection), with both System Health and the Knowledge Graph rendering correctly. **Explicitly not done this WP, per EIP-ESR0032-001's own scope**: clean-machine (separate, no-toolchain VM) acceptance testing - only verified on this development machine; code signing; CI changes (EBG-0103); release automation/version sync (EBG-0104). `pyproject.toml` gained `pyinstaller>=6.21` as a dev dependency. |
| EBG-0103 | CI Does Not Build, Test or Gate the Complete Desktop Product | Same two external reviews, 21 July 2026, independently verified against `.github/workflows/ci.yml` | Complete | High | Programme Sponsor | Implemented per [[EIP-ESR0032-002_CI_BUILD_GATE_HARDENING|EIP-ESR0032-002]] v1.0. `.github/workflows/ci.yml` gained a `rust` job (`cargo build`/`clippy -- -D warnings`/`fmt --check`/`test`, all blocking from the start) and a `playwright` job; `frontend-build`'s `continue-on-error` narrowed from job-level to only its `npm audit` step. **Real Linux verification, not assumed**: at the Programme Sponsor's suggestion, a WSL Ubuntu 26.04 install on this machine was used to actually run the planned CI checks locally before writing the workflow, rather than trusting the first real GitHub Actions run alone. This found and fixed three genuine bugs: (1) `schemars` 0.8.22 (pulled in transitively via `tauri-build`/`tauri-utils` for Linux/dbus capability-schema generation, a code path Windows never exercises) requests `indexmap`'s `serde-1` feature but never `std`, and `indexmap` 1.9.x has no default features at all - without `std`, `indexmap`'s own `#[cfg(has_std)]` split drops the `RandomState` default from `IndexMap<K, V, S>`, breaking `schemars`'s own type alias (`error[E0107]`); fixed by forcing `indexmap`'s `std` feature via an explicit `[build-dependencies]` entry. (2) `scripts/build_backend_sidecar.py` (EBG-0102) was Windows-only - hardcoded a `.exe` suffix PyInstaller never produces on Linux, and used `shutil.copyfile` (which silently drops the executable permission bit) instead of `shutil.copy`. (3) Only a single 32x32 `.ico` existed for the app icon - Tauri's `generate_context!()` macro fails outright without a PNG present, on any platform; regenerated the full icon set via `npx tauri icon`, discarding the iOS/Android/Windows-Store assets it also produces (no mobile targets exist), disclosed as low-resolution (upscaled from the only available 32x32 source) and left for a future design task, not fixed here. After all three fixes, a full `cargo build`/`clippy`/`fmt --check`/`test` passed cleanly on real Ubuntu. `playwright.config.js` and `tests/e2e/app.spec.js` (new, scope expanded per Programme Sponsor direction) - two real committed tests (app launch/system health; a full send-message round-trip), both passing, mocking Tauri's IPC layer via `page.addInitScript` against the real `window.__TAURI_INTERNALS__.invoke` mechanism (confirmed by direct source read of `@tauri-apps/api/core.js`), matching this repository's own prior ad hoc verification approach (EBG-0072/EBG-0073). Two genuine test-writing mistakes found and fixed by actually running the suite: a CSS-class selector matching both the user's and Guardian's messages, and a mocked `send_message` response returning a bare string when the app expects `{message, provider}`. `@playwright/test` added; the previously-unused bare `playwright` dependency removed as redundant. |
| EBG-0104 | No Automated Release Workflow or Version Synchronisation Across Build Files | Same two external reviews, 21 July 2026; distinct in scope from EBG-0098 (which only covers a single document's own badge-vs-table drift, not cross-file release versioning) | Candidate Backlog | High | Programme Sponsor | Verified: `pyproject.toml`, `package.json`, `src-tauri/Cargo.toml` and `src-tauri/tauri.conf.json` each carry their own independent version field (all currently `0.1.0`, not yet drifted, but with no mechanism preventing drift - four separate manual edit points for one logical release version). No tag-driven release workflow, artifact checksum generation, release manifest or publication process exists anywhere in the repository. This is a distinct, broader concern from EBG-0098 (which is scoped narrowly to `validate_repository.py` detecting a single controlled artefact's own badge-vs-Document-Control-table drift) - this item is about product release versioning across four build-tooling files, not governance-document versioning. Proposed scope, not authorised for implementation by this entry: choose one authoritative version source; a version-bump script updating all four files together (the same class of tooling as `scripts/bump_version.py`, but for product build files rather than controlled artefacts); a CI check validating version equality across all four; SHA-256 checksums and retained immutable build artifacts on release. No implementation authorised by this entry. |
| EBG-0105 | Transcript Export Filename Collision Risk | Same "Backlog and Additional Feature Development Gap Analysis" review, 21 July 2026; independently verified by direct code read, not taken on the report's word alone | Candidate Backlog | Low | Programme Sponsor | Verified in `jarvis/gui/app.py:222`: `timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")` - second-level granularity only, used directly as the transcript export filename (`jarvis_transcript_{timestamp}.md`/`.txt`) with no microsecond component, no exclusive-creation check (e.g. `os.O_EXCL`) and no collision-safe suffix. Two transcript exports triggered within the same wall-clock second would silently overwrite one another with no error surfaced to the user. A genuine, if narrow, defect - low severity given transcript export is a manual, infrequent user action, not a hot path. Proposed scope, not authorised for implementation by this entry: switch to a higher-resolution timestamp (microseconds) or add a collision-safe suffix / exclusive-creation check. No implementation authorised by this entry. |
| EBG-0106 | EBR-0001 Mixes Historical Ledger, Product Backlog and Process Register in One Table, With No Active-Development View | "Backlog and Additional Feature Development Gap Analysis" review, 21 July 2026; independently confirmed by both Claude Engineering Implementer and Codex Engineering Reviewer against the actual register structure | Candidate Backlog | Medium | Programme Sponsor | Verified: EBR-0001 is genuinely `Status: Draft` (not Approved), and its single table mixes Complete/Closed historical entries, open Candidate/Approved Backlog items, pure research findings, technical-debt log entries and process-improvement observations together with no relative sizing, target-release/horizon field, or separation of "value" from "urgency" within Priority. This is a real scanning/prioritisation cost, not a traceability defect - EBR-0001's completeness and historical preservation are not in question and should not be sacrificed. Proposed scope, not authorised for implementation by this entry: keep EBR-0001 as the sole authoritative historical register (do not fork governance authority), but generate a smaller, derived active-product-backlog view (e.g. a Now/Next/Later/Blocked grouping, mechanically generated from EBR-0001's own Status/Priority fields rather than manually maintained as a second source of truth - avoiding exactly the single-source-of-truth problem this session's own Documentation Debt Discipline was directed to prevent). No implementation authorised by this entry - a future EIP should scope the exact generation mechanism (a script analogous to `session_launcher.py`, not a new hand-maintained document) and whether it should be committed to the repository or generated on demand. |
| EBG-0107 | `session_launcher.py`'s `CurrentState` Omits Next Work Package Candidate and EBR-0001 Section 5A Entirely | Discovered live, post-ESR-0031, 21 July 2026: the Programme Sponsor ran `scripts/session_launcher.py` twice expecting to see the Theme 1/Theme 7 candidates just discussed and registered in PST-0001/EBR-0001 Section 5A, and the report showed neither, on either run - not a caching or overwrite defect, confirmed by direct code inspection | Candidate Backlog | High | Programme Sponsor | `scripts/session_launcher.py`'s `CurrentState` dataclass has exactly two fields (`current_mode`, `current_baseline`); `read_current_state()` only regex-matches PST-0001's "Current Mode" and "Current Repository Baseline" rows, and `build_report()` only ever prints those two under "## Current State" - "Next Work Package Candidate" was never read or displayed by this script, before or after PST-0001 was updated to name real candidates (commit cca6b29). Separately, the script has no knowledge of EBR-0001 Section 5A's grouped active-backlog view (added after this script was built) at all - its `read_high_priority_backlog()` scans only Section 5's main table and requires exactly 7 pipe-delimited cells, so Section 5A's 3-column theme rows are silently skipped even if encountered, not mis-parsed. Together these two gaps mean the launcher currently omits the two things most directly relevant to its own stated purpose - informing WP0B objective selection - undermining the tool's practical value for exactly the task it exists to support, per the Programme Sponsor's own direct observation. Proposed scope, not authorised for implementation by this entry: extend `CurrentState`/`read_current_state()` to also extract PST-0001's "Next Work Package Candidate" row; add a new reader for EBR-0001 Section 5A's theme tables (or, if EBG-0106 is implemented first, generate directly from Section 5's Status/Priority fields rather than reading Section 5A at all, since Section 5A is itself only a stopgap manual snapshot pending that automation); update `build_report()` to include both. A future EIP should scope whether this fully or partially supersedes EBG-0106's own generation-mechanism scope, since both concern surfacing the same underlying active-backlog information through the same script. No implementation authorised by this entry. |

---

# 5A. Active Backlog View - Manual Snapshot (21 July 2026)

**This section is a dated, manually-produced snapshot, not a live or automatically-regenerated view.** It exists to satisfy EBG-0106's own recommendation ahead of that item's actual automation being built, and to avoid the exact risk EBG-0106 itself names: a hand-maintained second source of truth that silently drifts from the table above. **This snapshot shall not be manually updated in place** - if it goes stale, either regenerate it fresh (dating the regeneration) or remove it once EBG-0106's own generation mechanism exists. It covers every row in Section 5 above with Status `Candidate Backlog` or `Approved Backlog` as of this date (57 items) - Complete/Closed/Superseded/Deferred/Adopted rows are intentionally excluded, since those are exactly the historical-ledger content this view is meant to filter out.

## Theme 1 - Deployment & Release Engineering

Both external read-only reviews (21 July 2026) and both AI reviewers independently converge on this as the highest-value near-term cluster.

| ID | Priority | Item |
|---|---|---|
| EBG-0102 | Critical | Guardian Desktop Distribution Foundation (sidecar packaging, Tauri bundling, clean-machine install) |
| EBG-0103 | High | CI does not build/test/gate the complete desktop product |
| EBG-0104 | High | No release automation / cross-file version sync |

## Theme 2 - Product Data Trust (Personal Memory Era)

| ID | Priority | Item |
|---|---|---|
| EBG-0023 | Medium | Backup, Recovery and Data Protection - its own stated trigger ("before persistent memory becomes significant") is now met by EBG-0080 |
| EBG-0100 | Medium | UXP Memory row hardcoded, never live-data-backed |

## Theme 3 - Guardian Action / Local Agent Boundary

| ID | Priority | Item |
|---|---|---|
| EBG-0021 | High | Local Agent Permission Boundary |
| EBG-0042 | High | Agent Framework Architecture |

## Theme 4 - UXP/Backend Technical Debt and Performance

| ID | Priority | Item |
|---|---|---|
| EBG-0081 | Medium | Shared animation scheduler (Question 1 remaining - Q2 already delivered) |
| EBG-0098 | Medium | Version badge/table drift detection |
| EBG-0105 | Low | Transcript export filename collision |

## Theme 5 - Security Hygiene

All five items originate from the same external security review, 19 July 2026.

| ID | Priority | Item |
|---|---|---|
| EBG-0085 | Medium | esbuild/vite dev-server vulnerability |
| EBG-0086 | Medium | `.aiems-exchange/` filesystem permission enforcement |
| EBG-0087 | Low | GitHub Actions pinned by mutable tag, not commit SHA |
| EBG-0088 | Low | No explicit `.env` entry in `.gitignore` |
| EBG-0089 | Low | Tauri CSP `style-src 'unsafe-inline'` |

## Theme 6 - AIEMS Process and Approval-Workflow Tooling

| ID | Priority | Item |
|---|---|---|
| EBG-0096 | Medium | Automate Claude<->Codex review handoff |
| EBG-0092 | Medium | Formalise WP-independence/scope-creep discipline into PBK-0001 |
| EBG-0106 | Medium | EBR-0001 active-backlog view (this section's own future automation) |
| EBG-0101 | Medium | `bump_version.py` `--date` stale default |
| EBG-0090 | Low | CLI-driven coding agents investigation |
| EBG-0091 | Low | Streamline daily Sponsor Approval command |
| EBG-0093 | Low | PC-side installer script for the Sponsor Approval Service |
| EBG-0094 | Low | Self-service Sponsor Approval Service status check |
| EBG-0095 | Low | Auto-start the Sponsor Approval Service on Windows login |

## Theme 7 - Dormant AIEMS Governance/Standards Debt

The notable finding of this snapshot: 23 items, almost all `Approved Backlog`, `Medium`/`High` priority, sitting untouched since ESR-0001 through ESR-0006 - some for 25+ sessions. Mostly proposed standards/registers approved-in-principle early in the project and never revisited because product/process work always outranked them. This cluster is itself just over 40% of the entire open backlog, competing for the same Priority field as active product blockers like EBG-0102.

| ID | Priority | Item |
|---|---|---|
| EBG-0006 | Medium | REV-0002 Repository Baseline Verification |
| EBG-0008 | Medium | Create Engineering Implementation Package Standard |
| EBG-0009 | Medium | Create Engineering Session Standard |
| EBG-0010 | Medium | Define repository metadata and cross-reference validation rules |
| EBG-0011 | Medium | Create AI Roles and Capability Matrix |
| EBG-0013 | Medium | Create Engineering Decision Index |
| EBG-0014 | Medium | Assess repository validation automation |
| EBG-0032 | Medium | Historical Engineering Register |
| EBG-0033 | Medium | AIEMS Improvement Register / AIEMS Improvement Review |
| EBG-0034 | High | Engineering Authority by Work Package |
| EBG-0035 | High | Context Activation Guidance |
| EBG-0036 | High | WP6 Repository Content Verification Standard |
| EBG-0037 | Medium | Engineering Package Classifications: EIP / EAP / ECP |
| EBG-0038 | High | Formal AIEMS Standards Review |
| EBG-0040 | Medium | AIEMS Repository Integrity Troubleshooting Playbook |
| EBG-0043 | High | Engineering Ecosystem Synchronisation Workflow |
| EBG-0044 | Medium | Obsidian / OSE Validation Workflow |
| EBG-0061 | Medium | Engineering Workspace Standard (EWS-0001) |
| EBG-0062 | Low | CPS-0001 Codex Prompt Standard |
| EBG-0063 | Medium | TMP-0002 Engineering Session Chat Summary Template |
| EBG-0064 | Medium | ISTL-0001 Engineering Inter-Session Task Register |
| EBG-0066 | Medium | AIEMS Maturity Model / Index |
| EBG-0067 | Medium | Dropped ADR-0007 Topics: AIEMS Knowledge Architecture remaining sub-topic |

## Theme 8 - Deferred Product Research

Mostly intentionally parked pending prerequisites (identity/authentication, mature action controls, etc.) rather than simply neglected.

| ID | Priority | Item |
|---|---|---|
| EBG-0065 | High | STD-0006 Configuration and Secrets Standard - arguably closer to Theme 1 now real credentials/local databases exist |
| EBG-0059 | High | Engineering Assurance Capability (EAC) Implementation |
| EBG-0046 | High | Device Independence and Restore Architecture |
| EBG-0047 | High | Sentinel Gate of Durin Architecture Specification |
| EBG-0015 | Medium | Investigate JARVIS Human-AI Interaction Memory and Behavioural Intelligence Layer - possibly superseded in part by EBG-0080's real Personal Memory, not re-assessed by this snapshot |
| EBG-0022 | Medium | JARVIS AIEMS Knowledge Capability |
| EBG-0025 | Medium | JARVIS Home Assistant and Smart Home Integration Assessment |
| EBG-0029 | Medium | Product Growth Philosophy |
| EBG-0052 | Medium | PBK-0001/EE-0001 "Execute After Approval" Principle |
| EBG-0054 | Low | Dev-Environment Setup Automation Expansion |

---

# 6. Candidate Backlog Intake

Backlog items discovered from historical chat sessions must be marked **Candidate Backlog** until reviewed and accepted by the Programme Sponsor.

Candidate backlog items shall not be treated as approved engineering scope.

Candidate items may be reviewed and assigned one of the following outcomes:

- Accepted
- Merged
- Superseded
- Rejected
- Deferred

Accepted candidate items may be added to the approved Backlog Register with a unique backlog identifier.

Merged, superseded, rejected or deferred candidate items shall retain sufficient notes to preserve review traceability.

---

# 7. Prioritisation Rules

Backlog priority shall be assigned by the Programme Sponsor or through an approved engineering review.

Priority values are:

- Critical
- High
- Medium
- Low

Priority shall consider repository integrity, governance consistency, engineering risk, implementation dependency and programme value.

Priority does not authorise implementation.

---

# 8. Status Definitions

| Status | Definition |
|--------|------------|
| Candidate Backlog | Identified item awaiting Programme Sponsor review and acceptance. |
| Approved Backlog | Accepted backlog item that may be progressed through a future approved Engineering Implementation Package. |
| Planned | Approved backlog item selected for future planning but not yet authorised for implementation. |
| In Progress | Backlog item currently being implemented under an approved Engineering Implementation Package. |
| Completed | Backlog item implemented and accepted into the repository baseline. |
| Merged | Candidate or backlog item consolidated into another backlog item. |
| Superseded | Item replaced by a newer or more appropriate backlog item or artefact. |
| Rejected | Item reviewed and declined for inclusion or implementation. |
| Deferred | Item intentionally retained for later consideration without current implementation commitment. |

---

# 9. Relationship to AIEMS

EBR-0001 supports AIEMS by maintaining a controlled view of future engineering work without expanding current implementation scope.

It complements:

- Engineering Session Reports, which record completed sessions and deferred work.
- Programme Status, which records current programme state.
- Engineering Implementation Packages, which define approved implementation scope.
- Controlled Artefact Registers, which record governed artefacts.
- Engineering Reviews, which may identify backlog candidates or approve backlog progression.

Where conflict exists, approved Engineering Implementation Packages define current implementation scope.

---

# 10. OSE Relationships

| Artefact | OSE Relationship |
|----------|------------------|
| [[OSE-0001_ORGANIC_SEMANTIC_ENHANCEMENT_UPDATE_RULE|OSE-0001]] | Defines the retrospective relationship-only enrichment rule applied to this backlog register. |
| [[ADR-0013_ENGINEERING_ECOSYSTEM_SYNCHRONISATION|ADR-0013]] | Establishes Engineering Ecosystem Synchronisation and the repository-compatible OSE context for backlog traceability. |
| [[REG-0001_CONTROLLED_ARTEFACT_REGISTER|REG-0001]] | Records authoritative artefact identity, ownership, status and current version for controlled backlog-related artefacts. |
| [[REG-0004_ACTION_REGISTER|REG-0004]] | Records governed actions that may become, complete or relate to backlog work. |
| [[PVTM-0001_PRODUCT_VISION_TRACEABILITY_MODEL|PVTM-0001]] | Provides product vision traceability context for backlog candidates and deferred work. |
| [[PST-0001_PROGRAMME_STATUS|PST-0001]] | Records current programme status and backlog review context. |
| [[RBL-0009_REPOSITORY_BASELINE|RBL-0009]] | Current accepted repository baseline supporting this retrospective OSE pass. |

---
# 11. Related Artefacts

| Artefact | Relationship |
|----------|--------------|
| [[RBL-0007_REPOSITORY_BASELINE|RBL-0007]] | Source baseline for ESR-0006 repository readiness classification and new backlog intake. |
| [[RBL-0006_REPOSITORY_BASELINE|RBL-0006]] | Previous baseline that provided the starting point for ESR-0006. |
| [[REG-0001_CONTROLLED_ARTEFACT_REGISTER|REG-0001]] | Registers controlled artefacts whose changes may create backlog follow-up. |
| [[PST-0001_PROGRAMME_STATUS|PST-0001]] | Programme status context for session transition and future backlog review. |
| [[ESR-0005_ENGINEERING_SESSION_REPORT|ESR-0005]] | Source of the prototype JARVIS chat export artefacts classified during ESR-0006. |

---

# 12. Maintenance

The Engineering Backlog Register shall be reviewed:

- At engineering session transition.
- When deferred work is identified during session closure.
- When candidate backlog items are submitted for Programme Sponsor review.
- When backlog items are accepted, merged, superseded, rejected, deferred or completed.
- When the Programme Sponsor directs a backlog review.

Updates to this register shall preserve unique backlog identifiers and maintain traceability to source artefacts where known.

---

# 13. Version History

| Version | Date | Author | Summary |
|---------|------|--------|---------|
| 1.122 | 24 July 2026 | Claude Engineering Implementer | ESR-0032 WP2 Complete: EBG-0103 (CI Build Gate Hardening) delivered per EIP-ESR0032-002 v1.0 - Rust CI job, narrowed continue-on-error, real Playwright suite. Real Linux verification via WSL Ubuntu found and fixed three genuine bugs (schemars/indexmap, build_backend_sidecar.py Windows-only, missing PNG icon) before writing the CI job. |
| 1.121 | 24 July 2026 | Claude Engineering Implementer | Fix Codex-caught Low finding on commit 293780a: EBG-0102's entry cited EIP-ESR0032-001 v0.2 (a stale pre-fix-round reference) instead of the actual implemented v1.0. |
| 1.120 | 24 July 2026 | Claude Engineering Implementer | ESR-0032 WP1: EBG-0102 (Guardian Desktop Distribution Foundation) marked Complete per EIP-ESR0032-001 v1.0 - PyInstaller sidecar packaging, tauri-plugin-shell wiring, Tauri bundling, real installer build/run, all live-verified with zero regression to dev mode. |
| 1.119 | 21 July 2026 | Claude Engineering Implementer | Registered EBG-0107 (session_launcher.py's CurrentState omits Next Work Package Candidate and EBR-0001 Section 5A entirely, High) - discovered live when the Programme Sponsor ran the launcher twice expecting to see the Theme 1/Theme 7 candidates just registered, and it showed neither. Confirmed by direct code inspection, not a caching/overwrite defect: CurrentState has exactly two fields (current_mode, current_baseline), never Next Work Package Candidate; the script predates Section 5A entirely. Deliberately not added to Section 5A's own theme tables, per that section's own explicit no-manual-update rule. |
| 1.118 | 21 July 2026 | Claude Engineering Implementer | Post-ESR-0031-closure: added Section 5A, Active Backlog View - Manual Snapshot (21 July 2026), a dated one-time grouping of all 57 open Candidate/Approved Backlog items into 8 themes (Deployment/Release, Product Data Trust, Guardian Action/Agent Boundary, UXP Technical Debt, Security Hygiene, AIEMS Process Tooling, Dormant AIEMS Governance/Standards Debt, Deferred Product Research), following a Programme Sponsor request to see the backlog grouped WP-shape. Explicitly marked as a non-authoritative, non-live snapshot per EBG-0106's own guard against a hand-maintained second source of truth - not to be manually updated in place. Notable finding surfaced: the dormant AIEMS governance/standards cluster (Theme 7, 23 items dating to ESR-0001-0006) is just over 40% of the entire open backlog. |
| 1.117 | 21 July 2026 | Claude Engineering Implementer | Post-ESR-0031-closure: registered EBG-0102 through EBG-0106, all Candidate Backlog, no implementation authorised, following two independent external read-only repository reviews (21 July 2026, deployment readiness + backlog gap analyses), each claim independently verified against the live repository by Claude Engineering Implementer and separately by Codex Engineering Reviewer before registration. EBG-0102 (Guardian Desktop Distribution Foundation, Critical) promotes EBG-0050's remaining sidecar-packaging scope to a first-class item - EBG-0050's own text updated to reflect this and that streaming notifications closed at ESR-0031 WP2 (EBG-0099). EBG-0103 (CI does not build/gate the complete desktop product, High), EBG-0104 (no automated release workflow or cross-file version sync, High, distinct from EBG-0098's narrower single-document scope), EBG-0105 (transcript export filename collision, Low), EBG-0106 (EBR-0001 mixes historical ledger/product backlog/process register with no active-development view, Medium). |
| 1.116 | 20 July 2026 | Claude Engineering Implementer | ESR-0031 WP4 post-implementation fix round: registered EBG-0101 (bump_version.py's --date flag silently defaults to a stale hardcoded placeholder, 9 July 2026) - discovered live when three real bump calls this same fix round were run without --date and silently mis-dated. Candidate Backlog, Medium. No implementation authorised. |
| 1.115 | 20 July 2026 | Claude Engineering Implementer | ESR-0031 WP2 fix round (Codex Low finding on commit 337761777c38b7635ee3439b210840139e84ae7a): EBG-0099's row incorrectly described a real-subprocess smoke test as part of the committed test suite's '4 new tests' - it was actually a separate, ad hoc manual verification script, never added to jarvis/tests/test_stdio_rpc.py. Corrected to distinguish the 4 real in-process unit tests from the manual real-subprocess verification. |
| 1.114 | 20 July 2026 | Claude Engineering Implementer | ESR-0031 WP2: registered EBG-0100 (UXP Capability Sidebar Memory Row is Hardcoded, Never Live-Data-Backed, Candidate Backlog, Medium) - discovered during WP2's live smoke check screenshot. No implementation authorised. |
| 1.113 | 20 July 2026 | Claude Engineering Implementer | ESR-0031 WP2 delivered: EBG-0099 (Streaming Notifications MVP) marked Complete per EIP-ESR0031-002 v1.0. Python heartbeat emitter, Rust reader-thread restructuring, React notification listener all implemented and live-smoke-tested via a real npm run tauri dev session (two screenshots proving live update). 351 tests total (was 347). |
| 1.112 | 20 July 2026 | Claude Engineering Implementer | ESR-0031 WP2B: registered EBG-0099 (Streaming Notifications MVP, EBG-0050 remaining scope, Approved Backlog, High) - draft EIP-ESR0031-002 v0.1 registered, not yet reviewed or approved. Selected after Shared Family Memory was ruled out as premature (no household identity/authentication system exists per GAM-0001 Section 10). |
| 1.111 | 20 July 2026 | Claude Engineering Implementer | ESR-0031 WP1 delivered: EBG-0097 (AIEMS Session-Opening Launcher) marked Complete per EIP-ESR0031-001 v1.0. scripts/session_launcher.py implemented exactly as scoped, with two genuine defects found and fixed during the required live smoke check and test suite (WikiLink-pipe table-parsing corruption silently dropping valid backlog rows; header-row false-positive in the no-rows-found error case). 9 new tests (347 total, was 338). |
| 1.110 | 20 July 2026 | Claude Engineering Reviewer | ESR-0031 WP0B: registered EBG-0097 (AIEMS Session-Opening Launcher, Approved Backlog, High - draft EIP-ESR0031-001 v0.1 registered, not yet reviewed or approved) and EBG-0098 (Version Badge/Table Drift Detection in validate_repository.py, Candidate Backlog, Medium - discovered during this session's own WP0 version-drift bug, no implementation authorised). |
| 1.109 | 20 July 2026 | Claude Engineering Implementer | Post-ESR-0030-closure: EBG-0096 refined - traced the recurring sandbox helper error to the Programme Sponsor's deliberate `elevated` Windows sandbox setting (introduced to stop Codex misreading pasted EIP text as authorisation to push/commit), not a bug; redesigned the automated review invocation to run Codex in read-only sandbox mode (zero write capability by construction) with Claude relaying its verbatim findings into the bridge under explicit Sponsor approval - tested live and confirmed clean (Pass, no sandbox error) for POST-ESR0030/WP1 commit f8c42b5. |
| 1.108 | 20 July 2026 | Claude Engineering Implementer | Post-ESR-0030-closure: EBG-0096 updated with a full live end-to-end test result - a real `codex exec -s workspace-write` invocation (not just a text-only probe) failed entirely on first attempt (all Python invocations blocked by a Windows sandbox helper error, Codex correctly refused to fabricate a response when blocked) but succeeded in full after the Programme Sponsor adjusted a local sandbox setting: Codex read its real inbox, verified real repository state, ran validate_repository.py itself, and successfully called return-findings - a genuine unattended round trip. One narrower limitation remained: pytest specifically still failed post-fix while other Python invocations succeeded. A cold-start cost (models-cache load failure on every invocation) was also observed and noted, consistent with the Programme Sponsor's own observation of slower first runs. |
| 1.107 | 20 July 2026 | Claude Engineering Implementer | Post-ESR-0030-closure: registered EBG-0096 (Candidate Backlog) - automate the Claude<->Codex review handoff via non-interactive CLI invocation (`codex exec`, `claude -p --no-session-persistence`), following a live bidirectional feasibility test in conversation confirming both directions work with no session conflict, and disclosing a sandbox-escalation nuance found on Codex's own side. No implementation authorised - a future EIP must scope initiation model, audit-trail preservation, and sandbox/permission mode for each side. |
| 1.106 | 20 July 2026 | Claude Engineering Implementer | Post-ESR-0030-closure: registered EBG-0091 through EBG-0095 (all Candidate Backlog, no implementation authorised), following a Programme Sponsor review of the whole approval-workflow brainstorming thread: EBG-0091 (drop the explicit session identifier from daily approvals), EBG-0092 (formalise the already-adopted WP-independence/scope-creep-flagging discipline into PBK-0001), EBG-0093 (PC-side installer script for the Sponsor Approval Service, mirroring EBG-0078), EBG-0094 (self-service status check, Claude's own suggestion), EBG-0095 (auto-start the service on Windows login, Claude's own suggestion). Two other ideas from the same thread (an approval queue with agent-write capability; reusing the approval-separation pattern for Guardian's future Action faculty) were explicitly reviewed and dropped by the Programme Sponsor, not registered. |
| 1.105 | 20 July 2026 | Claude Engineering Implementer | Post-ESR-0030-closure: registered EBG-0090 (Candidate Backlog) - investigate CLI-driven coding agents (Claude Code/Codex CLI) as a cost-avoidance route for Guardian research/engineering tasks specifically, using existing subscription billing instead of metered API cost, following a Programme Sponsor discussion. Explicitly scoped as investigation only, narrow to research-type tasks (not a conversational-backend replacement), conditional on no performance regression versus the current direct-API route - the Programme Sponsor's own explicit condition. |
| 1.104 | 19 July 2026 | Claude Engineering Implementer | ESR-0030 WP2: EBG-0084 marked Complete in full - the Programme Sponsor deployed the Sponsor Approval Service behind a real Tailscale address, holding AIEMS_SPONSOR_TOKEN exclusively on a genuinely separate second device (Android/Termux), satisfying EIP-ESR0030-001 Section 9 item 4. Live end-to-end confirmed with real separation: a Sponsor-originated approval from the separate device was independently fetched and accepted by submit-response with zero Engineering Implementer involvement in the approval action - exceeding the Section 9 item 4 bar. |
| 1.103 | 19 July 2026 | Claude Engineering Implementer | ESR-0030 WP1: EBG-0084 updated from Approved Backlog to "Code Complete, Tailscale deployment pending Programme Sponsor acceptance" - the Sponsor Approval Service's code half delivered per EIP-ESR0030-001 v1.0 (stdlib-only HTTP service, sponsor_client.py, aiems_bridge.py diff), live-verified against a real running instance on 127.0.0.1. Full closure remains pending the Sponsor's own Tailscale deployment (EIP Section 9 item 4), not claimed here. |
| 1.102 | 19 July 2026 | Claude Engineering Implementer | Registered five findings from an external read-only security review (post-ESR-0029 closure, no files touched by the review itself) as EBG-0085 through EBG-0089: esbuild/vite dev-server vulnerability (Medium), `.aiems-exchange/` filesystem permission enforcement currently convention-only (Medium), GitHub Actions pinned by mutable tag not SHA (Low), no explicit `.env` entry in `.gitignore` (Low), Tauri CSP `style-src 'unsafe-inline'` (Low). All Candidate Backlog - no implementation authorised by this registration; the review's own positive findings (no hardcoded secrets, parameterized queries throughout, no eval/exec/pickle/unsafe yaml.load, no listening network service, path-traversal defended, no raw exception leakage, dev server already bound to 127.0.0.1) were not registered as backlog items since they describe already-correct state, not open work. |
| 1.101 | 19 July 2026 | Claude Engineering Implementer | ESR-0029 WP7 implemented: EBG-0083 Phase 1d (Local engineering-environment state) delivered per EIP-ESR0029-005 v1.0, closing Phase 1 entirely - additive extension of WP3/WP4/WP6's GiaSnapshot/LocalResourceObserver/gia.status with presence detection for VS Code/Obsidian/GitHub Desktop/ChatGPT Desktop, real process names verified via tasklist, no new dependency, 295 tests total, live-verified against both running and not-running states. Only Phases 2-4 remain not delivered for EBG-0083. |
| 1.100 | 19 July 2026 | Claude Engineering Implementer | ESR-0029 WP6 implemented: EBG-0083 Phase 1c (Process health, self-observation only) delivered per EIP-ESR0029-004 v1.0 - additive extension of WP3/WP4's GiaSnapshot/LocalResourceObserver/gia.status with the JARVIS backend's own process status/uptime/CPU/memory, no new dependency, 294 tests total, live-verified. Only Local engineering-environment state and Phases 2-4 remain not delivered for EBG-0083. |
| 1.99 | 19 July 2026 | Claude Engineering Implementer | ESR-0029 WP5: ADR-0022 approved by the Programme Sponsor (1.0 to 1.1, Draft to Approved) - Sponsor Approval Service architecture decision. EBG-0084 updated: decision half Complete, implementation half (the service, sponsor_client.py, aiems_bridge.py diff) remains not authorised, future EIP required. |
| 1.98 | 19 July 2026 | Claude Engineering Implementer | ESR-0029 WP5: registered EBG-0084 (Sponsor Approval Service, Approved Backlog) - Programme Sponsor proposal to replace file-based sponsor-decision with a remote, agent-read-only approval service, following Claude's review confirming the self-approval gap is real and that submit-response has never actually been used as the real gate in practice this session. Decision-only, drafted as ADR-0022. |
| 1.97 | 19 July 2026 | Claude Engineering Implementer | ESR-0029 WP4: addressed an Engineering Reviewer finding - EBG-0083's original WP3-era "Scope of this item" paragraph still said Storage state was deferred, directly contradicting the newer WP4 delivery note in the same row; corrected to distinguish the original WP3 scope from Storage state's actual WP4 delivery. |
| 1.96 | 19 July 2026 | Claude Engineering Implementer | ESR-0029 WP4 implemented: EBG-0083 Phase 1b (Storage state) delivered per EIP-ESR0029-003 v1.0 - additive extension of WP3's GiaSnapshot/LocalResourceObserver/gia.status with real disk usage, no new dependency, 294 tests total, live-verified. |
| 1.95 | 19 July 2026 | Claude Engineering Implementer | ESR-0029 WP3: addressed an Engineering Reviewer finding on the implementation - gia.status RPC tests called the real host instead of a deterministic fake, so the serialization path was unproven; fixed via a new injectable gia_observer parameter on StdioRpcServer and rewritten tests asserting exact camelCase output from a fake GiaSnapshot. EIP-ESR0029-002 1.0 to 1.1, 293 tests total. |
| 1.94 | 19 July 2026 | Claude Engineering Implementer | ESR-0029 WP3 implemented: EBG-0083 Phase 1a delivered per EIP-ESR0029-002 v1.0 - jarvis/gia/observability.py (new), gia.status RPC method, psutil dependency, 6 new tests (292 total), live-verified via the real production code path with genuine host data. Status Approved Backlog to Complete (Phase 1a). |
| 1.93 | 19 July 2026 | Claude Engineering Implementer | ESR-0029 WP3: registered EBG-0083 (Guardian Instrumentation Agent, Phase 1a Local Resource Observability), Approved Backlog. Grounded in ESR-0010/ESR-0011's authoritative GIA definition, explicitly distinguished from the unrelated `jarvis/gia/bootstrap.py` GIA-BOOT Proof of Concept (engineering-request readiness gating, not platform observability), scoped to CPU/memory state and event publication only - Storage/process-health/engineering-environment state and Phases 2-4 deferred to future items. |
| 1.92 | 19 July 2026 | Claude Engineering Implementer | ESR-0029 WP2: addressed two Engineering Reviewer (Codex) findings on the implementation review - a hit-testing lookup-order bug (fixed to derive from actual paint order, not baseNodes' arbitrary order) and a REG-0001 staleness timing artefact (reconfirmed consistent). EIP-ESR0029-001 1.1 to 1.2. |
| 1.91 | 19 July 2026 | Claude Engineering Implementer | ESR-0029 WP2: real hardware confirmation added to EBG-0081's entry - Programme Sponsor observed Windows Task Manager directly against the live Tauri app (JARVIS Guardian Shell: 1.4% CPU, 1.0% GPU, Power Usage "Low"), confirming the SVG-era sustained "Very high" power classification is resolved, complementing the CDP TaskDuration comparison already recorded. |
| 1.90 | 19 July 2026 | Claude Engineering Implementer | ESR-0029 WP2: EBG-0081 Question (2)'s Canvas 2D migration implemented per EIP-ESR0029-001, migrating GuardianOrbGraph.jsx from SVG DOM rendering to a single Canvas 2D surface plus a static glow/gradient node treatment. A genuine performance defect (clip+per-edge-stroke combination costing more than the SVG baseline it was meant to improve on) was found and fixed during this package's own live verification, disclosed in EBG-0081's updated entry. |
| 1.89 | 19 July 2026 | Claude Engineering Implementer | ESR-0029 WP1, Engineering Reviewer (Codex) Medium finding via the bridge: EBG-0082's VS Code evidence claim was factually stale - microsoft/vscode#233842 is Closed as not planned, not "open, unresolved" as originally written. Directly verified and corrected to the accurate, still-supporting claim: VS Code received and declined (closed as not planned) the same per-extension CPU-limiting request, rather than merely leaving it pending. |
| 1.88 | 19 July 2026 | Claude Engineering Implementer | ESR-0029 WP1: added EBG-0082 (Cross-Module Resource Interaction Research) recording the Programme Sponsor's broader post-ESR-0028 concern - real external research into how comparable modular/plugin architectures (VS Code Extension Host, Electron, Tauri, Home Assistant, Mycroft) manage cross-module CPU/memory interaction, mapped against JARVIS's actual current architecture. Cross-referenced against EBG-0081's still-open shared-animation-scheduler question. Research delivered, no implementation authorised. |
| 1.87 | 18 July 2026 | Claude Engineering Implementer | ESR-0028 WP5 per Sponsor-approved ADR-0021: applied a split disposition to EBG-0081 - the rendering-engine re-evaluation half is Complete (Canvas 2D decision recorded in ADR-0021, no implementation authorised), the shared-animation-scheduler half remains open Candidate Backlog. |
| 1.86 | 18 July 2026 | Claude Engineering Implementer | ESR-0028 WP4 seventh fix per Engineering Reviewer findings: added a visibilitychange listener so a hidden/backgrounded window's resume doesn't jump-forward when requestAnimationFrame was suspended during the hidden period (the idle-timeout fix's pinning approach didn't cover this case). Corrected the original WP4 delivery paragraph's now-stale description of the implementation. |
| 1.85 | 18 July 2026 | Claude Engineering Implementer | ESR-0028 WP4: added EBG-0081 (UXP Animation Performance Policy, Candidate Backlog) capturing the Programme Sponsor's forward-looking concern raised during WP4's power-usage investigation - a shared animation scheduler for future UXP work, and a Canvas-vs-SVG rendering re-evaluation for GuardianOrbGraph.jsx specifically. Also added GuardianOrbGraph.jsx's idle-timeout pause (45s of no interaction) as a further WP4 fix. |
| 1.84 | 18 July 2026 | Claude Engineering Implementer | ESR-0028 WP4 fifth fix: a fresh-restart, per-process Task Manager comparison conclusively isolated rotation as the sole remaining driver of high power usage. Decoupled edge update rate to a third of node rate (edges are 8.6x the element count) and added a Page Visibility check to skip DOM mutation when the window is hidden. |
| 1.83 | 18 July 2026 | Claude Engineering Implementer | ESR-0028 WP4 fourth fix: with an unrelated leaked find.exe process eliminated, an isolated Task Manager comparison confirmed rotation itself was still the real driver of high power usage via an uncapped requestAnimationFrame loop. Capped actual DOM-mutation work to ~12 updates/second while keeping rAF for its automatic hidden-window pause behaviour. |
| 1.82 | 18 July 2026 | Claude Engineering Implementer | ESR-0028 WP4 third fix: found the real dominant cause of persistent "Very high" power usage was unrelated to rotation - a pre-existing `guardian-breathe` infinite box-shadow-animating CSS rule on `.guardian-orb`, missed during the earlier direct-instruction decoration cleanup. Removed. |
| 1.81 | 18 July 2026 | Claude Engineering Implementer | ESR-0028 WP4 second post-implementation fix: the interval-throttling fix (1.80) was insufficient per Programme Sponsor confirmation (still high power usage, now visibly choppy). Replaced with a ref-based `requestAnimationFrame` direct-DOM-mutation architecture, bypassing React re-render for the animation hot path entirely. |
| 1.80 | 18 July 2026 | Claude Engineering Implementer | ESR-0028 WP4 post-implementation fix: reduced GuardianOrbGraph's rotation interval from 50ms to 200ms (20 to 5 ticks/second) after the Programme Sponsor observed sustained "Very high" power usage on real hardware - the disclosed-but-unmeasured performance risk in EIP-ESR0028-004's own Risk register. Same rotation speed retained, 4x render load reduction. |
| 1.79 | 18 July 2026 | Claude Engineering Implementer | ESR-0028 WP4 per Sponsor-approved EIP-ESR0028-004: delivered EBG-0055 Phase 1.5 - full-sphere coordinates, real Y-axis rotation, reduced-motion support, and a directionally-informed, empirically-reverified force retuning against the real 195-node graph (centroid 4.88px, 0 overlaps, 0 boundary breaches). |
| 1.78 | 18 July 2026 | Claude Engineering Implementer | ESR-0028 WP3 post-commit fix per Engineering Reviewer (Codex) Medium finding: EBG-0024 and EBG-0045's closure notes did not explicitly preserve the policy-defined-versus-code-not-implemented and education-credit-not-actioned distinctions that EBG-0049's closure already carried. Both notes updated to state them explicitly. |
| 1.77 | 18 July 2026 | Claude Engineering Implementer | ESR-0028 WP3 per Sponsor-approved EIP-ESR0028-003: closed EBG-0045, EBG-0049 and EBG-0024 (Complete) - PEM-001 revised to make Cost a genuine first-class criterion (10% to 15%) with an explicit no-discretionary-budget default, a new Cost-Aware Routing Policy design principle recorded for EBG-0049 while explicitly excluding dynamic routing code as future work, and EBG-0024's cost-principles ask folded into the same PEM-001 revision rather than a separate document. |
| 1.76 | 18 July 2026 | Claude Engineering Implementer | ESR-0028 WP2 per Sponsor-approved EIP-ESR0028-002 v0.4: closed EBG-0017 (Complete) - identified JARVIS_PRODUCT_ARCHITECTURE.md and the JARVIS Capability Readiness Matrix as the already-existing authoritative product requirements backlog, both now formally registered in REG-0001, the Matrix refreshed to v2.1 with two evidence-backed staleness corrections. |
| 1.75 | 18 July 2026 | Claude Engineering Implementer | ESR-0028 WP1 per Sponsor-approved [[EIP-ESR0028-001_AIEMS_PROCESS_HYGIENE_BATCH|EIP-ESR0028-001]] v0.3: created and closed EBG-0071 (REG-0001 HST/FCH Registration Gap, Complete - 10 rows registered); closed EBG-0005 (Resolved by Attrition); closed EBG-0068 (Superseded); closed EBG-0058 (Complete - PBK-0001 clause consolidation, all four candidate clusters reviewed and disposed). Every closure carries a disclosed rationale per the EIP's own Constraint 2. |
| 1.74 | 18 July 2026 | Claude Engineering Implementer | ESR-0027 WP2 fully closed - Engineering Reviewer (Codex) post-implementation review via the bridge: Pass, no blocking findings, every claim in the committed diff independently verified. No fix cycle needed. |
| 1.73 | 18 July 2026 | Claude Engineering Implementer | ESR-0027 WP2: closed EBG-0077 (UXP Static Placeholder Row Reconciliation) as Complete per EIP-ESR0027-002 v0.2 - `first-light` removed from DiagnosticsPanel, `boundary`/`shell`/`agents` kept unchanged as honestly-labelled, roadmap-grounded diagnostics. Verified live, `npm run build` clean. |
| 1.72 | 18 July 2026 | Claude Engineering Implementer | ESR-0027 WP1: Engineering Reviewer final confirmation (Codex, via the bridge, against fix commit dbd8424) - Pass, both Medium findings resolved, no new blocking findings, one non-blocking advisory recorded. WP1 (EBG-0080) fully closed end-to-end. |
| 1.71 | 18 July 2026 | Claude Engineering Implementer | ESR-0027 WP1: recorded post-commit review findings and fixes on EBG-0080's note - two Medium findings (runtime-state boundary check, storage-layer consent-traceability enforcement) found by the Engineering Reviewer against the committed diff, both fixed. 286 tests total (was 282). |
| 1.70 | 18 July 2026 | Claude Engineering Implementer | ESR-0027 WP1: closed EBG-0080 (Personal Memory Implementation) as Complete per EIP-ESR0027-001 v0.3 - `jarvis/memory/` implements the Personal Memory tier with a working minimal consent gate, the first Sentinel REVIEW decision in this codebase ever actually resolved by a human. 282 tests total (was 254), validator clean. A genuine SQLite connection-leak defect found and fixed during the package's own live smoke check. |
| 1.69 | 18 July 2026 | Claude Engineering Implementer | ESR-0027 WP0B: added EBG-0080 (Personal Memory Implementation), Approved Backlog, High priority, following the Programme Sponsor's selection of MDS-0001's anticipated first follow-on. A scope check during EIP-ESR0027-001 drafting found GAM-0001's own evidence check is accurate - no approval workflow exists in code today for any REVIEW-classified action - and the Programme Sponsor chose to expand scope to build the minimal mechanism rather than defer or descope. EIP-ESR0027-001 v0.1 drafted, not yet reviewed or approved; no implementation authorised by this entry. |
| 1.68 | 17 July 2026 | Claude Engineering Implementer | ESR-0026 WP3 fully closed - Codex's post-commit review confirmed the committed ADR-0020 content matches what was reviewed and approved, and that EBR-0001/REG-0001/REG-0002 entries are accurate. No blocking findings. All three of ESR-0026's Work Packages are now fully closed end-to-end, no manual relay throughout. |
| 1.67 | 17 July 2026 | Claude Engineering Implementer | ESR-0026 WP3: closed EBG-0076 (Network Exposure Security Hardening) as Complete per [[ADR-0020_SENTINEL_NETWORK_EXPOSURE_SECURITY_REQUIREMENTS|ADR-0020]] v1.0 (Approved) - a binding three-part security gate (authentication, rate limiting, TLS) any future network-facing Guardian/Sentinel interface must satisfy before approval. Engineering Reviewer (Codex) reviewed via the bridge, no blocking findings, Programme Sponsor approved. A decision record, not an implementation - no network interface exists or is built by this item. |
| 1.66 | 17 July 2026 | Claude Engineering Implementer | ESR-0026 WP2 fully closed - Codex's post-commit review confirmed the committed MDS-0001 content matches what was reviewed and approved, and that EBR-0001/REG-0001 entries are accurate. No blocking findings, no manual relay. |
| 1.65 | 17 July 2026 | Claude Engineering Implementer | ESR-0026 WP2: closed EBG-0019 (JARVIS Memory and Data Storage Architecture) as Complete per [[MDS-0001_MEMORY_AND_DATA_STORAGE_ARCHITECTURE|MDS-0001]] v1.0 (Approved) - Engineering Reviewer (Codex) reviewed via the bridge, no blocking findings, Programme Sponsor approved. A specification resolving the architecture boundary GAM-0001 Section 9.2 reserved for it; no code implementation. |
| 1.64 | 17 July 2026 | Claude Engineering Implementer | ESR-0026 WP1 fully closed - Codex's final confirmation via the bridge: no blocking findings, the AttributeError fix closes the previous finding. First Work Package in this project to complete the entire real cycle (draft, review, approval, implementation, commit, post-commit review, fix, final confirmation) via genuine Claude<->Codex exchange, no manual relay. |
| 1.63 | 17 July 2026 | Claude Engineering Implementer | ESR-0026 WP1 post-implementation review (Codex, via the now-fixed bridge) found one genuine finding on EBG-0075's real diff: `sentinel/ollama_provider.py`'s `execute()` could raise `AttributeError` instead of `RuntimeError` on valid-but-non-object JSON. Fixed at [[EIP-ESR0025-002_OLLAMA_LOCAL_FALLBACK_PROVIDER|EIP-ESR0025-002]] v1.1, 4 new tests, 254 total (was 250). |
| 1.62 | 17 July 2026 | Claude Engineering Implementer | Programme Sponsor-authorised deviation (same pattern as ESR-0025A): added EBG-0079, Completed - fixed `validate_repository.py`'s `.aiems-exchange/` scan-exclusion gap, discovered when the Engineering Reviewer appeared to hang 12+ minutes on a post-implementation review request bloated by a runaway evidence-recapture feedback loop (104 to 425 to 1279 warnings). One regression test added, 250 tests total (was 249). |
| 1.61 | 17 July 2026 | Claude Engineering Implementer | ESR-0026 WP1: closed EBG-0075 (Implement Ollama Local Fallback Provider) as Complete per [[EIP-ESR0025-002_OLLAMA_LOCAL_FALLBACK_PROVIDER|EIP-ESR0025-002]] v1.0 - Engineering Reviewer (Codex) gave the first genuine, real bridge review (no blocking findings), Programme Sponsor approved via `sponsor-decision`. `sentinel/ollama_provider.py` implemented exactly as scoped; a genuine test-isolation defect found and fixed (Ollama's lack of a credential gate meant a shared test helper was making real, non-deterministic network calls); a live smoke check disclosed the EIP's own 90s timeout recommendation actually timed out once, only succeeding at 180s - implemented as approved regardless, flagged as a future tuning candidate. 249 tests total (was 238). |
| 1.60 | 17 July 2026 | Claude Engineering Implementer | Post-closure update to EBG-0057's completion note (see [[ESR-0025A_POST_CLOSURE_ENGINEERING_ADDENDUM|ESR-0025A]]): `claude`/`codex` CLIs installed and authenticated for the first time, surfacing a genuine Windows `subprocess.run`/`.CMD`-shim bug in `run_preflight()`, fixed and live-verified end-to-end across all five bridge commands. Programme Sponsor explicitly authorised implementing this fix directly, without a prior EIP draft or Engineering Reviewer review - disclosed as a deliberate, one-off deviation from the standard Working Report Lifecycle, not a new default. 238 tests total (was 237). |
| 1.59 | 17 July 2026 | Claude Engineering Implementer | Added EBG-0078 (JARVIS/Guardian Desktop Launcher, `scripts/start-jarvis.bat`), Completed, Low priority - recorded per Engineering Reviewer (Codex) finding that the launcher was an ungoverned untracked file with no governance record, mirroring the EBG-0054 precedent of recording already-delivered dev tooling rather than folding it into an unrelated EIP (EIP-ESR0025-002 covers Ollama only). Also split a malformed `1.57` version-history row (two unrelated entries had been merged into one table row with a stray `\|`, flagged Medium by Codex) into proper `1.57`/`1.56` rows. |
| 1.58 | 17 July 2026 | Claude Engineering Implementer | Added EBG-0076 (Network Exposure Security Hardening - authentication, rate limiting, TLS), Approved Backlog, High priority, following the Programme Sponsor's direct question at ESR-0025 ("how robust is Sentinel - can it be exposed to the internet?") and explicit "sooner rather than later" direction; confirmed no network-facing interface exists today (stdio JSON-RPC only) and that TrustTierPolicy/GAM-0001 is a behavioural boundary, not a network security boundary. Added EBG-0077 (UXP Static Placeholder Row Reconciliation Against Reference Mock-up), Approved Backlog, Medium priority, extending EBG-0073's explicitly-left-open question, tied to preparing the UXP ahead of the planned Guardian self-awareness work. Neither item authorises implementation. |
| 1.57 | 17 July 2026 | Claude Engineering Implementer | Added EBG-0075 (Implement Ollama Local Fallback Provider), Approved Backlog, Medium priority. PEM-001's Decision Outcome named Ollama as the local fallback path (ESR-0015 WP3a) but this was never separately backlogged for implementation. Programme Sponsor confirmed the direction at ESR-0025 (fallback, not complexity-based routing); Ollama confirmed installed, running and correctly migrated to `I:\Ollama Models` via a real live `/api/generate` call. |
| 1.56 | 17 July 2026 | Claude Engineering Implementer | Updated EBG-0057's completion note: a second Engineering Reviewer (Codex) post-implementation review found one Medium (TOCTOU) finding - `submit-response`'s authorisation/drift checks and evidence capture ran before the Work Package lock was acquired, only locking for the final write - addressed at [[EIP-ESR0025-001_AIEMS_EXCHANGE_BRIDGE_MVP|EIP-ESR0025-001]] v1.2 by moving the entire sequence inside the lock. 237 tests total (was 236). |
| 1.55 | 17 July 2026 | Claude Engineering Implementer | Updated EBG-0057's completion note: Engineering Reviewer (Codex) post-implementation review of the EBG-0057 diff found one High (path-traversal via unsanitised `session`/`work_package`) and one Medium (`submit-to-review`/`submit-response` not checking `pytest`/`validate_repository.py` exit codes) finding, both addressed at [[EIP-ESR0025-001_AIEMS_EXCHANGE_BRIDGE_MVP|EIP-ESR0025-001]] v1.1. 236 tests total (was 227). |
| 1.54 | 17 July 2026 | Claude Engineering Implementer | ESR-0025 WP1 (Engineering Reviewer/Codex reviewed v0.2 after one High and one Medium finding addressed, Programme Sponsor approved): closed EBG-0057 (Claude<->Codex Engineering Bridge) as Complete - `scripts/aiems_bridge.py` MVP implemented per [[EIP-ESR0025-001_AIEMS_EXCHANGE_BRIDGE_MVP|EIP-ESR0025-001]] v1.0, role-locking enforced as a real code gate (not only documented convention), 15 new tests (227 total). |
| 1.53 | 17 July 2026 | Claude Engineering Implementer | Resolved the JRM-0001-flagged EBG-0060/EBG-0057 overlap post-ESR-0024 closure (Programme Sponsor-approved, following the Engineering Implementer's analysis): EBG-0060's DCE component (single-AI direct execution) marked Superseded by EBG-0057's two-AI handover architecture, citing ESR-0023's write-boundary incidents as direct empirical evidence against it; EBG-0060's REA component folded into EBG-0057's future-phase scope rather than retained as a competing item. Also recorded the Programme Sponsor's decision that EBG-0057 runs as a separate AIEMS/engineering-tooling workstream, outside JRM-0001's JARVIS/Guardian product tracks. |
| 1.52 | 17 July 2026 | Claude Engineering Implementer | ESR-0024 WP1 (Engineering Reviewer/Codex reviewed v0.2 with two Medium findings addressed, Programme Sponsor approved): closed EBG-0074 (Wire TrustTierPolicy as SentinelCore's Production Default) as Complete per [[EIP-ESR0024-001_TRUSTTIERPOLICY_PRODUCTION_WIRING|EIP-ESR0024-001]] v1.0 - `build_default_runtime()` now wires `TrustTierPolicy`, closing the operational gap EBG-0041's validation surfaced at ESR-0023 WP5. 211 tests pass, validator clean. |
| 1.51 | 16 July 2026 | Claude Engineering Implementer | ESR-0023 WP6 (Engineering Reviewer/Codex reviewed, Programme Sponsor visually confirmed): closed EBG-0073 (UXP Duplicate Monitoring Elements Tidy-up) as Completed - removed the DiagnosticsPanel/SystemHealthPanel Guardian/Sentinel/Providers duplication. First product-code change this session, satisfying PBK-0001's Feature-First Delivery Discipline. |
| 1.50 | 16 July 2026 | Claude Engineering Implementer | ESR-0023 WP5 (Engineering Reviewer/Codex reviewed, Programme Sponsor approved): resolved EBG-0041 (AAM-0001 promoted Draft to Approved, GAM-0001 cross-reference added - see AAM-0001 v0.3). Added EBG-0074 (Wire TrustTierPolicy as SentinelCore's Production Default), High priority, Approved Backlog - the operational gap this validation surfaced: GAM-0001's policy content is not yet connected to the live Guardian runtime. |
| 1.49 | 16 July 2026 | Claude Engineering Implementer | ESR-0023 WP4 (Engineering Reviewer/Codex reviewed, Programme Sponsor approved): closed EBG-0048 (Guardian HITL Governance Specification) as Completed, resolved by GAM-0001 v1.2's extended Section 9. |
| 1.48 | 16 July 2026 | Claude Engineering Implementer | ESR-0023 WP3 (Engineering Reviewer/Codex reviewed, Programme Sponsor approved): closed EBG-0020 (Guardian, Family Safety and Emergency Controls) as Completed, resolved by GAM-0001 v1.1's new Section 8. |
| 1.47 | 16 July 2026 | Claude Engineering Implementer | ESR-0023 WP2 (Engineering Reviewer/Codex reviewed, Programme Sponsor approved): closed EBG-0031 (Guardian Architecture Specification) as Completed, resolved by the new GAM-0001 (Guardian Authority and Boundary Model, v1.0). |
| 1.46 | 16 July 2026 | Claude Engineering Implementer | ESR-0023 WP1 (Engineering Reviewer/Codex reviewed, Programme Sponsor approved): closed EBG-0018 (Provider Abstraction Architecture) as Completed, satisfied by the existing Sentinel provider abstraction (CURRENT_ARCHITECTURE.md, ADR-0018); promoted EBG-0067 (Dropped ADR-0007 Topics) from Candidate to Approved Backlog with a split disposition - UI Architecture Strategy sub-topic judged Superseded, AIEMS Knowledge Architecture/relationship-vocabulary sub-topic confirmed as a genuine live gap and retained as the item's remaining scope. |
| 1.45 | 16 July 2026 | Claude Engineering Implementer | Added EBG-0073 (UXP Duplicate Monitoring Elements Tidy-up): Programme Sponsor observed, during the live visual check of EBG-0072's System Health panel, that it duplicates part of the existing DiagnosticsPanel (both show Guardian/Sentinel/Providers from the same platform.status data). No implementation authorised by this entry. |
| 1.44 | 16 July 2026 | Claude Engineering Implementer | Marked EBG-0070 and EBG-0072 Complete: implemented per EIP-ESR0022-001 v1.0 (Engineering Reviewer approved, Programme Sponsor approved) - production provider wiring and the System Health panel, with the Reviewer's Medium finding (blank model env var handling) incorporated directly. 209 tests pass, validator clean. ESR-0022 WP1. |
| 1.43 | 16 July 2026 | Claude Engineering Implementer | Following an Engineering Reviewer High finding on EIP-ESR0022-001 v0.1 (a retroactive package cannot be genuinely approved), fully reverted the premature implementation of EBG-0070/EBG-0072 (nothing was ever committed) and corrected both entries' status from `In Progress` to `Approved Backlog`, pending EIP-ESR0022-001 v0.2's approval. |
| 1.42 | 16 July 2026 | Claude Engineering Implementer | Added EBG-0070 and EBG-0072 (status `In Progress`, not `Complete`): production provider wiring and the System Health panel, selected via JRM-0001 Track B/C Near-term horizon guidance. **Corrected same-day**: both were drafted and locally tested before an Engineering Implementation Package existed, a self-disclosed process deviation - [[EIP-ESR0022-001_PROVIDER_WIRING_AND_SYSTEM_HEALTH_PANEL|EIP-ESR0022-001]] drafted retroactively for Engineering Reviewer review, status held pending that review and Programme Sponsor final approval. EBG-0071 deliberately skipped here - reserved by JRM-0001 Section 9 for the still-open REG-0001 HST/FCH registration gap. |
| 1.41 | 15 July 2026 | Claude Engineering Implementer | Marked EBG-0069 Complete: JARVIS_CAPABILITY_READINESS_MATRIX.md refreshed to v2.0 at ESR-0021 WP7, following the EBG-0056/PCB-0001 precedent. Selected as this session's next WP per JRM-0001 Track B Near-term guidance - the first backlog decision made using JRM-0001's horizon placement rather than re-deriving priority from scratch. ESR-0021 WP7. |
| 1.40 | 15 July 2026 | Claude Engineering Implementer | Marked EBG-0012, EBG-0027 and EBG-0028 Completed: [[JRM-0001_PROJECT_ROADMAP|JRM-0001]] (Project Roadmap) created at ESR-0021 WP5 and Programme Sponsor-approved (v1.0) 15 July 2026, consolidating all three into one unified roadmap. EBG-0028's phase-content detail preserved as the authoritative source JRM-0001 Track C references rather than duplicates. ESR-0021 WP5. |
| 1.39 | 15 July 2026 | Claude Engineering Implementer | Added EBG-0059 through EBG-0069 (11 new Candidate Backlog items) discovered during the ESR-0021 WP6 historical audit of all HST/FCH history artefacts (Tier 1 only, per Programme Sponsor decision - Tier 2/3 findings recorded in-session only, not added). Most notable: EBG-0059 (Engineering Assurance Capability, a fully-specified but never-implemented independent verification layer from ESR-0012), EBG-0065 (STD-0006 Configuration and Secrets Standard, never created despite being named at project bootstrapping). ESR-0021 WP6. |
| 1.38 | 15 July 2026 | Claude Engineering Implementer | Updated EBG-0055's note recording delivery at ESR-0021 WP4: Knowledge Metrics and Active Clusters panels added to the live UXP, derived from existing knowledge-graph data. No status-label change; no other backlog content altered. |
| 1.37 | 13 July 2026 | Claude Engineering Implementer | Added EBG-0058 (PBK-0001 Clause Consolidation), raised directly by the Programme Sponsor after ESR-0020 closure discussion of the session's two self-disclosed process deviations: rather than adding another incident-specific clause, identify and merge PBK-0001's overlapping restatements of the same underlying principles into fewer, clearer statements. No implementation authorised by this entry. |
| 1.36 | 13 July 2026 | Claude Engineering Implementer | EBG-0056 marked Complete: the Programme Sponsor accepted PCB-0001 v2.1 (In Review to Accepted) at ESR-0020 WP4 closure. |
| 1.35 | 13 July 2026 | Claude Engineering Implementer | ESR-0020 WP4-WP6, reviewed by the Engineering Reviewer (no blocking issues): EBG-0026 (transcript export UX) marked Complete, with the Reviewer's non-blocking timestamp-collision finding recorded for future cleanup; EBG-0056 (PCB-0001 refresh) marked Drafted/In Review pending Programme Sponsor acceptance of PCB-0001 v2.0, with the Reviewer's non-blocking wording-consistency finding recorded. |
| 1.34 | 13 July 2026 | Claude Engineering Implementer | Marked EBG-0051 (Gemini Provider Production Readiness) Complete: the live smoke test against the real Gemini API (item 3, the last outstanding prerequisite) was performed at ESR-0020 WP3 via `scripts/gemini_provider_smoke_test.py`, Programme Sponsor-run - real generated response received, Policy decision Allow, 1 Sentinel decision, 2 audit events. Wiring `GeminiProvider` into a production route remains separate, not-yet-authorised scope. |
| 1.33 | 11 July 2026 | Claude Engineering Lead | Corroborated the ESR-0019 Codex/Business finding on EBG-0057 with direct usage-meter evidence: a monthly limit (resets 11 Aug 2026), shared across Codex/Work/Workspace Agents/ChatGPT for Excel, excluding Chat - 99% remaining after an afternoon of real review work that had it at 100%. Different mechanic from the Plus-tier 5-hour window already documented; strengthens the case against the new Plus subscription being needed. |
| 1.32 | 11 July 2026 | Claude Engineering Lead | Recorded ESR-0019 empirical finding on EBG-0057: Codex ran through the ChatGPT Business account (gpt5.4 mini, medium speed) without consuming workspace credits, refining the earlier no-Codex-capacity conclusion - the bundled standard-seat allowance apparently supports it. New personal Plus subscription now marked not-yet-needed pending confirmation this holds up beyond one session. |
| 1.31 | 11 July 2026 | Claude Engineering Implementer | Marked EBG-0055 Completed (Phase 1, ESR-0019): recorded the backend graph builder, JSON-RPC method, and the Guardian Orb integration (replacing the placeholder glow/ring/label animation with the live knowledge graph itself, per Programme Sponsor direction and the UAM-0001_GUARDIAN_ORB_MOCKUP.jpg reference). Flagged true 3D rendering and live animation as explicitly deferred next candidates. |
| 1.30 | 10 July 2026 | Claude Engineering Lead | Added a concrete Plus-tier rate-limit expectation to EBG-0057 (third-party source, treated as approximate): ~50 soft / ~200 hard cap per 5-hour window, hard caps reachable within 1-2 hours of focused work - matches the Programme Sponsor's own prior Plus experience and confirms the wall to expect is a rate limit, not a bill. |
| 1.29 | 10 July 2026 | Claude Engineering Lead | Recorded the Programme Sponsor's explicit no-new-recurring-budget constraint on EBG-0057: self-hosted/free is the hard default for the Claude<->Codex bridge roadmap unless stated otherwise; reinforces Phase 4 (A2A) and any message broker staying deferred, since both are the only places genuine hosting cost could arise. |
| 1.28 | 10 July 2026 | Claude Engineering Lead | Updated EBG-0057 post-ESR-0018-closure: ruled out browser automation of ChatGPT's web UI (OpenAI ToS violation risk to the Programme Sponsor's actual account), retained the raw-GitHub-URL live-read principle both ChatGPT and Gemini independently converged on, and recorded ChatGPT's verified four-phase venue roadmap (GitHub-native handover first, then a Review Gateway, MCP as tool-access layer, A2A only if multi-agent expansion is justified). |
| 1.27 | 10 July 2026 | Claude Engineering Lead | Updated EBG-0057 with ChatGPT's completed Lead Reviewer code review of the original bridge prototype, reconciled by Claude: explicit sponsor-decision command replacing a trusted text label, gitignored/permission-restricted exchange directory replacing the prototype's false read-only claim, preflight checks, HEAD-drift detection, session IDs demoted to optional convenience. |
| 1.26 | 10 July 2026 | Claude Engineering Lead | Added EBG-0057 (Claude<->Codex Engineering Bridge): recorded the post-appointment architecture and cost decisions, not yet authorising implementation - deferred to a future ESR with its own EIP. |
| 1.25 | 10 July 2026 | Claude Engineering Reviewer | Marked EBG-0053 (EE-0001 Review Gate Compliance Criterion) Adopted, per the dated Section 8 EE-0001 entry from ESR-0018 WP2 - was Candidate Backlog since ESR-0017. |
| 1.24 | 10 July 2026 | Claude Engineering Lead | Added EBG-0056 (PCB-0001 Product Capability Baseline Refresh), flagged at RBL-0013: PCB-0001 is materially stale, still stating Guardian capability is not implemented after WP2/WP9 delivered and verified it live through the real UXP. |
| 1.23 | 9 July 2026 | Claude Engineering Lead | Corrected EBG-0055's scale assumption: confirmed by direct count (~135 markdown artefacts) and the Programme Sponsor's real Obsidian graph screenshot that the mock-up's illustrated node/connection counts are aspirational, not current state. At real scale, rendering performance is not a significant Phase 1 constraint - removed the earlier 2D-only performance caveat; a modest 3D sphere is realistically achievable for Phase 1 too. |
| 1.22 | 9 July 2026 | Claude Engineering Lead | Updated EBG-0050 status from Candidate Backlog to Completed (Foundation Scope) - both phases delivered at ESR-0017 WP9, two sessions ahead of the WP4 roadmap's original schedule; remaining production-hardening scope kept open under the same item. Added EBG-0055 (Knowledge Graph Phase 1 - Static Live Graph), the concrete next slice of the Guardian Orb design direction (UAM-0001 8.1), recommended for the ESR-0018/ESR-0019 capacity EBG-0050's early completion freed up, per Programme Sponsor direction to build this organically in line with planned Work Packages. |
| 1.21 | 9 July 2026 | Claude Engineering Lead | Added ESR-0010's phased achievability roadmap (from FCH-0010's Guardian Orb design discussion, retroactively incorporated into UAM-0001 v1.2) to EBG-0028's note: Phase 1 static live graph, Phase 2 cluster colours/chat UI, Phase 3 agent traversal animation, Phase 4 Guardian reasoning/telemetry connection. |
| 1.20 | 9 July 2026 | Claude Engineering Lead | Added ChatGPT Engineering Reviewer's ESR-0017 WP9 implementation review Minor Finding 2 (handler exception messages should not surface verbatim once external/provider-backed paths exist) to EBG-0050's note. |
| 1.19 | 9 July 2026 | Claude Engineering Lead | Added EBG-0054 (Dev-Environment Setup Automation Expansion), recording ESR-0017's new setup.bat / scripts/setup-dev-environment.ps1 clone-bootstrap tooling and candidate future expansion (cross-platform equivalent, CI parity, environment-doctor mode, version-floor checks). |
| 1.18 | 9 July 2026 | Claude Engineering Lead | Refined EBG-0052 wording with ChatGPT Engineering Reviewer's own confirmation/clarification of the execute-after-approval finding; added EBG-0053 (EE-0001 Review Gate Compliance Criterion), jointly recommended by Lead and Reviewer during ESR-0017 scorecard reconciliation. |
| 1.17 | 9 July 2026 | Claude Engineering Lead | Added EBG-0052 (PBK-0001/EE-0001 Execute After Approval Principle) per Programme Sponsor-reported EE-0001 trial improvement finding for ESR-0017. |
| 1.16 | 9 July 2026 | Claude Engineering Lead | Added EBG-0051 (Gemini Provider Production Readiness) per ChatGPT Engineering Reviewer's ESR-0017 WP3 observations: richer response parsing, extended metadata, and a required live smoke test before Gemini is enabled as a production provider. |
| 1.15 | 9 July 2026 | Claude Engineering Lead | Added ChatGPT Engineering Reviewer's ESR-0017 WP2 Observation 2 (consider a streaming converse() interface at EBG-0050 implementation time) to EBG-0050's note. |
| 1.14 | 9 July 2026 | Claude Engineering Lead | Added explicit failure-mode implementation requirements (backend crash, restart policy, version negotiation, protocol compatibility, IPC timeout strategy) to EBG-0050, per ChatGPT Engineering Reviewer's ESR-0017 WP1 Recommendation 3. |
| 1.13 | 9 July 2026 | Claude Engineering Lead | Added EBG-0050 (UXP-Backend Bridge Implementation) per ADR-0019 / ESR-0017 WP1. |
| 1.12 | 9 July 2026 | Claude Engineering Reviewer | Added EBG-0049 (Cost-Aware Provider Routing and PEM-001 Revisit) per Programme Sponsor request following post-ESR-0016A discussion on balancing JARVIS running cost against performance. Cross-referenced with EBG-0045 (overlapping scope, not a duplicate) in both directions. |
| 1.11 | 8 July 2026 | Claude Engineering Implementer | Marked EBG-0030 Sentinel Architecture Specification Completed, satisfied by ADR-0018 and the Guardian/Sentinel boundary defined in ESR-0014. EBG-0047 left as Candidate Backlog - trust tiers and platform-entry validation are not confidently satisfied by the current Sentinel Core implementation. |
| 1.10 | 2 July 2026 | Codex Engineering Implementer | Added retrospective OSE relationships to improve semantic traceability without changing backlog governance. |
| 1.9 | 2 July 2026 | Codex Engineering Implementer | Completed EBG-0039 by moving prototype JARVIS chat exports into logs/chats/ runtime evidence archive. |
| 1.8 | 2 July 2026 | Codex Engineering Implementer | Added ESR-0008 closure candidate backlog items for UXP, Guardian identity, Agent Framework, Engineering Ecosystem Synchronisation, Obsidian/OSE, strategic value, device independence, Sentinel and Guardian governance. |
| 1.7 | 1 July 2026 | Programme Sponsor & Chief Engineering Advisor | Added ESR-0006 follow-up backlog items for AIEMS standards review, JARVIS runtime chat archive and repository integrity troubleshooting playbook. |
| 1.6 | 30 June 2026 | Programme Sponsor & Chief Engineering Advisor | Added ESR-0005 closure backlog items for export workflow, product roadmaps, Sentinel and Guardian architecture, context activation and engineering package authority guidance. |
| 1.5 | 29 June 2026 | Programme Sponsor & Chief Engineering Advisor | Added recovered knowledge promotion candidate backlog items from ESR-0004 EIP-EKR-0001. |
| 1.4 | 29 June 2026 | Programme Sponsor & Chief Engineering Advisor | Marked STD-0004 Validation and Quality Assurance Standard backlog item completed following ESR-0004 EIP-ESR0004-02. |
| 1.3 | 29 June 2026 | Programme Sponsor & Chief Engineering Advisor | Added deferred candidate backlog item for future WP0 naming consideration following ESR-0004 WP3. |
| 1.2 | 28 June 2026 | Programme Sponsor & Chief Engineering Advisor | Consolidated future validation, capability, automation and EIP/session standardisation backlog themes from ESR-0003 EIP-R4. |
| 1.1 | 27 June 2026 | Programme Sponsor & Chief Engineering Advisor | Marked ADR-0004 and ADR-0005 recovery backlog items completed following ESR-0003 EIP-R2. |
| 1.0 | 27 June 2026 | Programme Sponsor & Chief Engineering Advisor | Initial Engineering Backlog Register created with ESR-0001 deferred work items. |

---
